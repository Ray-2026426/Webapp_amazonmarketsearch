# 上线检查清单（M6 · 商用收口）

> 用途：正式对外前逐项过一遍。**每项都写清"怎么验"**，不接受"应该没问题"。
> 标记说明：`[自动]` = 有代码/测试兜底；`[人工]` = 必须人工确认一次；`[用户侧]` = 只能由你在真实环境执行。

## 0. 一句话前提

这个产品的北极星是**找出有证据支撑的未满足需求，并给出 0-N 个可决策的机会**。
上线检查的核心不是"功能有没有"，而是**结论有没有证据、能不能被推翻**。

---

## 1. 环境与密钥（`[自动]` + `[人工]`）

| # | 检查项 | 怎么验 | 预期 |
| --- | --- | --- | --- |
| 1.1 | 生产环境变量齐全 | 管理员登录 →「设置 → 管理员后台 → 环境自检」 | Supabase、`APP_JWT_SECRET`、`ADMIN_EMAILS`、`SERVICE_ROLE_KEY`、各 provider 均显示"已配置 + 指纹" |
| 1.2 | 密钥不回显到浏览器 | 同上；另 `[自动]`：`tests/securityKeys.test.ts` | 面板只显示指纹；测试全绿 |
| 1.3 | 数据池密钥可用 | 看竞对页点「抓取全字段（走服务端数据池）」 | 有进度、有字段抓取率，未出现"未配置密钥" |
| 1.4 | 游客被正确拦下 | 退出登录后点任意取数按钮 | 提示"数据池只对登录用户开放…可用示例数据"，不静默失败 |
| 1.5 | 浏览器里没有密钥残留 | DevTools → Application → Local Storage | 无 `amzdev_admin_keys`；`amzdev_admin_key_status` 只有状态与指纹 |

## 2. 数据库与迁移（`[人工]`）

| # | 检查项 | 怎么验 | 预期 |
| --- | --- | --- | --- |
| 2.1 | 迁移已执行 | Supabase SQL Editor 跑 `supabase/migrations/all_in_one.sql`（含 008） | 无报错；`usage_events`、`audit_events` 表存在 |
| 2.2 | RLS 正确 | 用 anon key 直接查 `usage_events` / `audit_events` | 查不到任何行（策略为全部拒绝，仅 service_role 可读写） |
| 2.3 | 备份策略 | Supabase → Database → Backups | 至少保留日备；确认恢复流程演练过一次 |

## 3. 权限（`[自动]` + `[人工]`）

| # | 检查项 | 怎么验 | 预期 |
| --- | --- | --- | --- |
| 3.1 | 管理员后台仅管理员可见 | 用普通账号登录 → 设置 | 看不到「管理员后台」「团队空间」两个 tab |
| 3.2 | 接口层也拦 | 用普通账号 token 调 `/api/admin/health` | 403「需要管理员权限」 |
| 3.3 | 团队角色语义一致 | 设置 → 团队空间：邀请/改角色/移除 | owner 不可被改/移除；admin 与项目成员权限一致 |
| 3.4 | 不能禁用自己 | 管理员后台 → 用户管理 → 对自己点禁用 | 明确拒绝（避免把自己锁在门外） |
| 3.5 | **自己就是管理员（开通方式）** | 见下方「3.0 怎么把自己设成管理员」 | 设置里能看到「管理员后台」与「团队空间」 |

**3.0 怎么把自己设成管理员（2026-09 修过一个真 bug）**

管理员身份 = **服务端环境变量 `ADMIN_EMAILS`（逗号分隔的邮箱白名单）**，这是唯一权威来源。
服务端在 `login / register / me` 响应里下发 `isAdmin`，前端据此显示两个 Tab。

> 曾经的真实故障：`AiSettingsPanel` 里写的是 `isAdminSession(undefined)`，而该函数第一步就是 `Boolean(user) && …`
> → **恒为 false**，导致「管理员后台 / 团队空间」对所有人（包括你）都不显示，无论白名单怎么配。
> 已修，并加 `tests/adminGate.test.ts` 7 条断言守死（含"全仓库不得再出现 isAdminSession(undefined)"）。

开通步骤（二选一，**都不需要重新部署**）：

| 环境 | 做法 |
| --- | --- |
| 线上（Vercel） | Project → Settings → Environment Variables 加 `ADMIN_EMAILS=你的邮箱`（Production 与 Preview 都勾上）→ 保存后**刷新页面**即可（`/api/auth/me` 会用 token 重新确认身份）。无需 Redeploy。 |
| 本机 | `.env.local` 加 `ADMIN_EMAILS=你的邮箱` → **重启 dev server**（服务端变量在启动时读入）→ 刷新页面。 |

注意两点：
1. 别再往 `VITE_ADMIN_EMAILS` 上想办法 —— 它是**构建期**注入：既要把管理员邮箱明文打进公开 JS 包，又必须重新部署才生效。它现在只作为老响应格式的兜底保留。
2. 前端藏 Tab 只是体验，**接口层同样会拦**（`api/admin/*` 与 `api/data/*` 的管理员动作都校验 `isAdminEmail` 并返回 403）；所以"看不到 Tab"不等于"没有权限"，"改前端也没用"。

## 4. 数据池与成本（`[自动]` + `[用户侧]`）

| # | 检查项 | 怎么验 | 预期 |
| --- | --- | --- | --- |
| 4.1 | 记账在跑 | 抓一次数 → 管理员后台 → 用量统计 | 事件数/外部调用/缓存命中有数，成本可估 |
| 4.2 | 缓存降本生效 | 同一 ASIN 连续抓两次 | 第二次显示缓存命中，成本为 0 |
| 4.3 | 配额生效 | 管理员后台给某用户设月配额后超额调用 | 返回 429 并说明"已达到本月配额" |
| 4.4 | 月度成本可度量 | 管理员后台用量统计（按工具/项目/用户） | 能回答"这个月钱花在哪"，并给出预算对比 |
| 4.5 | 🚧 未做：持久化缓存 | — | 当前为进程内缓存（实例回收即丢），接口不变、后续替换 Supabase 表 |

## 5. 结论质量（`[自动]`）

| # | 检查项 | 怎么验 | 预期 |
| --- | --- | --- | --- |
| 5.1 | 评分不由 AI 决定 | `tests/opportunityPlan.test.ts` | 五维 25/25/25/15/10，AI 给的分数被忽略 |
| 5.2 | 允许并强制 0 机会 | `tests/opportunityConclusion.test.ts` | 数据支持机会时**不产出**零结论；有卡点时必须写明卡在哪一环 |
| 5.3 | 结论可追溯 | 看机会 → 展开任一张卡的"证据" | 每条证据带 `sourceRef`，能点回来源 |
| 5.4 | 硬约束只降级不抹杀 | `tests/competitorAnalysis.test.ts` | 硬约束未过时仍给赢的路径，但最高"验证后进入" |

## 6. 性能与体验（`[用户侧]`）

| # | 检查项 | 怎么验 | 预期 |
| --- | --- | --- | --- |
| 6.1 | 项目中心 ≤2s | 跑几次项目中心 → 管理员后台 → 性能基线 | 中位数 ≤2000ms，面板直接显示达标与否 |
| 6.2 | 10 分钟跑完五看 | 操作手册 §2 的计时流程 | 首次用户 10 分钟内跑到机会卡（见 `M6-user-manual.md`） |
| 6.3 | 4 视口不破版 | 390×844 / 768×1024 / 1280×800 / 1440×900 | 无横向滚动、无遮挡 |
| 6.4 | 导出件可直接发管理层 | 看机会 → 导出 HTML / MD | HTML 离线可开；MD 含评分拆解、证据、路线图、控制点 |

## 7. 合规与法务（`[人工]`）

| # | 检查项 | 预期 |
| --- | --- | --- |
| 7.1 | 数据来源合规 | 抓取仅为内部调研用途，符合目标站点 ToS；不对外分发原始数据 |
| 7.2 | 账号与隐私 | 只收集登录所需信息；不采集与业务无关的个人信息 |
| 7.3 | 密钥与凭证管理 | 密钥只在服务端；离职人员立即移出 `ADMIN_EMAILS` 并轮换密钥 |
| 7.4 | 审计可查 | 关键操作（配置、禁用户、删项目、导出）在审计日志里可查 |

## 8. 上线前必须由你完成的动作（`[用户侧]`）

1. **3 份真实品类决策包**（HTML/MD）交管理层评审 —— 用真实项目跑完五看，导出后发出去；
2. **10 分钟计时可用性测试** —— 找一位没参与开发的同事，按操作手册从零跑到机会卡并计时；
3. **性能基线读数** —— 跑几次项目中心，把「性能基线」里的中位数记下来（这是 §12 M5 的验收线）；
4. **吊销本轮用过的 GitHub PAT**，并确认生产环境密钥与本地不同。

---

_本清单与 PRD §11/§12/§15 配套；任何一条不达标，先修再上。_
