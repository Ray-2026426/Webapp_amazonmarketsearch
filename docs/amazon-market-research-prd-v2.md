# Kairo（亚马逊市场调研 App）· 商用化改革 PRD（V2.10）

| 项 | 内容 |
| --- | --- |
| 文档版本 | V2.10（= V2.9 上线 + 用户指示的四项改动：背景信息与看自己重构 §15.19、删除项目归档与分支收口 §15.20、线上管理员 500 的两个根因 §15.21、MCP 简化与二级页标签诚实性 §15.22） |
| 产品名 | **Kairo**（用户确认，替换原 "AmzDev Tool"） |
| 起草 | 产品负责人（AI PM） |
| 评审对象 | `main` 分支（8-27 封版）、`phase-0` 分支（8-30 封版）、本地工作区（9-5/9-6，phase-0 延续） |
| 评审依据 | phase-0 五份文档（PRD / 产品总纲 / 五看重构计划 / 对抗式审查 / UI 优化清单）+ 本地源码对抗式审查 + 用户多轮共 22 项决策 + 6 轮修改要求确认 |
| 上一版本 | `docs/amazon-market-research-prd.md`（V1，phase-0 分支） |
| 状态 | 待用户终审（已吸收全部 22 项决策 + 6 轮修改要求，见 §15） |

> 配套文件：
> - `docs/ui-wireframes.html` —— **五看主流程 8 屏**（项目中心 / 向导 / 看用户 / 看市场 / 看竞对 / 看自己 / 看机会 / 导出）
> - `docs/ui-wireframes-l3.html` —— **二级页与明细层 23 屏**（搜索路径/偏好/需求树证据链/细分标准/细分完整大盘/方案对比/竞对深度/主图逐张/流量结构/评论画像/背景库/题目依据/机会详情/路线图/关键词/VOC/竞品/利润(侧栏)/大盘明细(侧栏)/机会扫描器(并入大盘)/报告/登录/管理员后台）

---

## 0. 术语说明

本文统一使用用户确认的"五看"命名：**看用户 → 看市场 → 看竞对 → 看自己 → 看机会**。
既有代码与文档中"看竞品 / 找机会 / 竞品分析 / 竞争空白扫描器"等叫法，在 V2 中按第 5.4 节术语表统一。

---

## 1. 执行摘要（TL;DR）

**一句话**：这个 App 已经完成了从"五模块分析工具"到"五看工作台"的方法论演进（phase-0 是及格的 0→1），但它现在是一台"零件齐全、装配松散"的机器——**数据联动靠手动搬运、AI 引擎写了没接、市场细分与看用户脱钩、竞对三列对比只有挑选没有对比、机会卡没有行动路线图**。商用化的改革不是加功能，而是做三件事：

1. **把"五看"从表单页改造成向导式决策流**——用户只做选择题和判断题，AI 负责取数、计算、起草（≤10 分钟出 Go/No-Go 结论）。
2. **把"需求分类"做成贯穿五看的唯一索引**——看用户产出需求与细分逻辑，看市场按它做细分，看竞对按它做满足矩阵，看自己按它做适配，看机会按它出卡。消灭全局缓存暗链。
3. **把"机会卡"升级为"决策包"**——每张机会卡不仅说"有没有机会"，还要输出**先做什么、后做什么、前置资源、控制点、Go/No-Go**。

**现状评分**：main 分支 = 工具集（4/10 的商用基础）；phase-0 = 五看闭环已通（6/10）；本地工作区 = 骨架已通血肉未连（**5/10，不能商用**）。

**已确认的 22 项商业决策**（见 §15.1）：内部团队自用为主顺带产品化；平台统一数据池；≤10 分钟 AI 向导；向导式推进；竞对自动抓取+三列对比；看自己六维度全选；在线决策看板+导出；6 个月打磨完整版；**产品名 Kairo；取数口径分档（词 TOP200 / 评论 60 条快速·200 条深度 / 竞对 3 个）；导出仅 HTML+MD；每一轮交付用户自测；双模式；数据双通道（小数据抓、大数据传）；评论构成与缓存；人优我廉须在红线内；新人=表现最好的代表；示例品类薄枕头；一键向导+每步局部重跑；管理员后台 M5；立即修复 Supabase（M0）。**

**改革后的成功标准**（北极星，§3）：
- 新用户（配好数据池）从建项目到拿到"有没有机会 + 机会路线图" ≤ **10 分钟**；
- 机会结论 **100% 证据可追溯**（每条结论可点回原始关键词/评论/图表/ASIN）；
- 机会卡 100% 含执行路线图（先做/后做/前置资源/控制点）；
- 连续 10 个真实品类回放测试中，**0 次**把"证据不足"误报为"有机会"，**0 次**把"无机会"误报为"证据不足"。

---

## 2. 现状评估（对抗式审查结论）

### 2.1 三份家底

| 版本 | 形态 | 强项 | 致命短板 |
| --- | --- | --- | --- |
| `main`（8-27） | 五模块工具集：市场大盘 / 关键词 / 竞品对比 / 评论 VOC / 利润计算器；游客模式 + 示例数据 | 工具层厚：大盘图表、ABA 意图分层、竞品主图抓取、VOC 打标、利润模型都可用 | 无项目载体，退出丢结果；模块间无方法论关系；AI 报告复述数据 |
| `phase-0`（8-30，领先 main 68 提交） | 五看项目化工作台：项目中心 → 非线性五看 → 机会卡 → HTML 审核报告；需求主线贯穿；29/29 测试通过 | 方法论成型：需求分类索引、细分评分、竞对三类挑选、0-N 机会+反证、链式失效标记、HTML 报告 | 五看靠手填；AI 引擎零调用；细分与工具层仍割裂；26 条 P0 UI 问题（文档自列） |
| 本地工作区（9-5/9-6） | phase-0 延续 + 市场细分评分（趋势/体量/竞争）+ 竞品自动挑选 + 云同步/成员/资产 | 确定性评分算法落地（segmentScore/competitorPicker 有测试）；五看视图齐全 | 10 条断链（§2.3）；跨项目数据污染；demo 态五看空转 |

### 2.2 五看成熟度评分（本地源码审查）

| 看 | 分 | 现状 | 主要扣分点 |
| --- | --- | --- | --- |
| 看市场 | 5.5/10 | 数据上下文 + 细分评分可用 | 细分数据全局化跨项目污染；细分卡片的选择回调是死接口；openQuestions 无消费者；AI 未接入 |
| 看用户 | 4/10 | 表单结构完整；候选→机会卡是唯一硬联动 | 关键词/VOC 结论只以 count 进入；证据链空心（evidenceIds 全空）；无搜索路径/搜索偏好产出 |
| 看竞对 | 5/10 | 头部/跟随/新人自动挑选规则清晰、可测 | 挑完只是文本列表，不进对比池；无三列对比视图；需求满足矩阵是自由文本 |
| 看自己 | 6/10 | 42 题模板覆盖全；进度与 stale 规则干净 | "适配度=完成度%"（填"不具备"也得满分）；42 题长滚屏；无汇总结论视图 |
| 看/找机会 | 6.5/10 | 评分公式透明；矩阵/决策/评审/云同步齐备；上游变更自动标 stale（亮点） | 无"先做/后做/前置资源/控制点"；风险/验证动作被降级为单行文本；55/100 分只反映"表单填没填" |
| **整体** | **5/10** | 方法论骨架已通 | "看"与"工具"之间是 IDB 暗链 + 手动搬运，数据联动远未闭环 |

### 2.3 最重要的 12 条断链 / 缺陷（V2 必须全部修复）

1. **五看 AI 引擎整体断链**：`lookAi.runLookAnalysis`（含四个看的结构化 Prompt 与 JSON 回填）零调用，五看全部退化为手填表格，"AI 驱动五看"未兑现。
2. **细分数据全局化、无项目快照**：SegmentScoreCards / CompetitorPickerPanel 读全局 IndexedDB，跨项目数据污染；"看市场"细分卡片的选择/捕获回调是死接口。
3. **机会卡无行动路线图**：缺"先做/后做/前置资源/控制点"；优先级矩阵只分象限不给行动顺序；RiskItem / ValidationAction 的 owner、severity、successCriteria 字段类型齐全但 UI 全部丢弃。
4. **竞对三列对比缺失**：头部/跟随者/新人角色只存在于挑选面板；挑好的 ASIN 不自动进入 CompetitorHub 对比池，用户手动重输。
5. **看用户证据链空心**：VOC/关键词工具结论（tagLib、5W1H、JTBD）只以 count 进入看用户；"捕获证据"只是元数据快照。
6. **评分通胀**：机会分中市场/竞品/自身共 55 分只反映页面完成度，与结论质量无关；看自己填"不具备"与填"已具备"得分相同。
7. **"待验证问题"承诺未兑现**：看市场 openQuestions 声称流向后续视角，实际无消费者。
8. **示例数据下五看空转**：demo 加载时主动清空细分（App.tsx 381-390），自动挑选与细分评分在演示态不可用，新用户第一印象是"功能不可用"。
9. **潜伏 bug**：`competitorWorkspace` 键只读不写；isDemo 正则永假；workspaceIdb 白名单缺该键。
10. **上下文割裂的导航**：五看→工具跳转丢失项目上下文（无面包屑、无参数、工具不认项目）；术语三套并存（竞品分析/竞品对比/看竞对；机会卡/黄金坑位/细分机会分）；ProjectCenter 死接线 props + 站点列表重复条目。
11. **市场准入评分卡被默认关闭**（§2.7.1）：`showMarketScorecard` 默认 `false`。**处置：保留在大盘内原样（含开关），不在看市场主屏强推**，看市场主屏用确定性细分机会分做结论。
12. **导出/交付管线分散**：PDF/Excel/飞书/MD 分散在 6 个组件，统一报告页时最易只留一个（§2.7.3 已建矩阵逐条保留）。

### 2.4 phase-0 文档已确认的 26 条 P0 UI 问题（归纳为 5 类）

1. **项目中心像管理后台**（P0-1）：全局统计噪声；项目卡不显示"研究到哪了、当前结论是什么、下一步点哪里"。
2. **五看被表达为五个平级 Tab**（P0-2）：表达不了业务主线（用户分类→市场细分→竞对缺口→机会）；跳工具断上下文、返回丢状态。
3. **五看内容定义错位**（P0-3）：看用户首页以关键词/评论数量为核心而非需求分类；看市场是泛品类总览而非"需求细分评分列表"；看竞对是并排 ASIN 数据列而非需求×竞品满足矩阵；看自己重复填通用长文本；看机会候选无法确认成正式机会。
4. **AI 透明度不足**（P0-4）：缺"结论/证据/推理"二级页；评分未绑定机会自身证据；Prompt 需可维护有版本。
5. **30 分钟闭环的 UI 细节**（P0-5）：缺数据无提前提示；一页多主标题；卡片套卡片；文字过小；状态只靠颜色；关键内容永久截断；空状态不解释；主按钮位置不统一；HTML 报告不是独立审核交付物；1024px 宽度挤压。

### 2.5 环境与部署阻塞项（P0）

> **根因更正（2026-09，用户核实）**：Supabase 项目**不是被删除，而是被免费额度"闲置自动暂停"**（约 7 天无活动触发）。用户在控制台 **Resume** 后，项目 DNS 立即恢复解析（`fbldvwzoqlewoistybto.supabase.co` → Cloudflare IP）、Auth 服务正常响应、数据与存储对象完好（Supabase 原话："所有数据，包括备份和存储对象，均安全无虞"）。
> **我此前的误判**：看到该域名 `DNS name does not exist`，就断言"项目已删除"。事实上**暂停的项目子域名也会停止解析**，两种解释都成立，而我选了更严重的那种——教训：下"不可逆"结论前必须先排除"可恢复"的可能（如暂停/欠费/迁移）。

| # | 问题 | 证据 | 影响 |
| --- | --- | --- | --- |
| 1 | **免费 Supabase 项目闲置约 7 天会自动暂停**（**本次故障真因，且会复发**） | 用户控制台截图："'Ray-2026426的项目'已暂停…可在 2027-10-12 前恢复"；暂停期间项目子域名停止解析，全环境登录失败 | 登录、注册、云同步全线失效；**表现极像"项目被删"，极易误判** |
| 2 | **多环境共用同一个硬编码库** | `api/auth/_shared.ts` 曾写死默认 `SUPABASE_URL`/publishable key；未配环境变量时静默回落到该值 | 预览/本地/生产互相污染；项目一旦异常全环境一起崩 |
| 3 | **本地 dev 无 `/api` 路由** | `vite.config.ts` 只代理 `/api-proxy/*`；`server/sellerspriteMcpProxy.ts` 只挂 `/api/sellersprite*` | `npm run dev` 下登录/注册/云同步/成员/资产全部不可用（须 `vercel dev` 或部署后验证） |
| 4 | **登录失败不可诊断** | 前端把错误统一抛为"登录失败"，无配置自检 | 用户无法区分"密码错 / 配置缺 / 服务不可用 / 项目被暂停" |
| 5 | **本地 `.env.local` 未配 `SUPABASE_SERVICE_ROLE_KEY` 与 `APP_JWT_SECRET`** | 变量清单核对 | 无 service key 时注册走 `signUp`，项目若开启邮箱确认则注册失败；JWT 回落到 `dev-insecure-secret`（安全风险） |

**M0 修复清单（✅ = 本次已完成并验证）**：

1. ✅ **删除代码里的硬编码默认 Supabase URL/Key**（`api/auth/_shared.ts`）：未配置就明确报"未配置"，从此不再静默连某个固定项目；
2. ✅ **新增 `/api/health` 配置自检**（`?probe=1` 附带连通性探测，不回显密钥）：把"登录失败"变成"指出缺哪项配置 / 服务是否可达"；
3. ✅ **新增 `/api/keepalive` + Vercel Cron 每日保活**（`vercel.json` 的 `crons`）：**直接针对第 1 条根因**，防止免费项目再次闲置暂停；
4. ✅ **本地开发补 `/api` 中间件**（`server/devApiPlugin.ts` + `vite.config.ts`）：`npm run dev` 下登录/云同步可用；
5. ✅ **新增 `.gitignore`**：仓库是公开的，挡住 `.env.local`、`.vercel*`（含生产 service_role key）等敏感文件；
6. ✅ **前端「设置 → 诊断」面板**（`src/components/SystemDiagnosticsPanel.tsx`，接在设置第 6 个 Tab）：读 `/api/health`，逐项显示配置状态与"**缺什么会坏什么**"，并提供连通性探测与按概率排序的排查建议；
7. ⏳ **本地 `.env.local` 补 `SUPABASE_SERVICE_ROLE_KEY` / `APP_JWT_SECRET`**（用户侧，可选但建议）。

**云端步骤（本次多数不需要）**：项目 Resume 后无需新建、无需重跑迁移、无需改环境变量；原清单保留在 `docs/M0-setup-checklist.md`，仅在"项目真的被删除"时按它执行。

### 2.6 数据落位（现状 vs 目标）

| 数据类型 | 现状存放 | V2 目标 |
| --- | --- | --- |
| 账号 | Supabase Auth（`signInWithPassword` / `admin.createUser`）——**项目已失效** | 新 Supabase 项目 + 服务端 API 认证 |
| 项目 / 五看 / 机会卡 | 浏览器 IndexedDB（workspaceIdb）为主 + Supabase `projects.data` 整包 JSON 云同步 | 本地优先 + 云同步，按 workspace/项目严格隔离（§7.2） |
| 成员与权限 | Supabase `project_members` + RLS + RPC（`push_project_if_revision` 乐观锁） | 保留并扩展角色（owner/editor/viewer） |
| 报告资产 | Supabase Storage bucket（`007_project_assets.sql`） | 保留 |
| AI / MCP 密钥 | 管理员账号 `user_metadata.appKeys`（服务端）+ 环境变量兜底 | 统一数据池（§11.2）+ 管理员后台管理（§11.4） |
| 演示数据 | 本地内置（`demoData.ts`） | 预置真实品类（薄枕头）贯穿五看 |

> 结论：**当前所有业务数据的第一副本都在你自己的浏览器里**（IndexedDB）；云端那一份随 Supabase 项目失效已不可用。清缓存/换浏览器即丢失。

### 2.7 老版资产清点与归位（禁止"能力静默丢失"）

> 清点范围：`src/components` 59 个文件 + `src/utils` 39 个模块 + `App.tsx`（2024 行），共约 2.4 万行。引用关系由全量 import 边推导。

#### 2.7.1 总量结论

**老版能力总量完好，没有"已删除"的功能**：五大模块 = 20 个图表 + 4 张明细表 + 3 套报告 + 4 类导出，全部在线。唯一真死代码是 `CompetitorAnalysis.tsx`（621 行，无人引用；其 `competitorArchiveParser` 已被 CompetitorHub 复用，能力未丢）。

但发现 **3 处"能力从未接线"**（不是丢失，是从未生效）——这是比丢失更隐蔽的问题：

| # | 未接线项 | 代码证据 | V2 必修 |
| --- | --- | --- | --- |
| 1 | **五看 AI 引擎零调用** | `lookAi.runLookAnalysis`(L202) 与 `gatherLookPresence`(L302) 导出但零调用方 | M1 接线，否则搬迁完仍是手填表格 |
| 2 | **`competitorWorkspace` 只读不写** | `lookAi.ts:69` `get('competitorWorkspace')`，全仓无 `set`；`workspaceIdb.ts` 白名单缺该键 | M1 写入项目快照 |
| 3 | **市场准入评分卡被默认关闭** | `mcpConfig.ts:477` `showMarketScorecard` 默认 `false`，门控 `App.tsx:1614-1616` | 保留在大盘内（原样，含默认关闭开关），不在看市场主屏强推；看市场主屏用确定性细分机会分做结论 |

另发现一处入口缺口：`ProjectWorkspace.tsx:187-196` 的 `toolButtons` 映射不完整——`opportunity`（看机会）分支返回 `[]`。**利润计算器改为侧栏工具后此缺口自然消失**（不再需要从看机会内打开）。

#### 2.7.2 老版五大模块能力 → V2 归位（模块级）

| 老版模块 | 完整能力（用户可见） | V2 归位 |
| --- | --- | --- |
| **市场大盘**（App.tsx:1609-1804） | KPI 三行 13 卡（销额/销量/均价 + 品牌数/Top10 垄断度/评论数/评论增长/评分 + 卖家数/重量/体积/新品渗透/FBA 费率）；`DateRangeSelector`（今年/去年/单月/自定义，输出 当前·环比·同比 三组月份）；13 个图表：细分份额饼图、市场趋势（月/季/年）、季节热力、集中度（HHI+Top3/5/10）、**机会扫描器**（价格×销量散点+空白区）、品牌排行（含近 6 月趋势/定位标签/**导出 Excel**）、BSR 分布（7 档）、**价格-评分四象限**（核心黄金机会/价格带断层/红海/垃圾场）、价格带分布（5 档桶宽/双指标/累计线）、上架时间、新老品（4 档阈值）、评分分布、卖家类型、卖家所属地（中国 vs 本地趋势）；`TopProductsTable`（13 列排序/搜索/分页/月度聚合/勾选对比≤5/导出 Excel/AI 单品分析/历史趋势/跳竞品对比）；`MarketScorecard`（8 维度加权准入评分，**默认关闭**）；数据质量面板；`SegmentationManager`（1626 行：手动+AI 分类、包含/排除词 OR/AND 圈选、三层细分、细分描述编辑、批量操作、词云、导出）；AI 深度市场报告（生成/编辑/重生成/存 PDF/飞书）；工作区历史 12 条快照 | 二级页⑤细分完整大盘 + 侧栏⑲全市场大盘（照搬原样）；MarketScorecard → 保留在大盘内（原样，默认收起）；机会扫描器 → 保留在大盘内（原样，不单独成页）；细分管理器 → 二级页④⑥；报告 → ㉑；快照 → 项目级 |
| **关键词分析** | Excel 上传/MCP 抓取（ASIN 流量词 or 种子词 ABA，前 50~全部）；AI 一键分析（意图分层 + JTBD + 8 类标签，分块进度）；意图漏斗+策略结论、JTBD 气泡地图（任务/关键词双模式）、词类分布、场景×人群热力、4 张 TOP10 榜；原始词表 8 列（词/翻译/搜索量/CPC/意图/JTBD/场景人群/AI 标签，可搜索/筛选/行内编辑/分页）；**导出 Excel（3 sheet）**；AI 用户洞察报告 + 飞书 | 明细层⑮关键词明细（原样保留 + 项目上下文）；结论进看用户 ①② |
| **用户洞察 / VOC**（2212 行） | 评论上传（列映射扫描）/MCP 抓取（40/100/200/400 条）；Step1 标签库生成 → Step2 自动打标（好评点/差评点/场景/人群）；标签可视化（单标签按月趋势、TOP 标签月度对比、标签国家分布饼图）；四维度标签筛评论 + 五星/差评/媒体/VP/国家/日期/有用数筛选 + 排序 + AI 翻译 + 图文视频预览 + 分页；Step3 VOC 深度报告；**Step4 用户旅程 5W1H（时间轴/表格双视图）**；飞书 | 明细层⑯评论 VOC 明细（**整页搬迁，禁止重写**）；结论以结构化 Evidence 回填看用户 ②③ 与看竞对 ⑩ |
| **竞品对比**（CompetitorHub 1850 行） | 三步向导（选 ASIN ≤5 / 上传 Listing 图包 zip / 对比结果）× 三 Tab：Listing 详情（**主图逐张并列**、标题、五点、**A+/附图逐张**、评级评论、价格与运营徽章、规格变体、买家务视角卡）、流量（结构对比 + 每 ASIN 流量词明细：占比/自然排名/广告排名/自然广告比）、产品矩阵（父子变体矩阵、品牌同门 ASIN 卡）；单品 AI 分析（带缓存）；竞品 AI 综合报告（+飞书+MD）；竞品快照 10 条 | 看竞对主屏三列对比 + 二级页⑦⑧⑨ + 明细层⑰（原样保留 + 自动接收 ASIN 池） |
| **利润计算器**（955 行） | 输入（国家/汇率/VAT、多变体增删、采购+头程+仓储、佣金/FBA、CPC/CVR/广告占比、自然广告订单拆解、盈亏平衡价）；**盈亏敏感性**（CVR/价格/CPC 承压）；**目标倒推**（15/20/25% 毛利对应售价）；**组合诊断**（变体占比/利润贡献/安全边际/建议、父体加权+饼图）；方案存档 | **侧栏工具（首页左侧·项目中心下，原样照搬）**；不进任何项目/二级页 |

#### 2.7.3 导出与交付能力矩阵（分散在 6 个组件，统一时最容易只留一个）

| 导出能力 | 现状位置 | V2 落位 |
| --- | --- | --- |
| 存为 PDF | `App.tsx:1500-1508`（大盘）、`MarketAnalysisReport` | ⑤/⑲/㉑（用户已确认导出以 HTML+MD 为主，PDF 作为浏览器打印兜底） |
| 导出 Excel | 品牌榜、TopProductsTable、关键词明细（3 sheet） | 各表格**原位保留** + ㉑ 报告页汇总入口 |
| 飞书推送 | `FeishuPushButton`（4 处调用） | 全局保留（所有报告页） |
| Markdown 下载 | `reportToMarkdown`（6 处调用） | ㉑ + 各报告原位 |
| 机会决策包 HTML | `opportunityHtmlReport`（phase-0） | ⑮ 决策包主出口 |

#### 2.7.4 死代码与孤儿（7 条，处置必须显式）

1. `CompetitorAnalysis.tsx`（621 行）无人 import → **删除组件，保留 `competitorArchiveParser`**（CompetitorHub 在用）。
2. `ProjectWorkspace.tsx` L342-425 `LookWorkspacePlaceholder` + L67-93 `LOOK_SCOPE` 定义但从不渲染 → 删除。
3. `lookAi.runLookAnalysis` / `gatherLookPresence` 零调用 → **不是删，是接线**（M1）。
4. `supabaseConfig.ts` 仅被测试引用 → **接回启动自检**（对应 §2.5-4 诊断面板）。
5. `App.tsx:574` 恒等三元 `competitorWorkspace?.hasResult ? competitorWorkspace : competitorWorkspace` → 删。
6. `lookAi.ts:97` `isDemo` 正则对站点码恒为 false → 修。
7. `workspaceIdb.ts` 白名单缺 `competitorWorkspace` → 补。

#### 2.7.5 最易丢失的老版能力 Top 10（搬迁时必须逐条核对）

| # | 风险 | 保留方案 |
| --- | --- | --- |
| 1 | **默认关闭的 MarketScorecard** 静默消失 | 保留在大盘内（原样，默认收起），不在看市场主屏强推 |
| 2 | **Prompt 中台 14 个预设**藏在设置第 5 个 Tab | 保留 ㉕ 入口 + 补 4 个 phase0-v1 预设 |
| 3 | **机会扫描器**被当"老图表"删除 | 保留在大盘全市场页内（原样），不单独成页、不加新行为 |
| 4 | **PageQuickNav 18 段导航**与向导步骤条职责重叠被误删 | ⑤/⑲ 内保留，改为按可见模块动态生成 |
| 5 | **锚点批注（AnchorAnnotations）**不属于任何"看" | 全局保留；`AnnotationView` 枚举扩展到五看 |
| 6 | **工作区历史快照的全量打包语义**（20+ 字段）迁移时字段对不齐 | 先做字段对齐清单 + 迁移器带预检（§7.2） |
| 7 | **VOC 三步 + 5W1H 旅程**被"只搬结论" | 整页搬迁 ⑯，禁止重写 |
| 8 | **竞品图包上传 + A+/附图逐张对比**（纯本地 zip 解析，非 MCP） | ⑦⑧ 保留，解析器必留 |
| 9 | **利润计算器三张敏感性表 + 目标倒推 + 组合诊断**只留"三格假设" | 侧栏整页保留（原样照搬） |
| 10 | **导出/交付管线**统一时只留一个 | 按 §2.7.3 矩阵逐条保留 |

#### 2.7.6 明细层上下文透传规范（消灭"跳出去就丢项目"）

统一路由态（M1 落地）：

```
ToolRoute = {
  projectId, marketplace,
  months: { current, previous, lastYear },
  segment?: { level1, level2?, level3?, matchedAsins, fallbackToAll },
  asinPool?: string[], roles?: { head?, follower?, newcomer? },
  demandId?, opportunityId?,
  returnTo: 'look' | 'subpage', breadcrumb: string[]
}
```

| 明细层 | 必须接收 | 必须回写项目 |
| --- | --- | --- |
| ⑤ 细分大盘 | projectId、marketplace、months、segment、segmentFallback | 加入竞对样本池 / 回填市场证据 |
| ⑮ 关键词 | projectId、marketplace、demandId、segment、anchor | 意图分层·JTBD·标签 → 看用户需求树 Evidence |
| ⑯ VOC | projectId、asinPool、demandId、tagLib（复用已有标签库）、marketplace | tagLib / 5W1H / 差评痛点 → 看用户；竞对画像 → 看竞对⑩ |
| ⑰ 竞品 | projectId、asinPool（≤5 自动入池）、roles、focus | 单品结论 / 流量词 / 缺口 → 满足矩阵与赢的路径 |
| ㉔ 数据质量 | 当前数据集 + scope | 缺口清单 → "缺数据先说"提示 |
| ㉕ Prompt | 全局 | 版本号 → 报告 `promptVersion` |

> ⑲ 市场大盘（全市场）与 ⑱ 利润计算器为**侧栏全局工具**（照搬原样），不参与项目上下文透传。

**强制规则**：① 面包屑 + 返回保留筛选与滚动位置（复用 `App.tsx:1324-1348` 的 scroll 逻辑，扩为按 tool 维度）；② 结论→证据→原始数据三层可回溯（原始数据落点现成：`ProductModal`、关键词表行、评论原文）；③ 明细层不产生新结论，只回填 Evidence；④ **保留 App.tsx 三处"常驻挂载 + hidden 切换"**（CompetitorHub / UserInsights / SegmentationManager）——它们承载"切 Tab 不丢结果、后台打标不中断"的体验，改成普通路由会破坏。

---

## 3. 商用化定位与北极星

### 3.1 产品定位（V2）

> **亚马逊立项决策系统**：输入一个品类意图（品类名 / 关键词 / 竞品 ASIN），10 分钟内输出一份可复核的决策包——**这个市场有没有机会？机会是什么？先做什么、后做什么？需要什么前置资源？卡哪些控制点？**

第一性原理拆解：

- 用户的真实任务不是"看数据"，是**"拍板"**——买不买库存、开不开模具、投不投广告。数据只是拍板前的证据。
- 用户愿意付费的不是图表，是**"替我完成专业分析师的推理，并且我能看懂、能复核、能拿去说服别人"**。
- 傻瓜式的本质不是"功能少"，是**"把方法论内化"**：五看是专家的工作流，向导把专家流变成选择题流。

### 3.2 目标用户与使用强度

| 角色 | 使用场景 | 核心诉求 |
| --- | --- | --- |
| 老板 / 项目负责人（拍板人） | 每周 1-2 次：这个品类做不做、继续投不投 | 10 分钟拿到结论 + 路线图 + 控制点，可转发给团队 |
| 选品 / 产品经理（研究员） | 每周 2-5 次：初筛品类、深挖细分 | 需求→细分→竞对缝隙的推理链可信、可修改 |
| 运营（执行层） | 拿到立项结论后拆 Listing/广告任务 | 决策包里的先做/后做清单可直接执行 |
| 审核者（外部视角） | 阅读导出的报告 | 证据可点击溯源，能独立判断 |

使用强度：每月 1-2 个深度项目 + 每日若干次快筛。**不要**按"每天处理几十个项目"的管理后台设计。

### 3.3 商业路径（内部自用 → 产品化）

- **Phase A（本 PRD，6 个月）**：内部团队自用打磨。数据由平台统一数据池供给（管理员配置密钥 + 用量记账）；导出 HTML/MD 支持对外交付报告（"先卖报告"的钩子）。
- **Phase B（产品化，另立 PRD）**：多租户订阅。V2 预埋三件事：① 项目/数据严格按 workspace 隔离（修复跨项目污染的同时就是租户隔离）；② 数据池用量计费埋点；③ 团队空间与权限（已有雏形）。

### 3.4 北极星与护栏指标

| 指标 | 目标 |
| --- | --- |
| **北极星：可信决策包产出时间**（建项→拿到含路线图的 Go/No-Go 结论） | 数据就绪下 ≤ 10 分钟（快速模式）/ ≤ 40 分钟（深度模式） |
| 证据可追溯率 | 100%：每条结论可点回原始关键词/评论/图表/ASIN |
| 错误立项防护（对抗测试） | 连续 10 个真实品类回放：0 次"证据不足→有机会"、0 次"无机会→证据不足" |
| 五看闭环完成率（7 天内） | ≥ 70%（V1 是 60%，AI 向导应显著提升） |
| 报告复用率（导出的决策包被转发/复用） | ≥ 50% |

---

## 4. 改革总纲：十条设计原则

1. **先结论，再证据，后数据**。每页一个主标题 = 一句最强结论；一个主操作。图表是证据，不是首页。
2. **用户只做选择题、判断题、拍板**。取数、聚类、起草、计算由系统完成；人确认或修改。任何需要用户"填长文本"的环节都视为设计失败（备注类除外）。
3. **需求分类是唯一索引**。看用户产出的需求分类 ID 贯穿细分标准、竞对满足矩阵行、机会卡需求字段。改一处，下游标"需复核"（复用并强化 applyLookProgressUpdate 链）。
4. **确定性计算与 AI 分权**。评分、进度、权限、利润计算=确定性公式（有测试）；AI 只做聚类、起草、解释，**永不改分、永不替人决策、永不在缺证据时编造机会**。
5. **0 个机会是合法结论**，并显式区分"已判断无机会"与"证据不足，暂不能判断"。
6. **每一步可回退、可跳过（跳过必须标记风险）**。向导不是监狱，是推荐路径。
7. **缺数据先说，不事后炸**。进入项目时即提示"缺什么、影响什么、怎么补"；演示模式全链路可用（修复 demo 空转）。
8. **决策包可交付**。所有结论可导出为独立、可打印、可转发、证据可溯源的 HTML/MD（不做 PPT，用户确认）。
9. **赢的路径必须落到三种之一**。看竞对与看机会的结论必须指明赢法：**人无我有**（差异化）/ **人有我优**（更优）/ **人优我廉**（更便宜，且必须在毛利红线内）；三种都不成立 → 结论为"无赢面/自身接不住"，不是机会。
10. **极简优先，一切服务"快速找到未满足需求"**。每个看主屏只放：一句人话结论 + 一个主操作 + 证据数量徽章；其余全部进二级页。高级/低频功能**默认收起，不删除**；任何看不懂的东西，要么删掉，要么补一句人话解释——没有解释的指标不显示。

---

## 5. 信息架构重构

### 5.1 页面地图（V2）

```
登录/注册（内部成员，照原样不改）
│
├── 左侧栏（全局常驻）
│     ├── 项目中心
│     ├── 利润计算器（原样照搬，不进任何项目）
│     ├── 市场大盘明细 · 全市场（原样照搬）
│     └── 设置（数据池 / AI / 成员 / 背景库 / Prompt）
│
├── 项目中心（少量深度的宽卡，非管理后台）
│     ├── 项目卡：项目名 / 一句话结论（进行中=当前最强结论） / 五看进度环 / 下一步唯一 CTA
│     └── 新建项目：品类名 或 关键词 或 竞品 ASIN（三选一）+ 站点 + 研究目标
│
└── 项目内（ProjectWorkspace V2）
      ├── 顶部：向导步骤条（5 步 + 概览 + 报告）｜面包屑｜保存状态
      ├── 主区：当前看的工作区（每看 2 屏：结论屏 + 明细屏）
      ├── 右侧常驻：「决策草稿」面板（机会卡实时预览 + 缺口提示）── 宽屏 ≥1280px，可折叠
      │
      ├── 概览：项目结论总览（一句话结论 + 决策包摘要 + 下一步 CTA）
      ├── 看用户：搜索路径 → 搜索偏好 → 需求分类 → 细分标准建议
      │         └─ 二级页：①搜索路径 ②搜索偏好 ③需求树(证据链) ④细分标准设置
      ├── 看市场：细分方案（需求×商品×竞对标题合成）→ 细分评分排行 → 目标细分选择
      │         └─ 二级页：⑤细分完整大盘(老版大盘自动筛选) ⑥细分方案对比
      ├── 看竞对：自动挑选（头部/跟随/新人各 1）→ 全量 Listing 三列对比（前台可见+卖家精灵可抓，全字段）
      │         → 每竞对三块分析（用户画像 / 痛点未满足矩阵 / 需求满足矩阵）→ 赢的路径（人无我有/人有我优/人优我廉）
      │         └─ 二级页：⑦单竞对深度 ⑧主图逐张对比(1-7张全对比) ⑨流量结构 ⑩评论画像
      ├── 看自己：背景库（账号级一次维护）+ 品类选择题（AI 动态生成 5-8 题）→ 适配度与硬约束
      │         └─ 二级页：⑪背景库 ⑫题目依据
      ├── 看机会：机会卡（0-N）→ 每卡决策包（先做/后做/前置资源/控制点）→ 优先级矩阵 → Go/No-Go
      │         └─ 二级页：⑬机会详情(证据/推理/反证) ⑭路线图与控制点
      └── 报告：决策包导出（HTML/MD）+ 历史版本
│
└── 明细层（仅三个，从五看内按需打开，不再是顶级导航）
      ⑮关键词明细 / ⑯评论VOC明细 / ⑰竞品明细
      ⑱利润计算器 → 侧栏 · ⑲市场大盘全市场 → 侧栏 · ⑳机会扫描器 → 并入⑲
      ㉑报告页 / ㉒登录页(照原样) / ㉓管理员后台(M5) / ㉔数据质量面板 · ㉕Prompt管理
      （完整清单与上下文参数见 §5.5）
```

### 5.2 关键结构决策

1. **关键词/VOC/竞品三个老模块降为"明细层"**（市场大盘与利润计算器已按用户要求移到侧栏、原样照搬）：从五看的"查看详细分析"按钮打开，打开后顶部保留面包屑（项目 / 看 X / 明细页）与"带回项目"按钮。明细层产出的结论通过"回填项目证据"按钮写入项目快照（消灭手动搬运）。
2. **向导式推进，非线性可回退**：默认顺序 用户→市场→竞对→自己→机会；完成一步解锁下一步（可跳过，跳过标记"该步未验证"并在机会评分中扣证据可信度分）；随时可点回任一步修改，修改触发下游 stale。
3. **"决策草稿"常驻右侧**：从第二步起，右侧面板实时显示"当前证据下可能的机会方向 + 还缺什么"，让用户全程有"我在往哪走"的感知。窄屏（<1280px）折叠为底部抽屉入口。
4. **双模式**：快速模式（默认，每步 3 个以内选择题，AI 全自动取数，评论 60 条）与深度模式（每步展开全部明细页，专家用户，评论 200 条）。同一数据，两套交互。
5. **触发模型（用户确认）：一键向导 + 每步局部重跑**。建项目后点一次「开始分析」，系统按序自动执行取数→看用户→看市场→看竞对→看自己出题，**每一步停在"请确认"卡**等用户确认/修改后进入下一步；同时每步常驻局部按钮：`重新取数`、`AI 重跑这一步`、`手动改结论`。**不做**"每个板块各自一个 AI 按钮、让用户自己决定跑什么"的形态——那等于把方法论的决策负担丢回用户。

### 5.3 顶层导航（V2）

```
┌─────────────────┬───────────────────────────────────────────┐
│ ◈ Kairo         │  项目内：面包屑 · 向导步骤条 · 保存 · 导出   │
│  项目中心        │                                           │
│  利润计算器      │           （内容区）                       │
│  市场大盘·全市场  │                                           │
│  设置           │                                           │
└─────────────────┴───────────────────────────────────────────┘
```
左侧栏全局常驻：**项目中心 / 利润计算器（原样）/ 市场大盘·全市场（原样）/ 设置**。项目内顶层只有：面包屑、向导步骤条、保存状态、导出按钮。删除旧"市场大盘/竞品分析/用户洞察/关键词/利润"顶级入口——关键词/VOC/竞品降为明细层（从五看打开）；利润计算器与市场大盘（全市场）移到侧栏（原样照搬）。

### 5.4 术语统一表（V2 强制）

| 统一术语 | 覆盖旧叫法 | 含义 |
| --- | --- | --- |
| 看竞对 | 看竞品 / 竞品分析 / 竞品对比（项目语境） | 项目内对头部/跟随者/新人的三维对比 |
| 竞品明细 | 竞品对比工具 / CompetitorHub | 工具层 ASIN 深度对比页 |
| 看机会 | 看/找机会 / 找机会 | 项目内机会卡与决策 |
| 细分机会分 | 机会分 / 黄金坑位 | 看市场里每个细分市场的 0-100 评分 |
| 机会卡评分 | 机会分 | 看机会里每张机会卡的 0-100 评分（五维加权） |
| 赢的路径 | 我们如何赢 / 取胜策略 | 看竞对结论的三选一：人无我有 / 人有我优 / 人优我廉 |
| 机会扫描器 | 竞争空白扫描器 / 黄金坑位 | 市场大盘（全市场）页内的散点扫描图表（保留原样，不单独成页） |

### 5.5 二级页面体系（L3 详情页 + 明细层）——老版能力全部归位

**通用规则（所有二级页强制遵守）**

1. **面包屑 + 返回保留上下文**：`项目中心 / 项目名 / 当前看 / 页面名`；返回主屏时保留筛选条件、滚动位置、展开状态（修复 P0-2 第 ⑤ 条）。
2. **三层证据链**：**结论 → 证据 → 原始数据**。二级页里任何结论都能点开证据，证据能点回原始来源（关键词表某行 / 评论原文 / 图表数据点 / ASIN）。这正是"证据链丰富"的落地方式。
3. **二级页不产生新结论**：二级页只负责"看明细、核证据、改数据"；机会结论一律回主屏确认。二级页里的操作可"**回填到项目**"生成 Evidence 记录（§7.3）。
4. **二级页 vs 明细层的分工**：
   - **二级页** = 属于某个看、自带该看上下文（项目 / 需求 / 细分 / 竞对），如"侧睡枕细分完整大盘"；
   - **明细层** = 通用分析工具（老版关键词 / VOC / 竞品三个模块），可从二级页按需打开，也可脱离五看单独使用；打开时带上项目与筛选上下文，不再"跳出去就丢项目"。（利润计算器与市场大盘已按用户要求移侧栏、原样照搬。）

**五看二级页清单**

| # | 二级页 | 入口（主屏） | 核心内容 | 数据来源 | 老版对应 |
| --- | --- | --- | --- | --- | --- |
| ① | 搜索路径详情 | 看用户 ①搜索路径图 → "查看完整路径" | 认知→考虑→决策→场景四层漏斗：每层词表、搜索量、占比、层间流向；可拖动改层 | MCP 词 TOP200 | 关键词分析（意图分层） |
| ② | 搜索偏好详情 | 看用户 ②搜索偏好卡 → "查看依据" | 价格敏感度、材质/功能/品牌词占比、长尾 vs 大词结构、买家对比焦点；每条偏好挂证据 | MCP 词 + 评论 | 关键词分析 + 评论标签 |
| ③ | **需求分类树详情（证据链重点）** | 看用户 ③需求分类树 → 点任一需求 | 树形：需求域→需求→子需求；右侧证据面板：关键词证据列表 + 评论原文引用 + 覆盖 ASIN + 证据强度；支持合并/拆分/重命名/排除 | MCP 词 + 评论 VOC + ASIN | 关键词 JTBD 聚类 + VOC 打标 |
| ④ | 细分标准设置 | 看用户 ④细分标准建议 → "配置细分标准" | 把需求标记为市场细分标准，可组合"需求 × 场景 × 价格带"；实时预览将切出几个细分、各含多少 ASIN | ③ 的需求树 + 商品表 | 细分管理器（SegmentationManager） |
| ⑤ | **细分市场完整大盘** | 看市场 → 细分卡片"查看完整大盘" | **就是老版"市场大盘"整页，但已自动筛选到该细分**：KPI 卡、趋势、集中度、价格带、价格-评分散点、品牌榜、BSR 分布、上架时间、新老品、评分分布、季节热力、卖家类型/位置、Top 商品表、数据质量提示；顶部筛选条显示"当前：侧睡枕细分（23 ASIN）"+ 切换细分 | 上传产品表/历史表 + MCP | **市场大盘（全套图表组件原样复用）** |
| ⑥ | 细分方案对比 | 看市场 → "切换/微调方案" | 2-3 个细分方案并排：按睡姿/按材质/按价格带，各自切出几块、评分对比、AI 利弊一句话，选择其一 | 商品表 + 竞品标题聚类 | 细分管理器 |
| ⑦ | 单个竞对深度页（Listing 全字段） | 看竞对 → 点列头 ASIN | 主图 7 张、视频、标题（词频标注）、五点、A+、参数表、Q&A、变体矩阵、价格历史、BSR 历史、月销/销额 | MCP Listing 全字段 | **竞品明细（CompetitorHub）** |
| ⑧ | 主图逐张对比 | 看竞对 → "主图对比" | 三个竞对**第 1-7 张逐张对比**（第 1 张对第 1 张……第 7 张对第 7 张，全部 7 张同等并排，第 4-7 张不省略）+ AI 每张一句话 | MCP 图片 | 竞品对比（主图并排） |
| ⑨ | 流量词与广告结构 | 看竞对 → "流量结构" | 流量词表（词/搜索量/自然排名/广告排名/广告依赖）、三方词重叠韦恩、自然 vs 广告占比 | MCP 流量词 | 竞品对比（流量词） |
| ⑩ | 竞对评论与画像证据 | 看竞对 → 每竞对"用户画像" | 该竞对评论样本、好评点、差评痛点、人群/场景反推，每条点开看原文 | MCP review | 用户洞察（VOC 打标） |
| ⑪ | 账号背景库 | 看自己 → "补全背景" | 六维度（资金预算/供应链/类目经验/合规资质/目标财务/风险偏好）每项四态 + 完整度进度 | 用户维护 | 自评模板（42 题重构） |
| ⑫ | 品类选择题与依据 | 看自己 → 任一问题"为什么问这题" | 出题依据（需求/细分/竞对缝隙）、选项含义、答完对适配度与硬约束的影响 | AI 生成 + 背景库 | 新增 |
| ⑬ | 机会卡详情（证据/推理/反证） | 看机会 → 机会卡 | 三栏：结论与评分 / 结构化推理链（证据→判断→结论）/ 反证与缺失证据；底部人工修改记录 | 四看 Evidence | 机会卡（HTML 报告的内容源） |
| ⑭ | 执行路线图与控制点详情 | 看机会 → 机会卡"路线图/控制点" | 0-30 天与 30-90 天时间轴、前置资源清单与缺口、三类控制点（准入/过程/止损）可编辑 | AI 起草 + 利润假设 | 新增（决策包核心） |

> **编号约定**：①-⑭ 为五看二级页；⑮-㉓ 为明细层/全局页；㉔-㉕ 为面板级（不单独出屏）。**本编号与 `docs/ui-wireframes-l3.html` 的 23 屏严格一一对应。**

**明细层与全局页（老版模块全部保留，作为通用工具页）**

| # | 页面 | 从哪进入 | 保留内容（老版能力，不删） | 上下文参数 |
| --- | --- | --- | --- | --- |
| ⑮ | 关键词明细 | 看用户 ①/② → "查看详细分析" | 意图分层、JTBD 聚类、词云、流量词、搜索趋势、4 张 TOP10 榜、原始词表（可编辑标签）、导出 Excel（3 sheet）、AI 关键词报告 | projectId + marketplace + demandId + segment |
| ⑯ | 评论 VOC 明细 | 看用户 ② / 看竞对 ⑩ → "查看详细分析" | 标签库、自动打标、标签趋势、好评/差评、5W1H、人群画像、用户旅程、VOC 深度报告 | projectId + asinPool + demandId + tagLib |
| ⑰ | 竞品明细 | 看竞对 ⑦ → "打开竞品对比" | 多 ASIN 对比池（≤5）、主图/A+/五点逐张对比、父子变体矩阵、历史趋势、竞品 AI 报告 | projectId + asinPool + roles + focus |
| ⑱ | 利润计算器【改为侧栏工具】 | 首页左侧栏（项目中心下），**不进任何项目/二级页** | **原样照搬，不做修改**：多变体输入、盈亏敏感性三表、目标倒推、组合诊断、父体加权利润、方案存档 | 全局（无项目上下文） |
| ⑲ | 市场大盘明细（全市场） | 首页侧栏 / 看市场 ⑤ → "切到全市场" | **照搬老版"市场大盘"原样，不做任何改变**（趋势/集中度/价格带/评分/品牌榜/BSR/上架/新老品/卖家/数据质量）；**机会扫描器（黄金坑位散点）作为本页内图表保留** | 全局（不依赖项目） |
| ⑳ | 机会扫描器【取消独立页】 | ——（并入 ⑲） | 取消单独成页，保留在"市场大盘（全市场）"页内，照原样 | —— |
| ㉑ | 报告页（决策包） | 项目顶部"报告" | 导出 HTML/MD、版本列表、报告结构预览、历史版本对比、飞书推送记录 | 全项目数据 |
| ㉒ | 登录页 | 未登录时 | **照原样，不做任何改动** | 全局 |
| ㉓ | 管理员后台（M5） | 管理员账号 | 用户列表与状态、用量统计（MCP/LLM/失败率）、数据池密钥配置、审计日志、环境自检 | 全局 |
| ㉔ | 数据质量面板 | ⑤/⑲ 与各图表右上角 | 样本数、时间范围、缺失字段、来源与时效（**面板级，内嵌不单独出屏**） | 当前数据集 + scope |
| ㉕ | Prompt 管理 | 设置 → Prompt（管理员配置中心内） | 14 个老版预设 + 4 个 phase0-v1 预设，可编辑可恢复（**设置内嵌，不单独出屏**） | 全局 |

**层级总图（主屏 → 二级页 → 明细层）**

```
看用户(主屏 ①②③④) ──► 二级页①搜索路径 ②搜索偏好 ③需求树(证据链) ④细分标准
      └─► 明细层：关键词明细 ⑮ · 评论VOC明细 ⑯
看市场(主屏：方案选择+细分机会池+目标细分) ──► 二级页⑤细分完整大盘 ⑥细分方案对比
      └─► 侧栏⑲市场大盘(全市场)·数据质量 ㉔
看竞对(主屏：三列对比+每竞对三块分析+赢的路径) ──► 二级页⑦单竞对深度 ⑧主图逐张(1-7张全对比) ⑨流量结构 ⑩评论画像
      └─► 明细层：竞品明细 ⑰ · 评论VOC明细 ⑯
看自己(主屏：优势/缺口/硬约束+选择题) ──► 二级页⑪背景库 ⑫题目依据
看机会(主屏：决策看板+Go/No-Go) ──► 二级页⑬机会详情(证据/推理/反证) ⑭路线图与控制点
      └─► 报告页 ㉑ · Prompt ㉕

全局侧栏（首页左侧，项目中心下）：利润计算器(原样) · 市场大盘明细·全市场(原样) · 设置
机会扫描器 → 并入「市场大盘明细（全市场）」页内，不单独成页
```

### 5.6 老版资产保留原则（重要）

**原则 1：老版模块整体搬迁，不做重写。** 市场大盘全套图表、关键词分析、VOC 打标、竞品对比（含主图逐张/A+/父体矩阵/流量词）、利润计算器、机会扫描器、数据质量面板、飞书推送、报告导出——**代码与交互整体保留**，只是换入口、加筛选上下文。

**原则 2：新架构只负责"组织与串联"，不负责"重新实现"。** 五看主屏与二级页负责给老模块喂上下文（项目/需求/细分/竞对），老模块负责展示明细。

**原则 3：删除的只有死代码与重复入口，不删能力。** 例如 `CompetitorAnalysis.tsx`（621 行，无人引用）要么接回为明细层，要么明确标注废弃；老五模块的顶级导航入口删除，但页面本身保留在明细层。

**原则 4：每一条老版能力都要在 §5.5 的二级页/明细层清单里有明确归属**，不允许出现"能力丢失且无人知晓"的情况。资产清点表见 §2.7。

---

## 6. 五看逐一 PRD（V2 规格）

> 每看统一页面结构（继承 phase-0 refactor 计划）：
> **顶部**：一句核心结论 + 状态 + 下一步 CTA ｜ **第一屏**：结论卡、关键证据、缺口/风险 ｜ **第二屏**：明细与证据 ｜ **底部**：证据来源 + "查看详细分析"入口。

### 6.1 看用户 V2：搜索路径、搜索偏好、需求分类、细分逻辑

**要回答**：用户是谁？怎么搜？搜的过程中在比什么？哪些需求没被满足？市场该怎么切？

**数据从哪来（数据锚点模型——回答"第一步看用户没有数据源怎么办"）**：

- 建项目时用户必须给一个**锚点**（品类名 / 关键词 / 竞品 ASIN 三选一）——这是用户自己的业务意图。**看用户的数据由锚点反查扩展而来，不依赖看市场先完成**：看市场需要的大盘数据晚于看用户取得，没有循环依赖。
- 快速模式固定口径（用户确认）：**词 TOP200 · 评论 60 条 · 竞对 3 个**；深度模式：**评论 200 条**（分档 + 缓存，见下）。
- **评论取样分档（用户确认，因卖家精灵 `review` 每次最多返回 20 条）**：
  - 快速模式 60 条 = 锚点 ASIN 20 条 + 品类头部 2 个 ASIN 各 20 条 = **3 次调用**；
  - 深度模式 200 条 = 锚点 ASIN 80 条（4 次）+ 头部 2 个 ASIN 各 60 条（各 3 次）= **10 次调用**；
  - **缓存 14-30 天**：同一 ASIN 的评论样本在 TTL 内复用，重复分析同一品类不再扣次数；
  - 一律标注来源构成与"基于样本，不代表总体"。
- **双通道取数（用户确认的混合模式：小数据抓、大数据传）**：
  - 🕸 MCP 抓取通道（小数据量）：词 TOP200（关键词扩展/ABA）、评论（快速 60 / 深度 200 条）、竞对 Listing 全字段（§6.3）、竞品标题 TOP30；
  - 📤 上传通道（大数据量）：**产品表 + 历史表（大盘快照）沿用现有 Excel/CSV 上传**——数据量大时 MCP 抓取慢且烧调用次数，默认走上传；可选 MCP 抓取但明示成本与耗时。
- 取数链路（数据池就绪时全自动）：
  1. 锚点=关键词 → 卖家精灵关键词扩展 / ABA → **TOP200 词**（含搜索量、意图分层）；
  2. 锚点=ASIN → 该 ASIN 流量词反查 + 评论；
  3. 锚点=品类名 → 类目 TOP ASIN 榜 → 头部 ASIN 的评论与标题；
  4. **评论样本**（用户已确认口径）：快速模式 60 条（锚点 20 + 头部 2×20，3 次调用）；深度模式 200 条（锚点 80 + 头部 2×60，10 次调用）；缓存 14-30 天复用；
  5. **竞品标题样本**：品类 TOP30 ASIN 标题（供 6.2 细分合成聚类用）。
- **无数据池 / 无 Key 时**（三条补数途径，建项目那一刻就检查并提示，绝不允许做到一半才发现）：
  ① 上传词表 Excel + 评论 Excel；② 粘贴 1-3 个竞品 ASIN，由系统经数据池抓取；③ 全部缺失 → 用内置示例品类（真实品类：薄枕头，用户确认）跑通全流程（醒目标注"示例数据，非真实结论"）。

**输入（系统自动取，用户可补充）**：
- 关键词集（ABA/流量词，快速模式 TOP200）
- 评论样本（快速 60 条 / 深度 200 条，标注来源构成与"基于样本"）
- 竞品标题样本（品类 TOP30，供市场细分参考，实际聚类在 6.2 执行）

**向导步骤（3 个选择题 + 1 次确认）**：

1. **搜索路径图（AI 自动生成 → 用户选择题确认）｜二级页①：搜索路径详情**
   产出：认知词 → 考虑词 → 决策词 → 场景/复购词 的四层漏斗，每层 Top 词、占比、流向。
   选择题示例："AI 判断您的品类中'枕头'层用户先搜大词、再按'睡姿'分化——对吗？A. 对 B. 先按'材质'分化 C. 我来改"。用户可拖动词到别的层。
   主屏只放漏斗图与最强结论，完整词表/占比/流向进**二级页①**（见 §5.5），可"回填项目"。
2. **搜索偏好画像（AI 起草 → 确认）｜二级页②：搜索偏好详情**
   产出：价格敏感度（低价词占比）、功能/材质/品牌词倾向、长尾 vs 大词结构、购买决策焦点（对比什么维度）。
   每条偏好必须挂证据（词 + 评论），点开进**二级页②**。
3. **需求分类树（核心产物）｜二级页③：需求分类树详情（证据链重点）**
   AI 对关键词意图 + 评论 VOC 打标聚类 → 需求分类树（2-3 层，每节点挂证据：关键词证据 ≥2 条 + 评论原文引用 ≥1 条）。
   用户操作：合并 / 拆分 / 重命名 / 排除 / 确认。**确认后的分类获得稳定 ID，成为全项目索引。**
   每条需求标注：已满足 / 部分满足 / 未满足（初判，供看竞对验证）。
   **证据链在二级页③展开**：选中任一需求 → 右侧面板显示关键词证据列表、评论原文引用、覆盖 ASIN、证据强度；证据可点回原始来源。
4. **细分标准建议（联动看市场）｜二级页④：细分标准设置**
   用户把某层需求分类标记为"**作为市场细分标准**"（可多选，可组合"需求×场景×价格带"）。
   示例：按"睡姿（侧睡/仰睡/俯睡）"切 → 看市场将按此维度聚合商品并评分。
   **二级页④** 实时预览：将切出几个细分、各含多少 ASIN，确认后写入看市场。

**产出物**：搜索路径图、搜索偏好卡、需求分类树（带证据）、细分标准建议；四者均可下钻到各自二级页（① ② ③ ④）。完成条件：≥1 条"未满足/部分满足"需求且每条挂证据。

**修复项**：断链 #5（VOC/关键词结论回流，不再只是 count）、#8（demo 预置完整示例路径与需求树）。

### 6.2 看市场 V2：需求驱动的市场细分（核心改革）

**要回答**：哪块细分市场有价值和进入窗口？

**细分合成逻辑（三方合成，替换现在的"手动 ASIN 分组"）**：

```
需求维度（看用户的需求分类）
   × 商品维度（大盘数据的价格带/材质/场景属性）
   × 供给维度（竞品标题关键词聚类：市场在卖什么、怎么自称）
        │
        ▼
  AI 提出细分方案草案（2-8 个细分，每个给：定义、代表 ASIN、为什么这么切）
        │
        ▼
  用户选择题确认/调整（这是傻瓜式的关键：用户改的是"方案选项"，不是原始数据）
        │
        ▼
  每个细分跑确定性评分（复用 segmentScore：趋势0.35/体量0.35/竞争0.3，有测试锁定）
```

**数据通道（用户确认）**：细分聚合所需的产品表+历史表默认来自**上传 Excel**（量大走上传，不烧 MCP 次数）；词/评论/竞品 Listing 走 MCP 抓取。

> **实现说明（M2③ 落地时的刻意偏差，2026-09）**：本节的"AI 提出细分方案草案"落地为 **三个方案全部由确定性规则生成**（A 需求域 / B 价格段×商品属性 / C 竞品标题聚类），AI 的角色是**解释方案差异与盲区**（`segmentAdvice`），prompt 里明确写入"分数与归属不可修改、不得替用户选方案"。
> 理由：① 用户诉求是"细分逻辑要跟上一步（看用户）挂钩"，这靠**命中规则**保证，不靠模型自由发挥；② 与 §4 原则 4 / §8「AI 只解释、不改分」一致——若方案由 AI 生成，评分与归属就无法复现，三例真实品类回放也无法比对偏差；③ 规则全部有测试锁定（单归属、不重叠、收入占比 ≤100%、未归属显式计数）。
> 用户看到的仍然是"2-3 个方案 + 选择题 + 一句利弊"，体验与 PRD 一致；差别只在方案的**来源**。

**向导步骤**：

1. AI 读入看用户的细分标准建议 + 竞品标题聚类结果，输出 2-3 个"细分方案"供选择（例如"按睡姿切 3 块"vs"按材质切 4 块"vs"按价格带切 3 块"），每个方案附一句利弊。
2. 用户选一个方案（可微调：合并/拆分细分），系统按 ASIN 归属聚合商品，对每个细分跑评分 → **细分机会池排行**（卡片：机会分 + 趋势/体量/竞争三分 + 规模 + 均价 + 代表 ASIN）。
3. 点进任一细分 → **二级页⑤「细分市场完整大盘」**：**就是老版"市场大盘"整页，但已自动筛选到该细分**——KPI 卡、趋势、集中度、价格带、价格-评分散点、品牌榜、BSR 分布、上架时间、新老品、评分分布、季节热力、卖家类型/位置、Top 商品表、数据质量提示全部保留（老版图表组件原样复用，§5.6 原则 1）；顶部固定在筛选条：`当前：侧睡枕细分（23 ASIN）` + 切换细分下拉 + "切到全市场"（进侧栏⑲全市场大盘）+ 返回看市场。无 ASIN 映射时显式标"全市场回退"，沿用 phase-0 控制点。
4. **选择目标细分**（单选为主，允许"暂不定，先看全市场"）→ 该细分的 ASIN 自动成为看竞对的样本池。

**产出物**：细分方案确认记录、细分机会池排行、目标细分选择（+ 其 ASIN 列表 → 看竞对）。完成条件：目标细分已选 或 显式选择"暂不定"。

**修复项**：断链 #2（细分按项目快照隔离，选择回调真正生效）、#7（openQuestions 改为结构化"对看竞对/看自己的待验证问题"，被下游消费并在对应页面顶部显示）。

### 6.3 看竞对 V2：全量 Listing 三列对比 + 每竞对三块分析 + 赢的路径

**要回答**：现有竞品靠什么赢？哪些需求它们没满足？我们凭什么赢（人无我有 / 人有我优 / 人优我廉）？

**自动挑选（复用 competitorPicker，口径=3 个，用户确认）**：
- 头部：目标细分内销量/销售额最强（1 个）
- 跟随者：接近头部、有增长、形成对照（1 个）
- 新人：上架 ≤180 天中**表现最好的代表性新人**（排名/销量/评论增长/关键词表现最优——不是"上架最新的"；1 个）。若品类无符合条件的新人 → 取"上线时间略早于该标准"的次新且表现好的 ASIN 替代（用户确认），仍按代表性挑选，不按上架时间排序。
- 每个带"为什么选它"一句话理由；用户可换人（从目标细分 ASIN 列表选）。缺位规则见 §15.1-17。

**自动抓取：全量 Listing 对比字段（买家前台能看到的一切 + 卖家精灵能抓到的一切，缺一不可）**：

| 组 | 字段（前台可见） | 字段（卖家精灵可抓） |
| --- | --- | --- |
| 产品 | 主图×7、视频、价格、划线价/优惠券、评分与评分分布、评论数、变体矩阵（父子 ASIN/颜色/尺寸）、参数表（材质/尺寸/重量/产地）、Q&A 数量与代表问题、上架时间、品牌/制造商 | 月销量、月销售额、BSR 及类目、评论增长曲线、FBA 费估算、卖家类型/发货方式 |
| Listing | 标题、五点×5、A+/品牌故事、图文策略 | 标题词频与关键词覆盖分析 |
| 流量 | —（前台不可见） | 流量词（自然/广告）与关键词数量、ABA 排名、广告依赖度、自然/广告流量比、历史价格与排名趋势 |

三列对比视图按上表分组展示，每个字段标注来源（🖥前台 / 🔮卖家精灵 / 📊推算）。

**每竞对下方三块分析（用户新增要求，做完全部对比后才允许做结论）**：

1. **用户画像（人群、场景、需求）**：从该竞对的评论反推——谁在买（人群特征）、什么场景用、核心诉求排序；每条附评论原文引用。
2. **痛点未满足矩阵**：该竞对差评痛点（质量/设计/物流/虚假宣传等）× 需求分类——痛点命中哪些需求、该竞对没解决什么、痛点占比。
3. **需求满足矩阵**：需求分类（看用户）× 该竞对——满足● / 部分◐ / 未满足○ / 未提及—，单元格点击看证据（标题/五点/评论原文）。

**对比完成后的结论（严格按此顺序产出）**：

```
① 未被满足的需求清单（三列对比 + 三块分析的交叉结论，每条挂证据）
        ∩
② 我们能够满足的部分（看自己的适配与硬约束）
        │
        ▼
③ 赢的路径（三选一，必须落到一种，落到即"我们如何赢"）：
   人无我有 —— 竞对都没有，我们有差异化供给
   人有我优 —— 竞对都有但做得差，我们做得更好
   人优我廉 —— 竞对做得也好，我们在毛利红线内以价格取胜
   三种都不成立 → 显式结论"无赢面/自身接不住"（喂给看机会的"无机会"分支）
```

**产出物**：全量 Listing 三列对比、每竞对三块分析（画像/痛点矩阵/满足矩阵）、未满足需求交叉清单、"我们如何赢"（人无我有/人有我优/人优我廉 三选一）。完成条件：3 竞对入池 + 三块分析全部完成 + 赢的路径已选定（或无赢面结论已确认）。

**二级页**：⑦单竞对深度页（Listing 全字段：主图 7 张/标题/五点/A+/参数表/Q&A/变体矩阵/价格历史/BSR 历史）、⑧主图逐张对比（**第 1-7 张全部逐张对比**：第 1 张对第 1 张……第 7 张对第 7 张，第 4-7 张与第 1-3 张同等并排）、⑨流量词与广告结构（词表 + 三方韦恩 + 自然广告比）、⑩竞对评论与画像证据（好评点/差评痛点/人群场景反推，可点原文）。全部复用老版竞品对比模块（§5.6）。

**修复项**：断链 #4（ASIN 自动入对比池）、#9（competitorWorkspace 写回项目快照）、#10（术语统一为"看竞对"）；新增：全字段抓取编排（§11.2）、每竞对三块分析组件化。

### 6.4 看自己 V2：背景库 + 品类选择题

> ⚠️ **已被 §15.19 取代（用户指示，2026-09）**：本节描述的"20 项背景库四态 + 5-8 道项目品类选择题"已改成
> **「设置 → 背景信息（选择题为主）→ 确定性推导能力」+ 「看自己」只问 3 个拍板问题**。
> 保留本节原文是为了留痕（它记录了当初为什么这么设计）；**当前口径以 §15.19 为准**。
> 不变量仍然成立：适配度公式（答案质量 × 背景完整度修正）、完成度与适配度分离、**硬约束 ⇒ 最高"验证后进入"**。

**要回答**：我们接不接得住？哪些边界会直接否决这个机会？

**两层设计（修复"42 题太重"与"适配度=完成度"两个缺陷）**：

1. **账号背景库（一次性，渐进完善，六维度全量保留）**：
   - 资金与预算（启动资金/广告预算/可承受亏损周期）
   - 供应链（工厂资源/MOQ/账期/交付周期）
   - 类目经验与团队（类目经验/运营/设计能力）
   - 合规资质（认证/商标/专利/品类准入）
   - 目标财务（目标售价/毛利率/净利率红线/回本周期）
   - 风险偏好（验证期长度/库存风险承受）
   
   每项四态（已具备/部分具备/不具备/待确认）+ 备注。**不再强迫一次填完**：进入项目时提示"背景库完整度 40%，将影响适配度可信度"，可随时补。
   
2. **项目内品类选择题（AI 动态生成 5-8 题）**：输入 = 背景库 + 需求分类 + 目标细分 + 竞对缝隙 → 生成与本项目直接相关的选择题（每题 2-5 个选项 + 选项旁"为什么不"说明）。示例："头部竞品已持有'凉感面料'专利。您的供应链能否在 60 天内交付同等级凉感面料？A. 能 B. 能但要 90 天 C. 不能 D. 不确定"。

**适配度计算（V2 修正，确定性公式）**：
- 仅取与目标细分/机会相关的问题子集（相关性由 AI 在出题时标注，用户可改）；
- 每题映射：已具备=1 / 部分具备=0.5 / 不具备=0 / 待确认=不纳入（降证据可信度）；
- **硬约束否决规则**：任何"决策边界"类问题答"不具备/不满足"（如毛利红线达不到、MOQ 超标、认证缺失）→ 关联机会不能推荐"直接进入"，最高"验证后进入"，并在机会卡顶部红条警示。
- 适配度 = 相关题得分均值 × 背景库完整度修正；**完成度与适配度彻底分离**。

**产出物**：背景库、品类问题与答案、优势/缺口/硬约束汇总、每张机会卡的适配度。完成条件：全部动态题已答（或显式"跳过并标记风险"）。

**二级页**：⑪账号背景库（六维度四态 + 完整度进度）、⑫品类选择题与依据（AI 为什么问这题、选项含义、答完对适配度与硬约束的影响）。

### 6.5 看机会 V2：机会卡 = 决策包（含路线图与控制点）

**要回答**：有没有机会？机会是什么？先做什么、后做什么？要哪些前置资源？卡哪些控制点？

**生成（0-N，两种合法零结论）**：
- AI 综合四看证据生成候选机会卡（默认 `ai_candidate` 状态，可确认/编辑/合并/删除/重生成；重生成不覆盖人工确认，AI 返回 0 不静默删人工结论——沿用 phase-0 防线）；
- 每条候选必须挂：需求证据 + 市场证据 + 竞对证据 + 自身适配证据；
- 零机会时输出结论二选一：**已判断无机会**（卡在哪一环：需求已满足 / 竞争无缝隙 / 自身接不住 / 利润不成立）或 **证据不足，暂不能判断**（缺什么数据、怎么补）。

**机会卡 V2 字段（在 FR-07 基础上新增决策包区）**：

```
┌─ 机会卡：#O1 「侧睡颈椎支撑枕」───────────────────────────┐
│ 需求   ：侧睡者需要高度可调的颈椎支撑（需求#U3）              │
│ 证据   ：需求强度 ████░ 市场 ███░░ 竞对缺口 ████░ 适配 ██░░░ 可信度 ███░░ │
│         评分 75 / 覆盖度 0.8 ｜ 状态：AI候选 → 已确认       │
│ 产品假设：记忆棉+分区高度+凉感套                            │
│ 利润假设：售价$45 / 采购$12 / CPC$1.2 → 毛利率 19%         │
│           （轻量三字段；更细测算用侧栏利润计算器，原样独立）   │
│ ── 执行路线图（决策包核心，新增）────────────────────────── │
│ 先做（0-30天）   ：① 送检 3 家工厂打样（成本≈$1,500）        │
│                    ② 小批量测评 20 件侧睡用户（$800）        │
│                    ③ 认证预沟通（CPC/加州65）                │
│ 后做（30-90天）  ：① 首批 500 件 FBA（资金≈$12,000）         │
│                    ② Listing 按需求#U3 关键词矩阵上线        │
│                    ③ 广告预算 $30/天 验证 CVR≥8%             │
│ 前置资源          ：打样费 / 测评样品 / 认证费用 / 首批资金    │
│                     / 关键词素材包 / 供应链账期               │
│ 控制点            ：☑准入：CVR≥8% 才放量   ☑过程：首批退货率   │
│                    <3%   ☑止损：14 天 ACOS>35% 或 亏损>$5,000 │
│                    即撤（每条控制点可编辑）                  │
│ 验证动作          ：[动作] [owner] [截止] [成功标准]          │
│ 风险              ：[需求] 侧睡人群占比存疑（中，缓解：测评验证）│
│ 决策              ：◉ 验证后进入  ○ 进入  ○ 暂缓  ○ 放弃     │
└────────────────────────────────────────────────────────────┘
```

- **先做/后做**：AI 按"验证成本最低、信息增益最高"生成两段时间线（0-30 天 / 30-90 天），每条动作可编辑、可拖序。
- **前置资源**：AI 从四看证据推导资源清单（资金/供应链/认证/素材/团队/数据），并标注"已知缺口"（对照看自己的背景库，缺的资源标红）。
- **控制点**：三类——**准入控制点**（满足才继续，如 CVR、认证通过）、**过程控制点**（监控指标，如退货率、评分）、**止损控制点**（触发即撤，如 ACOS、累计亏损额）。每条含数值与判定，默认由 AI 从利润假设+风险推导，用户必须确认（确认是拍板动作）。
- RiskItem / ValidationAction 的 category/severity/owner/successCriteria/dueDate 全部启用真实 UI（修复断链 #3）。

**优先级与决策摘要**：
- 机会卡按评分排序 + 2×2 矩阵（分数×覆盖度）；
- **全局"先做哪个机会"建议**：AI 给出排序理由（如"O1 与 O2 共用供应链，先 O1 可摊薄 O2 成本"），用户拍板；
- Go/No-Go 汇总：结论（进入 / 验证后进入 / 暂缓 / 放弃 / 无机会 / 证据不足）+ 一句话理由 + 引用证据编号。

**产出物**：0-N 张机会卡（含决策包）、优先级矩阵、全局先后建议、Go/No-Go 结论。完成条件：≥1 张卡已决策 或 零机会结论已人工确认（沿用 phase-0 控制点）。

**二级页**：⑬机会卡详情（证据/推理/反证 三栏 + 人工修改记录）、⑭执行路线图与控制点详情（0-30/30-90 天时间轴、前置资源缺口、准入/过程/止损三类控制点可编辑）。

---

## 7. 数据联动闭环（消灭暗链）

### 7.1 唯一索引：需求分类 ID

```
看用户: 需求分类树 ──ID──► 看市场: 细分标准(需求×商品×标题)
                            │
                            ▼ 目标细分 + ASIN 列表
                          看竞对: 样本池 / 满足矩阵行 / 缝隙
                            │
                            ▼ 缝隙 + 需求未满足度
                          看自己: 品类选择题(以需求+缝隙为题面)
                            │
                            ▼ 适配度 + 硬约束
                          看机会: 机会卡(挂四看证据ID) → 决策包
```

变更传播（强化版 applyLookProgressUpdate）：需求分类改 → 细分方案标"需复核" → 满足矩阵标"需复核" → 机会卡标"需复核"；未开始的页不伪标。

### 7.2 项目级数据快照（修复跨项目污染）

- 每个项目拥有自己的快照：`marketSnapshot`（商品+历史）、`segments`、`competitorPool`、`reviewSample`、`keywordSet`、`titleCorpus`。工具层/明细页写入"当前项目"快照，而不是全局 IndexedDB 键。
- 全局 IDB 键仅保留：账号、设置、演示数据、**跨项目复用的原始上传**（作为导入源，不再作为分析默认源）。
- "回填项目证据"按钮：在工具/明细页完成的任何结论（报告、标签、对比池、扫描器结果）可一键写入当前项目快照并生成 Evidence 记录。

### 7.3 证据对象（V2 新增统一结构）

```
Evidence {
  id, projectId, look: 'user'|'market'|'competitor'|'self',
  type: 'keyword'|'review'|'chart'|'asin'|'assessment'|'report',
  sourceRef: 'kw:B0XXX 流量词表 #12' | 'review:ASIN 评论 #34' | 'chart:价格带@目标细分',
  summary: 一句话摘要,
  createdAt
}
```
机会卡引用 Evidence id 列表；评分与"证据可信度"只认 Evidence，**不认页面完成度**（修复断链 #6）。

---

## 8. 评分与决策规则 V2

### 8.1 机会卡评分（权重沿用实现版 25/25/25/15/10，输入全部换血）

| 维度 | 权重 | V2 数据源（修复通胀） |
| --- | --- | --- |
| 需求强度 | 25 | 需求卡的证据强度：频度/严重度/关键词×VOC 一致性/替代代价（确定性折算） |
| 市场机会 | 25 | 目标细分机会分（趋势/体量/竞争）折算；未选目标细分则取全市场分×0.6 |
| 竞品缺口 | 25 | 需求满足矩阵 + 痛点未满足矩阵的"未满足/部分满足"占比 + 缝隙证据数折算 |
| 自身适配 | 15 | §6.4 适配度公式（答案质量×背景完整度），**与完成度无关** |
| 证据可信度 | 10 | Evidence 覆盖：样本量/来源/时效/跨源一致；缺项从分母移除并降覆盖度 |

硬规则：① AI 只解释不改分；② 数据覆盖度独立展示不纳入 100 分；③ 违反硬约束 → 不能推荐"直接进入"；④ 高分≠立项，风险与控制点必须同屏。

### 8.2 细分机会分（看市场，保持不变，已测试锁定）

趋势 0.35 + 体量 0.35 + 竞争 0.3（segmentScore，tests/lookOptimize.test.ts）。

### 8.3 进度与状态机（沿用并微调）

- 五看状态：未开始/进行中/已完成/需复核；完成度由必要证据项计算，禁止拖动，AI 不判断。
- 向导模式新增"已跳过（含风险）"子状态：跳过的看在机会卡证据可信度中降权并在报告标注。

---

## 9. UI 结构草图

> - **五看主流程 8 屏**：见 `docs/ui-wireframes.html`（浏览器直接打开）与本文件 §9.1-9.4 的 ASCII 草图。
> - **二级页与明细层 23 屏**：见 `docs/ui-wireframes-l3.html`（清单与设计意图见 §5.5）。

### 9.1 项目内骨架（宽屏）

```
┌────────────────────────────────────────────────────────────────────────┐
│ ← 项目中心 | 侧睡枕 · US | 目标: 新品立项       已保存 · 10:24  [导出▾] │
├────────────────────────────────────────────────────────────────────────┤
│  ●看用户 ── 看市场 ── 看竞对 ── 看自己 ── ○看机会 ── 报告    (步骤条)  │
├───────────────────────────────────────────────┬────────────────────────┤
│  结论: 侧睡人群对"高度可调"需求未被满足，细分    │  ▎决策草稿             │
│  市场价值中高，可先行验证。(AI 起草 · 你确认)   │  ▎可能的机会方向        │
│                                               │  ▎· 侧睡可调高度枕(强)  │
│  [第一屏] 细分机会池                           │  ▎· 凉感+支撑二合一(中)  │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐          │  ▎还缺: 竞对缝隙/自身    │
│  │侧睡 82分│ │仰睡 61分│ │俯睡 35分│  ...      │  ▎适配 → 去看竞对/看自己│
│  │趋势78 体量│ │趋势55 体量│ │趋势40 体量│        │  ▎                      │
│  │88 竞争80│ │61 竞争67│ │32 竞争38│           │  ▎[已确认 0 张机会卡]   │
│  └─────────┘ └─────────┘ └─────────┘          │  ▎                      │
│  [选择目标细分 ▸ 侧睡 (代表 ASIN 23 个)]        │  ▎                      │
│                                               │  ▎                      │
│  [第二屏] 证据 | 明细入口: 查看大盘明细 ↗       │  ▎                      │
└───────────────────────────────────────────────┴────────────────────────┘
```

### 9.2 看竞对：全量三列对比 + 每竞对三块分析 + 赢的路径（核心新组件）

```
  竞对样本: [●头部 B0A] [跟随 B0B] [●新人 B0D] (自动挑选·各1·可换人)  [源: 前台/精灵]
  ┌───────────────┬───────────────┬───────────────┐
  │ 组/字段(标来源) │  ● 头部 B0A   │  跟随者 B0B   │  ● 新人 B0D   │
  ├───────────────┼───────────────┼───────────────┤
  │ 🖥主图/视频    │ [7图][视频✓]  │ [7图][视频✗]  │ [7图][视频✓]  │
  │ 🖥价格/划线价  │ $45.9 -20%券  │ $39.9         │ $42.9 划线$59 │
  │ 🖥评分/评分分布 │ 4.5★ 分布图    │ 4.3★ 分布图    │ 4.1★ 分布图    │
  │ 🖥变体矩阵     │ 父+5子(色×尺)  │ 父+3子         │ 父+2子         │
  │ 🖥参数/Q&A    │ 材质/尺寸…QA45 │ 材质/尺寸…QA12 │ 材质/尺寸…QA6  │
  │ 🖥五点/A+/品牌 │ 五点✓A+✓故事✓  │ 五点✓A+✓故事✗  │ 五点✓A+✗故事✗  │
  │ 🖥BSR/上架时间 │ #12 枕类/2019  │ #38/2021      │ #52/2026.6    │
  ├───────────────┼───────────────┼───────────────┤
  │ 🔮月销量/销额  │ 8.2k/$376k    │ 4.6k/$184k    │ 1.9k/$82k     │
  │ 🔮流量词      │ 词45(自然18)   │ 词32(自然16)   │ 词18(自然13)  │
  │ 🔮ABA排名     │ 大词#3         │ 大词#9        │ 长尾#1×6      │
  │ 🔮广告依赖度  │ 60% 自然:广告  │ 42% 自然:广告  │ 25% 自然:广告  │
  │               │ 3:7           │ 5:5           │ 7:3           │
  │ 🔮历史价格/排名│ [走势图]       │ [走势图]       │ [走势图]       │
  ├───────────────┴───────────────┴───────────────┤
  │ AI 一行话: 头部广告强吃大词；新人长尾自然起量。    │
  │ 共同缺陷: "高度不可调"差评三列均 8%+。             │
  └───────────────────────────────────────────────┘

  [每竞对三块分析（做完全部才出结论）]
  ┌─ B0A: 用户画像(人群/场景/需求) ─┬─ B0A: 痛点未满足矩阵 ─┬─ B0A: 需求满足矩阵 ─┐
  │ 人群: 夏季怕热侧睡者           │ 痛点"睡醒颈痛"12% → 命中 │ 可调高度◐ 凉感● 机洗● │
  │ 场景: 夜间睡眠/孕期           │ 需求#U3(高度可调)未解决  │ (单元格点击看证据)     │
  │ 需求: 凉感>支撑>易清洗         │ 痛点"闷热"8% → 凉感未做透│                        │
  └──────────────────────────────┴─────────────────────────┴──────────────────────┘
  (B0B / B0D 同构三块，全部完成后解锁结论)

  [结论: 赢的路径]  ◉ 人无我有: "高度可调+凉感"二合一无人覆盖(证据#C4)
                   ○ 人有我优   ○ 人优我廉   ○ 无赢面/自身接不住
```

### 9.3 看机会决策看板

```
  结论: 存在 1 个值得验证的机会 + 1 个暂缓。Go/No-Go: 验证后进入(侧睡可调枕)
  ┌──────────────────────────────────────────────────────────────┐
  │ #O1 侧睡颈椎支撑枕 · 75分 · 覆盖度0.8 · ◉验证后进入           │
  │   先做: 打样→测评→认证预沟通 (0-30天)  后做: 首批500件→Listing→ │
  │   广告验证 (30-90天)                                          │
  │   前置资源: 打样$1.5k/测评$0.8k/认证$0.5k/首批$12k  [缺口: 认证] │
  │   控制点: ☑准入 CVR≥8% ☑过程 退货<3% ☑止损 ACOS>35%或亏>$5k   │
  │   [证据] [推理] [反证] [编辑]                                  │
  ├──────────────────────────────────────────────────────────────┤
  │ #O2 凉感+支撑二合一 · 41分 · 覆盖度0.5 · ○暂缓                 │
  └──────────────────────────────────────────────────────────────┘
  [优先级矩阵] 高价值高覆盖: #O1 ｜ 需补证据: #O2
  [决策摘要] 一句话结论 + 证据编号索引
  [导出] HTML 审核报告 | Markdown
```

### 9.4 导出决策包（HTML/MD 叙事顺序，沿用 charter）

研究目标 → 用户需求（搜索路径+需求树）→ 市场细分（方案+评分）→ 竞品缺口（三列对比摘要+矩阵）→ 自身适配 → 机会结论（卡片+路线图+控制点）→ 证据索引与口径。首页：研究目标/站点/最终结论/机会数量/生成时间。

### 9.5 二级页 ASCII 草图（关键三屏）

**⑤ 细分市场完整大盘（= 老版大盘 + 自动筛选）**

```
┌─ 项目中心 / 侧睡枕US / 看市场 / 侧睡枕细分大盘 ────────── [返回看市场] ─┐
│ 筛选条: 当前=侧睡枕细分(23 ASIN) ▾切换细分  [切到全市场]  月份: 近12月 ▾│
├────────────────────────────────────────────────────────────────────────┤
│ KPI: 月销 $1.2M · 月销量 29k · 均价 $41 · 均分 4.4 · Top10 集中度 38%   │
│ [趋势折线图]    [价格带分布]    [价格-评分散点]                         │
│ [品牌榜]        [集中度 CR4/CR8] [BSR 分布]                             │
│ [上架时间分布]  [新老品占比]     [评分分布]                             │
│ [季节热力图]    [卖家类型]       [卖家位置]                             │
│ [Top 商品表: ASIN/品牌/价格/月销/评分/上架/变体]   数据质量⚠ 样本 23     │
└────────────────────────────────────────────────────────────────────────┘
```

**③ 需求分类树详情（证据链重点）**

```
┌─ … / 看用户 / 需求分类树 ────────────────────────────── [返回看用户] ─┐
│ 需求树（左 40%）                   │ 证据面板（右 60%）选中: 高度可调    │
│ ▸ 支撑需求                         │ 结论: 部分满足（头部未做好）        │
│    ● 高度可调 [部分满足] ←选中      │ 关键词证据(6): adjustable height…  │
│    ○ 颈部贴合 [已满足]              │   ↳ 点开 → 关键词表第 12 行        │
│ ▸ 体感需求                         │ 评论原文(9): "睡醒脖子疼，不能调"   │
│    ● 凉感     [已满足]              │   ↳ 点开 → 评论原文 + ASIN         │
│    ○ 透气     [部分满足]            │ 覆盖 ASIN(14): B0A B0B …           │
│ ▸ 维护需求                         │ 证据强度: 强（跨 3 来源一致）       │
│ [＋新增][合并][拆分][重命名]        │ [回填项目证据] [标记为细分标准]     │
└────────────────────────────────────┴───────────────────────────────────┘
```

**⑬ 机会卡详情（证据 / 推理 / 反证）**

```
┌─ … / 看机会 / #O1 侧睡颈椎支撑枕 ──────────────────── [返回看机会] ─┐
│ 结论与评分              │ 结构化推理链               │ 反证与缺失证据     │
│ 75 分 · 覆盖度 0.8      │ 证据#U3 侧睡者高度可调需求  │ 反证: 侧睡人群占比 │
│ 需求强度 ████░ 21/25    │   ↓ 判断                   │ 存疑（仅评论推断） │
│ 市场机会 ███░░ 19/25    │ 头部三列均未解决(满足矩阵◐) │ 缺失: 无 ABA 明确  │
│ 竞品缺口 ████░ 21/25    │   ↓ 判断                   │ "高度可调"搜索量   │
│ 自身适配 ██░░░ 8/15     │ 供应链可承接（看自己 A2）   │ 人工修改记录:      │
│ 证据可信度 ███░░ 6/10   │   ↓ 结论                   │ 09-10 PM 下调适配度│
│ [决策: 验证后进入 ▾]    │ 赢的路径 = 人无我有         │ （原 10 → 8）      │
└─────────────────────────┴────────────────────────────┴────────────────────┘
```

---

## 10. "傻瓜式"体验细则（10 分钟路径）

### 10.1 计时路径（数据池就绪的常态）

| 分钟 | 步骤 | 用户动作 |
| --- | --- | --- |
| 0-1 | 建项目 | 输入品类名/关键词/ASIN（三选一），回车即开始 |
| 1-4 | 看用户 | 系统按锚点自动取数（词 TOP200 / 评论 60 条 / 竞品标题 TOP30）→ 3 个选择题（确认搜索路径/偏好/需求分类）+ 标记细分标准 |
| 4-6 | 看市场 | 选一个细分方案（2-3 选 1）→ 看评分排行 → 点"选目标细分" |
| 6-8 | 看竞对 | 系统自动抓 3 个竞对（头/跟/新各 1）→ 扫读全量 Listing 三列对比 + 每竞对三块分析 → 确认赢的路径（人无我有/人有我优/人优我廉） |
| 8-9 | 看自己 | 答 5-8 道品类选择题 |
| 9-10 | 看机会 | AI 出候选 → 确认/修改 → 拍板决策 → 导出决策包 |

**关键设计**：每步的 AI 结论都以"请确认"卡呈现（默认接受=点一下"确认"）；全程右侧"决策草稿"显示进展与缺口；任何一步缺数据立即提示"缺 X，影响 Y，[用样例继续] [补数据]"；快速模式固定口径=**词 TOP200 / 评论 60 条 / 竞对 3 个**（深度模式评论 200 条，缓存 14-30 天）；**触发模型=一键向导 + 每步局部重跑**；**每一轮交付由用户本人测试验收，PM 以用户的实测反馈作为下一轮输入。**

### 10.2 防呆与空状态规范（继承 P0-5 并强化）

- 每页一个主标题（结论）、一个主操作；正文 ≥14px，说明 ≥12px；状态=图标+文字+颜色三重表达；关键内容不永久截断（可展开）；
- 空状态四要素：为什么空 / 缺什么 / 点哪补 / 补完得到什么；
- 向导每步完成自动 toast"下一步：看市场（约 2 分钟）"；
- 错误提示带"重试/换源/跳过并标记"三选一；
- 演示模式：预置完整示例品类（薄枕头），**细分/竞对挑选/三列对比/机会卡全部可用**（修复 demo 空转）。

---

## 11. 技术架构改革

### 11.1 前端

- 保持 React 19 + Vite + TS + Tailwind；`src/components/` 按五看拆分（five-look/, user-look/, market-look/, competitor-look/, self-look/, opportunity-look/，沿用 refactor 计划的组件清单）；
- `App.tsx`（2024 行）拆分：路由/全局壳、项目域、工具域、设置域；删除 LookWorkspacePlaceholder、CompetitorAnalysis 等死代码；修复双写块、恒等三元、站点重复条目；
- 激活 `lookAi.runLookAnalysis`：结构化 Prompt + JSON Schema 回填 + 人工确认闸门（AI 产物一律 `ai_candidate`，不进正式结论）；
- 引入 ProjectContext：工具/明细页通过它写入项目快照（消灭 IDB 暗链与手动搬运）；
- 新增 `EvidenceStore`（§7.3 证据对象）；扩展 workspaceIdb 白名单。

### 11.2 服务端与平台数据池（商用核心）

```
浏览器 ──► /api/data/* (Vercel Functions + 本地 server/)
              ├─ 卖家精灵/领星 统一网关（管理员密钥，用户无感知）
              ├─ 请求缓存（词/ASIN/评论按 TTL 缓存，降本）
              ├─ 用量记账（workspace × 工具 × 次数，Phase B 计费预埋）
              └─ 竞品 Listing 抓取编排（前台全字段 + 卖家精灵后台数据一次编排）
```
- 管理员在"设置→数据池"维护密钥与配额（继承 phase-0"管理员一次配置"目标）；
- **平台**密钥只在服务端，前端永不接触（消除本地 Key 模式的安全负担）；
  ⚠️ 本条已被 **§15.26（BYO Key）** 精确化：平台密钥的口径不变，但**用户自带**的 MCP Key 允许存在其本机。
- 导出服务：HTML（已有）→ MD；**不做 PPT**（用户确认"HTML+MD 就够"）。

### 11.3 质量护栏

- 确定性逻辑（评分/进度/利润/挑选）全部保持纯函数 + 测试（现有 29/29 基础上每阶段加测）；
- 发布门槛：`npm test` + `npm run lint` + `npm run build` + 4 视口 UI 检查（390×844 / 768×1024 / 1280×800 / 1440×900，沿用 P1 回归清单）+ 截图基线。

### 11.4 管理员后台（V2 新增，M5 交付）

**现状：没有后台。** 仅有 `/api/settings`——管理员读写共享 Key，密钥存在管理员账号的 `user_metadata.appKeys` 里；管理员身份靠 `ADMIN_EMAILS` / `VITE_ADMIN_EMAILS` 环境变量判定。没有用户列表、没有用量统计、没有审计日志。

**目标**：新增 `/api/admin/*`（service_role + 管理员校验）+ 后台页面（仅管理员可见的独立入口）。

| 模块 | 内容 |
| --- | --- |
| 用户管理 | 列表/搜索、角色（admin/user）、最近登录、启用/禁用、重置密码、所属项目数 |
| 用量统计 | MCP 调用次数（按工具 × 项目 × 用户）、LLM token 与估算成本、项目数/报告数、取数失败率 |
| 配置中心 | 数据池密钥（卖家精灵/领星/AI 默认 Key）、默认模型、功能开关、示例数据版本、每用户配额 |
| 审计日志 | 登录、取数、报告生成、机会决策等关键操作留痕（谁/何时/做了什么） |
| 项目与报告 | 查看/转移/删除、异常报告复查 |
| 环境自检 | Supabase 连通性、service key、JWT secret、管理员邮箱、数据池密钥是否就绪（不回显密钥值）——§2.5-4 的根治 |

技术要点：新增 `usage_events` 记账表（M5 数据池同批落地）；管理员接口一律 service_role + JWT 校验，前端仅传 token；所有管理操作写审计日志。

---

## 12. 6 个月路线图（用户确认"打磨完整版"）

| 里程碑 | 周期 | 交付 | 验收 |
| --- | --- | --- | --- |
| **M0 环境修复（用户确认立即执行）** | 第 1 周（与 M1 并行） | 新建 Supabase 项目 + 执行 `all_in_one.sql`；Vercel 生产/预览/开发三环境配齐 `SUPABASE_URL`/`PUBLISHABLE_KEY`/`SERVICE_ROLE_KEY`/`APP_JWT_SECRET`/`ADMIN_EMAILS`/数据池密钥；更新本地 `.env.local`；**删除硬编码默认 URL/Key**；新增 `/api/health` + 设置页"诊断"面板；本地补 `/api` 中间件 | 预览版可注册/登录；本地 `npm run dev` 可登录；诊断面板能指出缺失配置；三个环境互不污染 |
| **M1 地基与闭环修复** | 第 1-4 周 | 项目级快照+Evidence 对象；激活 lookAi 引擎；修复 12 条断链中 #1/#2/#5/#7/#9/#10/#11/#12；**一键向导触发模型**+步骤条+每步局部重跑按钮；决策草稿面板；**老版资产归位第一批**：ToolRoute 上下文透传（§2.7.6）+ 删除 7 条死代码（§2.7.4）+ 利润计算器/市场大盘(全市场)移入侧栏（原样） | 旧项目数据可导入快照（字段对齐预检）；AI 回填进五看且不覆盖人工；demo 模式五看全链路可用；一键跑完五步并有过程反馈；从任一五看打开明细层后返回不丢上下文 |
| **M2 看用户+看市场 V2** | 第 5-8 周 | 搜索路径图/搜索偏好卡/带证据需求分类树；细分三方合成（需求×商品×竞对标题）+ 细分方案选择题 + 目标细分选择联动竞对池 | 真实品类 3 例：需求分类→细分→ASIN 归属全自动；细分评分与手动分组结果偏差可解释 |

> M2 进度：① 搜索路径图（含四层流向）+ 搜索偏好卡 ✅（`f334f7e` / 流向补充见 §15.8）、② 带证据需求分类树 ✅、③ 细分三方合成 ✅、④ 目标细分联动竞对池 ✅、⑤ 断链 #7 ✅ —— 全部落地，详见 §15.8。

| **M3 看竞对+看自己 V2** | 第 9-12 周 | 全量 Listing 三列对比（前台全字段+卖家精灵数据，标来源）+ 每竞对三块分析（用户画像/痛点未满足矩阵/需求满足矩阵）+ 赢的路径三选一；背景库（六维度渐进）+ 品类选择题 + 适配度公式 | 3 个竞对全字段抓取成功率 ≥90%（缓存命中不计）；硬约束能正确触发"不可直接进入"；赢的路径无法三选一时正确落到"无赢面" |

> M3 进度：① 竞对挑选（表现最好的代表性新人 + 换人 UI）✅、② 三列对比字段表 + 覆盖率 ✅、③ 三块分析（画像/痛点矩阵/满足矩阵）✅、④ 赢的路径（含硬约束只限制进入方式、不抹掉赢面）✅、⑤ 看自己 V2（六维度背景库 + 品类选择题 + 适配度 + 硬约束结论）✅、⑥ 断链 #4/#9/#10 全部关闭 ✅ —— **M3 目标项全部完成**，详见 §15.9。
| **M4 看机会 V2+决策看板** | 第 13-16 周 | 机会卡决策包（先做/后做/前置资源/控制点）；全局先后建议；Go/No-Go 汇总；HTML/MD 导出；**合并 phase-0 的 6 个能力文件**（`opportunityAi` AI 机会生成 / `opportunityHtmlReport` HTML 审核报告 / `projectDecision` 决策摘要 / `teamStore`+`TeamSettingsPanel` 团队设置，§15.1-24） | 10 个真实品类回放：0 次误判（北极星护栏）；控制点可编辑并出现在导出件；HTML 审核报告可独立打开 |

> M4 进度：① 机会卡模型（§6.5 决策包 + 证据引用 + 推理/反证）✅、② 确定性评分换血（§8.1 25/25/25/15/10）✅、
> ③ 决策包推导（路线图/前置资源/控制点）✅、④ AI 生成候选 + 反证审查 + 非破坏性合并 ✅、
> ⑤ Go/No-Go 汇总 + HTML 审核报告 ✅、⑥ 零机会两结论 ✅、⑦ 决策看板 UI ✅、
> ⑧ phase-0 六个能力文件合并（决策 A）✅ —— 全部落地，详见 §15.10。
| **M5 数据池+管理员后台+加固+内测** | 第 17-20 周 | 平台统一数据池（网关/缓存/配额/记账）；**管理员后台（§11.4：用户/用量/配置/审计/环境自检）**；安全加固（密钥不出服务端）；性能（项目中心 ≤2s）；**用户逐轮自测** + 内部试用 | 数据池月度成本可度量；管理员可看到每个用户的用量与状态；完成 ≥30 个项目回放；NPS/完成率基线 |

> M5 进度（详见 §15.11）：① 数据池网关 ✅、② 用量记账表 + 记账/配额/成本 ✅、③ 管理员后台接口 ✅、
> ④ 管理员后台面板 ✅、⑤ 安全加固（密钥不再离开服务端 + 源码级守卫）✅、⑥ 导出补 MD ✅；
> ⑦ 性能（项目中心 ≤2s）与 ≥30 项目回放 / NPS 基线属用户侧实测，待用户执行。
| **M6 商用收口** | 第 21-24 周 | 团队空间与权限收口；定价/订阅实验设计（Phase B 蓝本）；上线检查清单；操作手册 | 交付 3 份真实品类决策包（HTML/MD）给管理层评审；10 分钟计时可用性测试达标 |

每里程碑均有：功能验收 + 确定性测试 + 4 视口 UI 回归 + 真实品类回放（M2 起每阶段 ≥3 例）+ **用户本人实测通过后才进入下一里程碑**。

---

## 13. 风险与对抗式预演

| 风险 | 概率/影响 | 对策 |
| --- | --- | --- |
| 数据池成本失控（抓取/LLM 用量） | 高/中 | 缓存 TTL、按项目配额、用量记账仪表盘；快速模式固定口径（词 TOP200 / 评论 60 条 / 竞对 3 个）；**双通道：产品表/历史表默认上传不抓** |
| AI 幻觉导致错误立项 | 中/高 | 确定性评分+证据溯源+反证审查；"证据不足"与"无机会"强制区分；AI 只起草人拍板；回放测试护栏 |
| "10 分钟"与"深度"冲突 | 高/中 | 快速/深度双模式；快速模式结论标注"初筛级"，深度模式才建议直接进入 |
| 统一密钥的安全与滥用 | 低/高 | 密钥仅服务端；成员权限分级（owner 可配）；调用审计日志 |
| 向导过度设计反而复杂 | 中/中 | 每步 ≤3 个选择题；全程可跳过；可用性测试每阶段跑真实用户 |
| 旧数据/旧项目迁移 | 中/中 | 迁移预览不自动删；旧数据导入快照；读取兼容保留一个版本周期（沿用 PRD 迁移条款） |
| 第三方（卖家精灵等）接口变更 | 中/中 | 网关层做适配隔离；错误显式提示不伪造结论（沿用数据纪律） |

---

## 14. 范围外（V2 明确不做）

多项目组合管理驾驶舱；任务/提醒/日历；复杂权重编辑器；广告投放与库存管理；支付订阅系统（Phase B）；数据可视化炫技换肤；移动端 App；PPT 导出（用户确认 HTML+MD 足够）。

---

## 15. 决策记录与待确认项

### 15.1 用户已确认（多轮共 22 项）

1. 商用定位：内部团队自用为主，顺带产品化
2. 数据接入：平台统一数据池
3. 傻瓜式标准：新用户 ≤10 分钟拿到结论，AI 向导对话式引导
4. 五看形态：向导式逐步推进
5. 竞对对比：自动抓取 + 可视化三列对比（头部/跟随者/新人 ≥3）
6. 看自己：六维度全选（资金/供应链/类目经验/合规/目标财务/风险偏好）
7. 交付物：在线决策看板 + 一键导出（第二轮修正为 **HTML/MD**，不做 PPT）
8. 节奏：6 个月打磨完整版
9. **产品名：Kairo**（替换 AmzDev Tool）
10. **快速模式口径：词 TOP200 / 评论 60 条 / 竞对 3 个**（深度模式评论 200 条，见第 20 项）
11. **导出：HTML + MD 就够，不需要 PPT**
12. **测试方式：每一轮交付由用户本人测试验收**
13. **双模式：快速 10 分钟 / 深度 40 分钟，接受**
14. **数据获取双通道**：词/评论/竞对 Listing 走卖家精灵 MCP 抓取（小数据）；**产品表+历史表沿用上传 Excel/CSV**（大数据，抓取慢且烧 MCP 次数，默认上传，可选抓取但明示成本）
15. **评论 200 条构成（深度模式）**：锚点 ASIN 80 + 品类头部 2 个 ASIN 各 60，合并去重（快速模式按 20+20+20，见第 20 项）
16. **人优我廉**：允许作为赢的路径，但须在净利率红线内，机会卡标注价格战风险
17. **新人定义与缺位处理**：新人=上架 ≤180 天中**表现最好的代表性 ASIN**（不是上架最新的）；缺位时取"上线略早"的次新且表现好的替代
18. **示例品类**：真实品类"薄枕头"贯穿五看，作为演示模式默认体验路径
19. **触发模型：一键向导 + 每步局部重跑**（不做"每板块各自 AI 按钮"形态）
20. **评论分档 + 缓存**：快速 60 条（3 次调用）/ 深度 200 条（10 次调用），同 ASIN 缓存 14-30 天
21. **管理员后台：M5 与数据池同批交付**（§11.4）
22. **Supabase 立即修复（M0）**：新建项目 + 配齐三环境变量 + 删硬编码默认值 + `/api/health` 自检
23. **云底座选型（2026-09 确认）**：现阶段**沿用 Vercel + Supabase**，不迁国内；七牛/CloudBase 等国内底座**后置**——待 App 功能打磨完成后另行立项迁移（见 §17）。
24. **phase-0 分叉的 6 个能力文件**（`opportunityAi` / `opportunityHtmlReport` / `projectDecision` / `teamStore` / `TeamSettingsPanel` / `teamStore.test`）**在 M4 合并回来**（用户确认，2026-09）。它们正是"AI 生成机会候选卡 / 独立 HTML 审核报告 / 项目决策摘要 / 团队设置"的现成实现，对应 V2 的 §6.5、㉑ 报告页与 Phase B 团队空间。

### 15.2 本 PRD 中的 PM 决策（可推翻）

- 关键词/VOC/竞品降为明细层（不再顶级导航）；利润计算器与市场大盘（全市场）移到侧栏（原样照搬）；
- 评分权重沿用 25/25/25/15/10 但输入改为证据质量；
- 看自己改"背景库渐进 + 品类选择题"两层（解决 42 题负担）；
- 快速/深度双模式；
- 6 个月里程碑切分（M1-M6）；
- 数据锚点模型（§6.1）：看用户数据由"品类/关键词/ASIN 锚点"反查扩展，无循环依赖。

### 15.3 追问已全部拍板（第二轮追问 5 项 → 已并入 §15.1 第 14-18 项）

| 追问 | 拍板结果 |
| --- | --- |
| 数据池权限/取数方式 | 双通道：小数据 MCP 抓、大数据上传 Excel（§15.1-14，§6.1） |
| 评论 200 条构成 | 锚点 80 + 头部 2×60（§15.1-15） |
| 人优我廉 | 允许，须在毛利红线内（§15.1-16，§6.3） |
| 竞对缺位处理 | 新人=表现最好的代表；缺位取次新且表现好的（§15.1-17，§6.3） |
| 示例品类 | 真实品类：薄枕头（§15.1-18） |

> 当前无待拍板问题；下一步：用户放行后按 §12 执行 —— **M0 环境修复（立即）** 与 **M1 地基与闭环修复**。

### 15.4 第三轮已拍板（2026-09 侦察后 4 项，并入 §15.1 第 19-22 项）

| 问题 | 拍板结果 |
| --- | --- |
| 流程如何触发 | **一键向导 + 每步局部重跑**（§5.2-5：一键"开始分析"→ 按序执行 → 每步"请确认"卡；局部按钮：重新取数 / AI 重跑这一步 / 手动改结论） |
| 评论取样成本（`review` 单次上限 20 条） | **分档 + 缓存**：快速 60 条（3 次调用）/ 深度 200 条（10 次调用），同 ASIN 缓存 14-30 天（§6.1） |
| 管理员后台时机 | **M5 与数据池同批**（§11.4、§12） |
| Supabase 失效 | **立即修（M0）**：新建项目 + 三环境变量 + 删硬编码 + `/api/health`（§2.5、§12 M0） |

**评论取样算式**：快速 60 条 = 锚点 20 + 头部 2×20 = **3 次调用**；深度 200 条 = 锚点 80（4 次）+ 头部 2×60（各 3 次）= **10 次调用**；命中缓存 **0 次**。

> 当前无待拍板问题。下一步：用户放行后按 §12 执行 —— **M0 环境修复（立即）** 与 **M1 地基与闭环修复**（断链 + 一键向导骨架 + 双通道底座）。

### 15.5 第四轮要求已落地（2026-09，用户三条指示）

| 用户要求 | 落地位置 |
| --- | --- |
| 「查看完整大盘」点开后弹出完整细分市场数据与图表 = **老版市场大盘页自动筛选** | §6.2 步骤 3、§5.5 二级页⑤、§9.5 ASCII 草图、`ui-wireframes-l3.html` 页 5（chip：本页全部模块复用老版大盘 · 仅筛选条件不同）；⑲ 为未筛选的全市场版，二者同一模块 |
| 看用户 ①-④ 都要有二级页面、讲清楚、**证据链丰富** | §5.5 二级页①②③④、§6.1 步骤 1-4 各自挂二级页；③ 需求分类树为证据链重点页（关键词证据 + 评论原文 + 覆盖 ASIN + 证据强度，均可点回原始来源）；`ui-wireframes-l3.html` 页 1-4 |
| **老版（main）资产要完整保留并作为改革基础，不能丢** | §2.7 老版资产清点与归位（总量结论 + 3 处"能力未接线" + 7 条死代码 + 最易丢失 Top 10 + 导出能力矩阵 + ToolRoute 上下文规范）、§5.6 老版资产保留四原则、§5.5 明细层 ⑮-㉓ 全部标注"复用老版 XX 模块"、`ui-wireframes-l3.html` 七个页面写明"复用老版 XX 模块并自动带入当前项目与筛选上下文" |

### 15.6 第五轮修改已落地（简洁化 + 归位，2026-09）

| 用户修改要求 | 落点 |
| --- | --- |
| 主图逐张第 4-7 张与 1-3 张同样对比 | §6.3 二级页⑧、§5.5 二级页⑧（第 1-7 张全部逐张对比） |
| 利润计算器原样照搬，放首页左侧栏（项目中心下），不进项目/二级页 | §5.1 左侧栏、§5.3、§5.5 ⑱、§2.7.2、§2.7.6 |
| 市场大盘明细（全市场）照搬原样，不做任何改变 | §5.5 ⑲、§2.7 |
| 机会扫描器取消独立页，仍放在大盘全市场内 | §5.4 术语表、§5.5 ⑳、§2.7 |
| 登录页照原样，不改 | §5.5 ㉒ |
| 审查一遍，减少看不懂/无用的东西，保持简洁 | §4 原则 10 + §16 简洁性裁剪 |

---

### 15.7 M1 地基与闭环修复 —— 完成记录（2026-09）

推送区间：`phase-0` 分支 `5436f6c → 45e908a`（**main 保持 `63abd4c` 未动**）。每步均通过 `tsc --noEmit` + 全部测试套件 + `vite build`。

| M1 目标项 | 状态 | 关键提交 |
| --- | --- | --- |
| 处置 7 条死代码 | ✅ | `e90557e`（删 CompetitorAnalysis 692 行、占位骨架+LOOK_SCOPE、恒等三元）、`1f45eea`（supabaseConfig 接回自检） |
| 修复埋伏 bug（isDemo 恒假 / competitorWorkspace 只读不写 / workspaceIdb 白名单） | ✅ | `e90557e` |
| 利润计算器 + 市场大盘（全市场）移入左侧栏（原样照搬） | ✅ | `e90557e` |
| 激活 lookAi AI 引擎（断链 #1） | ✅ | `5b07bd4`（看市场/看用户/看竞对）、`9ec050d`（看自己，新增 `aiDraft`） |
| 一键向导触发模型 + 决策草稿面板 | ✅ | `88c09d1`（步骤条 + 开始分析 + 每步状态 + 决策草稿；回填逻辑收敛到 `lookAiApply` 共用） |
| 项目级数据快照与 Evidence 对象 | ✅ | `fdce257`（快照修断链 #2 跨项目污染，AI 带数据范围）、`76709bb`（Evidence 结构化 + 按来源去重 + 向导展示） |
| ToolRoute 上下文透传（断链 #10） | ✅ | `c784ca2`（从某看打开工具后，返回按钮精确回到那个看） |
| demo 态五看空转（断链 #8） | ✅ | `45e908a`（示例数据派生展示用细分，细分评分与竞对挑选在演示态可用，示例报告缓存仍命中） |

**新增测试**：`tests/lookAiMerge.test.ts`（10 条：AI 回填非破坏性规则）、`tests/evidence.test.ts`（7 条：证据去重/截断/分组/描述）。测试套件由 8 个增至 **10 个，共 84 条断言全绿**。

**未纳入 M1（顺延）**：
- 断链 #7「看市场 openQuestions 无消费者」→ 随 M2「看用户/看市场 V2」的二级页联动一起做（openQuestions 要显示在对应看页面顶部）；
- `optimistic.test.ts` 曾出现 1 次偶发失败：**连跑 10 次无法复现**，判定为环境瞬时问题，未改动断言（不削弱既有校验）。

---

### 15.8 M2 看用户 V2 —— 进度记录（2026-09，进行中）

推送区间：`phase-0` 分支 `45e908a → 8cc6e01`（**main 保持 `63abd4c` 未动**）。每步均通过 `tsc --noEmit` + 全部 16 个测试套件（139 条断言）+ `vite build`。

| M2 目标项（§12） | 状态 | 落点 |
| --- | --- | --- |
| ① 搜索路径图 + 搜索偏好卡 | ✅ 已推送 `f334f7e`（四层流向补于本轮） | `userLook.ts`（`SearchPath`/`SearchPathLayer`/`SearchPreference`/**`computeSearchPathFlow`**）、`lookAiApply.normalizeSearchPath/normalizeSearchPreference`、`UserLookView` 四层漏斗（认知→考虑→决策→场景）+ 流向条 + 偏好卡（价格敏感度/品牌倾向/长尾程度） |
| ② 带证据需求分类树 | ✅ 本轮落地 | `NeedEvidence`（关键词 + 评论原文 + 覆盖 ASIN）、候选 `category/subCategory/evidence`、`computeNeedEvidenceStrength` 确定性证据分、`UserLookView` 按**需求域 → 需求卡**分组 + 证据链区（词 chip / 评论原文带 ASIN / 覆盖 ASIN / 证据分） |
| ③ 细分三方合成（需求分类 × 商品属性 × 竞品标题聚类 → AI 2-3 方案选择题） | ✅ 本轮落地 | `segmentSynthesis.ts` + `SegmentSchemeChooser`（§6.2 步骤 3） |
| ④ 目标细分选择联动竞对样本池 | ✅ 本轮落地 | `CompetitorPickerPanel` 改为项目作用域 + 读看市场选定目标细分（§6.2 步骤 3 → §6.3） |
| ⑤ 修断链 #7（看市场 `openQuestions` 被下游消费） | ✅ 本轮落地 | `openQuestions.ts` + `OpenQuestionAnswersCard`（看用户/看竞对页顶同意回答）+ 下游 AI 提示词强制正面回应 |

**M2② 关键设计（确定性优先，AI 只解释不改分）**：

| 规则 | 实现 |
| --- | --- |
| 证据强度不用 AI 主观档位 | `computeNeedEvidenceStrength`：关键词 每条 +12（上限 36）、评论原文 每条 +18（上限 54）、覆盖 ASIN 每个 +5（上限 20）、三类齐全再 +10「跨来源一致」；≥70 高 / ≥40 中 / 其余低 |
| AI 给的 `evidenceStrength` 只在**完全没有结构化证据**时兜底 | `mergeUnmetNeedCandidates`：有 `evidence` → 用公式档位；无 → 才用 AI 档位（避免"AI 说高就高"） |
| AI 输出不可信 | `normalizeNeedEvidence`：空白词/空引用过滤、搜索量非正数丢弃、评论引用**逐字保留不改写**（≤400 字）、ASIN 大写 + `^[A-Z0-9]{6,}$` 清洗 + 条数上限（词 10 / 引用 8 / ASIN 20） |
| 人工内容永不覆盖 | 已有任何一条人工候选 → AI 一条都不动（`acc.skipped`） |

**新增测试**：`tests/needEvidence.test.ts`（10 条：折算公式与上限、跨来源加分、归一化清洗、过短 ASIN 丢弃、证据优先于 AI 档位、无证据兜底）、`tests/lookAiFailure.test.ts`（7 条：失败归类 + "缺 Key 绝不算跳过"回归）、`tests/segmentSynthesis.test.ts`（16 条：分词/词表确定性、单归属不变式、需求挂钩、方案比较与推荐、空数据不造假）、`tests/openQuestions.test.ts`（6 条：问答对齐与"不编造"）、`tests/searchPathFlow.test.ts`（8 条：四层流向的两种口径、层序与 AI 顺序无关、缺口径不编造）。测试套件由 11 个增至 **16 个，共 139 条断言全绿**。

**M2① 搜索路径「流向」补充（本轮）**：`computeSearchPathFlow` 把四层按固定顺序对齐，算出每层相对上一层的**保留比例与流失比例**：两层都有搜索量时用搜索量口径（真实需求收缩），否则退化到词数口径并明确标注口径（绝不混算）；搜索量为 0/缺失时返回 `null` 并给出人话说明，不编造比例。AI 提示词同时明确"你不需要也不要输出任何流向百分比"——流向永远由系统算。

**本轮修掉的一个真实 bug（我自己引入的）**：向导在无数据时保存了**空快照**并被当成有效范围，导致 AI 永久跳过取数。已修为「空快照忽略并重新采集」，并在向导"缺数据"卡上给了可点按钮（去取数 / 加载示例）。用户此前反馈的"四步都被跳过"即由此而来。

**同时修掉"看起来正常、其实是没配置"**：旧逻辑用 `/尚未|请先/` 正则把一切都算「已跳过」，导致**没配 AI 模型 Key 时也显示"四步都被跳过"**（用户会以为流程正常）。新增 `src/utils/lookAiFailure.ts`：失败收敛为 `no-key / no-data / parse / other` 四类，**只有 `no-data` 才叫「跳过」**；缺 Key 时向导显示红色结论「四步都没跑：还没有配置 AI 模型 Key（这不是"跳过"）」并给一个直达「设置 → API 与模型」的按钮（`LookWizardPanel` → `ProjectWorkspace` → `App` 透传 `onOpenSettings`）。

**M2③ 细分三方合成（用户核心诉求："细分逻辑没跟上一步挂钩"）**：

| 方案 | 切法（全部确定性） | 什么时候选它 |
| --- | --- | --- |
| A 需求域 | 细分名 = 看用户的需求域；商品按"标题命中该需求的词"**单归属**；每个细分挂上命中的未满足需求 + 证据分 | 结论要能直接回到用户需求与 VOC 证据 |
| B 价格段 × 商品属性 | 价格按分位切三段，段内按样本里最普遍的商品属性（固定词表）对半分 | 要定价、算利润、定卖点 |
| C 竞品标题聚类 | 标题词频（document frequency）取前 4 个词簇，单归属，命中词表给中文释义 | 找"竞对反复强调却仍被差评"的点 |

- 三个方案都是**真切分**：商品不重叠、收入占比合计 ≤ 100%、未归属商品显式计数（不藏长尾）；
- 评分复用 `segmentScore`（趋势 × 体量 × 竞争 → 机会分）；**AI 只能解释差异，不能改分、不能替用户选方案**（prompt 里明写）；
- 推荐公式（确定性）：需求挂钩度 50% + 覆盖率 30% + 收入加权机会分 20%，平局优先需求域方案，且必须声明"最终方案由你选"；
- 选定结果写回看市场（`segmentSchemeId` / `chosenSegmentIds`），看竞对的样本池据此挑选（M2④）。

**M2⑤ 断链 #7 的真实闭环**（不只是显示）：看市场的待验证问题 → 看用户/看竞对页顶**可就地回答**（回答存在下游那一看自己身上）→ 下游 AI 提示词里被强制"必须正面回应，不要编造数据"。

**环境备注（便于下次接手）**：本机 `git` 不在 PATH 上，位于`C:\Users\A\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\git\cmd\git.exe`（提交/推送需用全路径调用）；`optimistic.test.ts` 的偶发失败只在**与 `vite build` 并发抢占 CPU** 时出现，独立连跑 6 次 + 13 套件串行全跑均全绿，判定为环境噪音，未改动断言。

---

### 15.9 M3 看竞对 V2 + 看自己 V2 —— 完成记录（2026-09）

推送区间：`phase-0` 分支 `3d26690 → 24eeaad`（**main 保持 `63abd4c` 未动**）。每步均通过 `tsc --noEmit` + 全部 22 个测试套件（200 条断言）+ `vite build`。

| M3 目标项 | 状态 | 落点 |
| --- | --- | --- |
| ① 自动挑选 3 竞对（表现最好的代表性新人） | ✅ | `competitorPicker.ts`：新人 = 上架 ≤180 天里 **表现分 = 收入位置 0.5 + 评论增长 0.3 + 评分 0.2** 最高者（不是上架最新）；无新人 → 次新窗口（≤540 天）回退并显式标注；再没有 → 新人位**空缺**（绝不用老品冒充新人）。跟随者排序固定为 同品牌 → 收入更高 → 有增长 → 价格贴近 → ASIN。每项带"为什么选它"（含数字）；**换人 UI 在 `CompetitorDeepDive` 每张卡右上角**（按角色规则列候选 + 一句"为什么它也行"，改的是看竞对样本池那一列） |
| ② 全量 Listing 三列对比 | ✅ 注册表与视图就绪，抓取待 M5 | `listingFields.ts`：33 个字段 × 3 组（产品/Listing/流量），每字段标来源 🖥前台/🔮卖家精灵/📊推算；"未抓取"与"值为空"严格区分（未抓取给补齐方式）；另给**字段抓取率**（M3 验收线 ≥90%）与缺口按来源归因。**Listing 全文与流量字段暂无生产者（§11.2/M5 的抓取编排）**，当前如实显示"未抓取" |
| ③ 每竞对三块分析 | ✅ | `competitorAnalysis.ts`：痛点未满足矩阵（差评 1-3 星 → 10 类痛点单归属 + 占比 + 原文）、需求满足矩阵（●◐○—，判定顺序：差评命中→○ / 好评命中→● / 标题五点自称→◐ / 全无→—）、用户画像（人群/场景/诉求，逐字引用；抽不到就写缺什么）；证据全部可点回 |
| ④ 赢的路径 | ✅ | 严格按 人无我有 → 人有我优 → 人优我廉 → **无赢面** 顺序判定；三块分析与交叉清单是前置条件（对比没做完不出结论）；**硬约束不抹掉赢面**，只把"直接进入"降级为"验证后进入"（§6.4）；用户可覆盖系统判定并随时恢复 |
| ⑤ 看自己 V2 | ✅ | `capabilityLibrary.ts`（六维度四态 + 完整度 + 硬约束）、`categoryQuiz.ts`（确定性种子题 + AI 增补 + 适配度公式）、`ourCapability.ts`、`SelfReadinessPanel`（挂在看自己页顶）——详见下表 |
| ⑥ 断链 #4/#9/#10 | ✅ 全部关闭 | #9 M1 已修（`App.tsx` 写回 `competitorWorkspace` + IDB 白名单 + 快照读取）；#10 术语统一（`看竞对` = 项目内 / `竞品明细` = 工具层，38 处替换 + 守卫测试）；#4 本轮接通（下条） |

**M3⑥ 断链 #4 接通方式（ASIN 自动入对比池）**：看竞对里挑完 3 个竞对后，两处都能一键进对比池——拣选面板的「送进竞品明细做深度对比」与深度分析卡的「送进竞品明细（主图逐张 / A+ / 流量词）」；走的是大盘选品表同一条通路（`selectedCompareAsins` → `CompetitorHub.preselectedAsins` → 对比池 `selected`），并记住来源项目与看，返回按钮精确回到「看竞对」。归一化规则抽成可测的 `normalizeComparisonAsins`（去空 / 大写 / 去重保序 / 截断 ≤5，**没有合法 ASIN 就返回空并由调用方提示，不猜**）。

**M3⑤ 看自己 V2 关键设计**：

| 规则 | 实现 |
| --- | --- |
| 不强迫填完 | 六维度 18 项，每项四态 + 备注；完整度只影响**可信度**，随时能补、空缺不阻塞分析；给出"最拖后腿的维度"作为补齐优先级 |
| 完成度 ≠ 适配度 | 适配度 = 相关题均分（已具备 1 / 部分 0.5 / 不具备 0）× 背景库完整度；**"待确认"不纳入分母**，只降可信度 |
| 出题必须与项目相关 | 种子题由数据确定性生成（交叉清单里未评的需求、差异化供给、合规、毛利红线、库存风险、背景库硬约束、证据缺口），每题带"为什么问"；AI 只能**增补**（结构校验 + 状态白名单 + 按 id 追加，用户答案永不丢失） |
| 每题都要"为什么不" | 每个选项都带 `why`（选它意味着什么、为什么不选其它），选项映射 have/partial/lack/unknown |
| 硬约束否决 | 决策边界类答"不具备" → 红条 + 联动看竞对：赢的路径的最高进入方式是"验证后进入" |

> **M4 衔接（已预埋）**：`buildHardConstraintVerdict` 把三个来源（看自己决策边界自评 / 账号背景库的财务·合规·MOQ / 品类选择题的边界答案）合并去重后写入 `SelfAssessment.hardConstraintVerdict`。M4 的「看机会」机会卡**直接读这个字段**画顶部红条并判定"不可直接进入"，不必重算。

> **当前局限（诚实标注，不是缺陷隐瞒）**：三列对比的 33 个字段里，商品类字段来自项目快照（可直接显示），
> **Listing 全文字段（主图 7 张/视频/五点/A+/参数表/Q&A/变体）与流量字段（流量词/ABA/广告依赖）目前没有生产者**——
> 抓取编排是 §11.2/M5 的事。在那之前这些格子会**如实显示"未抓取"并给出补齐方式**，字段抓取率也会如实偏低；
> 一旦 M5 接上 `listingDetails` / `trafficDetails`，这张表与覆盖率无需改代码即可填满。

**M3⑥ 术语统一（PRD §5.4）**：项目语境统一用 **看竞对**，工具层统一用 **竞品明细**；本次替换 20 个文件 38 处，并新增 `tests/terminology.test.ts`（禁用词扫描 + 正式术语在位 + 编码损坏守卫）。

> **事故记录（值得写下来）**：本次批量改字的第一版用了 PowerShell `Get-Content -Raw` + `Set-Content`，在中文环境下按 GBK 误读，把 12 个文件的中文写成了乱码。发现后立即 `git checkout --` 恢复（工作区本来只有 6 个刻意保留的删除，未受影响），改用 Node 显式 UTF-8 重做，并把这个坑写成了上面那个测试（含"新引入 BOM/替换字符"守卫）。教训：**批量改中文源码必须用显式 UTF-8 读写，改完先 diff 再提交**。

**AI 的边界（本轮落地）**：`describeCompetitorAnalysisForPrompt` 把确定性判定与证据交给 AI，prompt 明确"禁止修改判定结果或编造数字"；AI 新增输出字段 `winningPathExplain`（解释该路径的前置条件与风险），只显示在结论卡里，无法回写判定。

**修掉一个长期偶发失败（真实根因，非环境噪音）**：`tests/optimistic.test.ts` 的「内容相同仅 cloudRevision 不同不视为分叉」偶发失败，根因是**测试夹具非确定性**——`emptyFiveLookProgress()` 每次调用都写 `nowIso()`，两次调用跨毫秒时"两个 fixture 内容完全相同"的前提就不成立（实测：相隔 3ms 得到 `01:54:37.311Z` vs `01:54:37.314Z`）。夹具改为冻结一份进度对象后 **连跑 20 次 0 失败**。产品逻辑未改（生产数据里进度时间戳是真实编辑时间，理应参与内容比较）。

---

### 15.10 M4 看机会 V2 —— 完成记录（2026-09）

推送区间：`phase-0` 分支 `bf09a1b →`（**main 保持 `63abd4c` 未动**）。每步通过 `tsc --noEmit` + 全部 26 个测试套件（243 条断言）+ `vite build`。

| M4 目标项 | 状态 | 落点 |
| --- | --- | --- |
| ① 机会卡模型（§6.5） | ✅ | `src/types/opportunity.ts`：状态机 ai_candidate→confirmed、进入方式、机会类型/可信度、证据引用、推理步骤、决策包（路线图/前置资源/控制点）、零机会结论；`OpportunityCard` 追加同名可选字段（向后兼容） |
| ② 确定性评分换血 | ✅ | `opportunityScoreV2.ts`：五维 25/25/25/15/10；**缺项从分母移除并降覆盖度**；覆盖度独立展示不进 100 分；每维带可复核口径 |
| ③ 决策包推导 | ✅ | `opportunityPlan.ts`：路线图两段式（硬约束优先→打样→测评→认证→备货→Listing→广告）；前置资源对照背景库标已知缺口；控制点三类且一律待确认；`rankOpportunities` 确定性排序 + "由你拍板" |
| ④ AI 生成 + 反证审查 | ✅ | `opportunityAi.ts`：候选卡 0-3 条；需求 id 与证据编号必须真实存在；评分/状态/决策由系统接管；`mergeGeneratedCards` 保证 AI 返回 0 不删卡、已确认的卡一张不动 |
| ⑤ Go/No-Go + 导出 | ✅ | `projectDecision.ts`（纯函数 + 薄 IO）、`opportunityHtmlReport.ts`（转义 + 评分拆解/证据/推理/反证/决策包同屏 + 零机会也能导出） |
| ⑥ 零机会两结论 | ✅ | `opportunityConclusion.ts`：`judged_none`（写明卡在需求已满足/竞争没缝隙/自身接不住/利润不成立）与 `insufficient_evidence`（写明缺什么）；**数据支持机会时返回 null，禁止伪造"无机会"** |
| ⑦ 决策看板 UI | ✅ | `OpportunityDecisionBoard.tsx`（挂在看机会页顶）：Go/No-Go 汇总 + 硬约束红条 + 零机会结论面板 + 机会卡（评分拆解/路线图/资源缺口/控制点可点确认/证据·推理·反证三栏）+ 拍板按钮 + HTML 导出 |
| ⑧ 合并 phase-0 六个能力文件 | ✅ | 见下 |

**修复断链 #6（评分通胀）**：旧 `scoreOpportunity` 用页面完成度当质量（市场分 = `market.completionPercent × 20`、竞对分 = `competitor.completionPercent × 20`、自身分 = `self.completionPercent × 15`），"填得越多分越高"。V2 把输入全部换成可追溯证据与确定性指标，并且**评分与"是否填完"彻底无关**（新测试专门锁这条）。

**phase-0 六个能力文件的合并方式（§15.1-24 决策 A）**：

| 文件 | 处理 | 原因 |
| --- | --- | --- |
| `opportunityAi.ts` | 按 V2 模型重写 | 原实现依赖已不存在的 `SelfGuidingQuestion` / `accountBackgroundSnapshot` / `OpportunityScoreBreakdown`；其中"AI 出引导题"的职责已由 M3 的品类选择题（`categoryQuiz`）接管 |
| `opportunityHtmlReport.ts` | 按 V2 卡片重写 | 证据引用、评分拆解、决策包三块都是 V2 新增，旧渲染器渲染不出来 |
| `projectDecision.ts` | 重写为纯函数 + 薄 IO | 为了让 Go/No-Go 判断可测（AI/IO 不能决定项目级结论） |
| `teamStore.ts` / `TeamSettingsPanel.tsx` / `teamStore.test.ts` | 原样合并（仅适配 `SessionUser` 字段：`nickname` → `username`） | 三者与机会模型无关；`SessionUser` 确实没有昵称字段，按新代码适配而不复活旧类型；测试断言数不变，未放宽任何校验 |

**环境事实（写下来避免下次踩）**：本机没有 `npm`（只有 harness 自带的 `node`），所以 `npm test` / `npm run build` 无法直接执行；所有验证都用等价命令：`node node_modules/typescript/bin/tsc --noEmit`、`node node_modules/tsx/dist/cli.mjs tests/<f>.test.ts`、`node node_modules/vite/bin/vite.js build`。

**批量改中文的教训（M3 事故的延伸）**：合并 phase-0 文件时必须用 Node 显式 UTF-8 读写（`fs.readFileSync(p,'utf8')` / `writeFileSync(p, text, 'utf8')`），禁止 PowerShell `Get-Content`/`Set-Content`——中文环境下会按 GBK 误读并写坏文件。`tests/terminology.test.ts` 里的"新引入 BOM / 替换字符"守卫就是为这件事加的。

---

### 15.11 M5 数据池与管理员后台 —— 进度记录（2026-09，进行中）

推送区间：`phase-0` 分支 `13f7e96 → 1e16696`（**main 保持 `63abd4c` 未动**）。每步通过 `tsc --noEmit` + 全部 32 个测试套件（295 条断言）+ `vite build`。

| M5 目标项 | 状态 | 落点 |
| --- | --- | --- |
| ① 数据池网关 | ✅ | `api/data/[action].ts`：一次调用固定四步（服务端解析密钥 → 查缓存 → 校验配额 → 调 MCP + 写缓存 + 记用量）；action = mcp / status / usage |
| ①b Listing 全字段抓取编排 | ✅ | `listingFetchPlan.ts`（计划器：33 字段 → 有序步骤、§15.4 采样口径 3/10 次、跳过已有字段、覆盖率评估）+ `listingFetchExecutor.ts`（执行器：走 `/api/data/mcp`、并发限 3、失败隔离）+ 看竞对「抓取全字段」按钮（抓完写回 `listingDetails`/`trafficDetails`，三列对比的"未抓取"格子被真实数据填上） |
| ② 缓存与配额 | ✅ | `src/utils/poolCache.ts`（键归一化、TTL 分档、命中决策、淘汰、命中率）+ `usageAccounting.checkQuota`（只计实际计费事件，缓存命中不占额度） |
| ③ 用量记账 | ✅ | `src/utils/usageAccounting.ts`（成本表、按工具/用户/项目/供应商汇总、按天趋势、失败率、缓存省下的钱）+ 迁移 `008_usage_events.sql`（含 `audit_events`，RLS 全拒、只允许 service_role） |
| ④ 管理员后台接口 | ✅ | `api/admin/[action].ts`：health / config / usage / users / audit / projects；管理员校验 + 全部写操作落审计；无 service_role 时优雅降级 |
| ⑤ 管理员后台面板 | ✅ | `src/components/AdminConsolePanel.tsx`（挂在「设置 → 管理员后台」，仅管理员可见）：环境自检、用量统计、用户管理、配置中心、审计日志、项目与报告 |
| ⑥ 安全加固 | ✅（主链路） | 见下方"发现并修掉的安全缺陷"；`tests/securityKeys.test.ts` 把口径变成会失败的测试；**出网路由收敛** `dataPoolRouting.ts`（自定义地址 > 服务端网关 > 浏览器直连）+ `dataPoolClient.ts`：登录用户在浏览器里**已无密钥可用、也不需要**，网关失败**不回退**到浏览器直连 |
| ⑥b 审计事件归一化 | ✅ | `auditEvents.ts`：动作白名单 + 中文标签、未知动作**留痕不丢弃**、缺操作人记 anonymous、明细走 `redactSecrets`（审计日志不能变成泄露渠道）、按动作/操作人聚合 |
| ⑦ 导出补 MD | ✅ | `buildOpportunityMarkdownReport` + 决策看板「导出 MD」按钮（HTML 已有；**不做 PPT**，用户已确认） |
| ⑧ 性能（项目中心 ≤2s） | 🔶 可测化已完成，数值待用户实测 | `perfBudget.ts`：预算表（每条写明 PRD 出处）+ 边界判定（等于预算算达标）+ **中位数口径**汇总；ProjectCenter 加载路径已打点（超预算 console 告警）；管理员后台「环境自检 → 性能基线」直接显示实测中位数/预算/超出百分比。我这边无浏览器，数字必须由用户跑几次后读出 |
| ⑨ ≥30 项目回放 / NPS 基线 | ⏳ 用户侧 | 属"用户逐轮自测 + 内部试用"阶段 |

**发现并修掉一个真实安全缺陷（这正是 §11.2 要根治的事）**：旧 `/api/settings?action=get` 把 deepseek/sellersprite 等密钥**明文返回给浏览器**，前端 `serverKeys.ts` 还写进 localStorage，MCP 直接在浏览器里带密钥调用——任何能打开浏览器的人都能取走密钥，一次 XSS 即全量泄露。改造：
1. `api/settings` 只回「是否已配置 + 指纹」（`describeKeys`）；源码级守卫禁止 `keys: { …: env(...) }` 这类回显；
2. 新增 `src/utils/keyMasking.ts` 作为**唯一**掩码实现（api 层禁止自己再写一份），并附 `redactSecrets` 供日志兜底；
3. `serverKeys.ts` 重写：不再保存明文，只缓存状态；`getDefaultServerKey()` 恒返回空串；新增 `migrateLegacyKeys()` 把浏览器里残留的明文一次性推到服务端并**删除本地副本**（推送失败则保留并如实告知，不悄悄丢密钥）；
4. App 登录后流程改为"先迁移旧明文 → 再读服务端状态"。

**环境坑（已修，2026-09）**：本地开着 `npm run dev` 时编辑 `api/**` 路由，Vite 的 watcher 会去 watch
devApiPlugin 生成的 `.[action].ts.<pid>.<uuid>.tmpdir/*.tmp`，Windows 上抛 `EBUSY: resource busy or locked`
并**直接让 dev server 退出**（实测 exit code 1）。修法：`vite.config.ts` 的 `server.watch.ignored` 排除
`**/*.tmpdir/**`、`**/*.tmp`、node_modules、dist、.git。

**环境事实（本地实测要点）**：`server/devApiPlugin.ts` 会把 `api/**`（含 `[action].ts`）映射成本地中间件，并把 `.env.local` 里的变量**只注入 dev server 进程**（不暴露给浏览器）——所以 `/api/data/status`、`/api/admin/health` 在 `npm run dev` 下可直接访问与实测。本机没有 `npm`（只有 harness 自带 node），验证统一用等价命令。


**用户决策 A（2026-09，已落地）**：平台数据池**只对登录用户开放**，游客只能用示例数据。
因此浏览器侧的密钥与直连 MCP 被**整段删除**（`sellerspriteApi.ts` 1309 → 966 行）：
`mcpHttp` / `parseMcpHttpBody` / `resolveSellerSpriteAuth` / `resolveLingXingAuth` / `callLingXingToolBrowser` /
`testMcpEndpoint` / `testMcpEndpointWithAuthHeaders` 全部移除；出网路由只剩 `gateway`（登录）与 `blocked`（未登录）两种结论，
"自定义 MCP 地址"与"浏览器兜底"两条旁路一并删掉（它们都要求浏览器持有密钥）。
设置页「MCP 数据」不再提供密钥输入框，改为提示"密钥由平台在服务端管理"，并给一个清除本机残留旧密钥的按钮。
`tests/securityKeys.test.ts` 增至 9 条源码级守卫（含"不得再出现密钥请求头构造""设置页不得再有密钥输入框"）。
**尚未做（诚实列出）**：
- ~~持久化缓存（Supabase 表）~~ → **已完成**：`009_pool_cache.sql` + `poolCacheStore.ts`，网关改为双后端（有 service_role 走表、没有退回进程内），`/api/data/status` 回报 backend/新鲜/过期/命中/命中率，并支持 `prune=true` 清理；
- 客户端 MCP 调用路径尚未全部切到 `/api/data/*`：本轮先把"密钥不再下发"做死，浏览器侧仍有走 `/api-proxy/*` 的旧路径，下一轮统一走网关；
- 性能基线与 30 次回放属用户侧实测。

---

## 16. 简洁性裁剪（本轮审查：消灭"看不懂、用不上"，一切服务快速找到未满足需求）
**总原则（§4 原则 10）**：每个看主屏只放「一句人话结论 + 一个主操作 + 证据数量徽章」，其余全部进二级页；高级/低频功能**默认收起，不删除**；看不懂的东西要么删、要么补一句人话解释。

**主屏最小模板（五个看都遵守）**：

```
┌─ 看市场 ──────────────────────────────────────────────┐
│ 结论（人话，≤20 字）：侧睡细分值得做，先验证"可调高度"。│
│ [AI 起草 · 请确认]     证据 12 条                     │
│ [主操作：确认 → 看竞对]  [次要：重新分析]              │
│ ▾ 查看详情（细分评分 / 完整大盘 / 证据链 → 进二级页）   │
└───────────────────────────────────────────────────────┘
```

**裁剪清单（默认收起 / 简化，不删除）**：

| 项 | 处置 | 理由 |
| --- | --- | --- |
| 利润计算器 | 移到首页侧栏（项目中心下），原样照搬 | 独立工具，不进五看流程，减少项目内噪音 |
| 机会扫描器 | 取消独立页，保留在大盘全市场页内 | 本质是大盘的一个图表，独立成页是过度设计 |
| 市场大盘（全市场） | 照搬原样，不改 | 用户熟悉 |
| 登录页 | 照搬原样，不改 | 无需改 |
| 市场准入评分卡（8 维度） | 保留在大盘内（原样，默认收起），不在看市场主屏强推 | 复杂且默认关闭；主屏用确定性细分机会分即可 |
| 锚点批注 | 默认收起（悬浮小按钮），不常驻显示 | 力量功能，常驻增加视觉噪音 |
| PageQuickNav 18 段导航 | 仅在大盘超长页出现 | 只在需要时出现 |
| 完成度% / 进度环 | 降为次要信息（灰色小字），不作主视觉 | 完成度 ≠ 决策质量 |
| 术语 | 只保留一套（§5.4） | 消除"竞品分析/对比/看竞对"混用 |
| AI 产出 | 每条必须带一句人话解释 + 证据可追溯 | 没有解释的指标不显示 |

---

### 15.12 M6 商用收口 —— 进度记录（2026-09，进行中）

推送区间：`phase-0` 分支 `38856a1 → 34ba8a5`（**main 保持 `63abd4c` 未动**）。每步通过 `tsc --noEmit` + 全部 33 个测试套件（304 条断言）+ `vite build`。

| M6 目标项 | 状态 | 落点 |
| --- | --- | --- |
| ① 团队空间与权限收口 | ✅ | `TeamSettingsPanel` 挂进「设置 → 团队空间」（**仅管理员可见**）；`teamRoleMapping.ts` 把团队角色（owner/admin/member/viewer）与项目角色（owner/editor/viewer）的映射写死并测试锁定：**只允许等价或降级**（团队 admin → 项目 editor，绝不升为 owner，避免"团队管理员接管别人项目"）；owner 永不可被改/移除；邀请列表不含 owner；面板顶部显示角色口径说明 |
| ② 上线检查清单 | ✅ | `docs/M6-launch-checklist.md`：8 大节逐项可勾选，每项写明**怎么验**与预期，并区分 [自动]/[人工]/[用户侧] |
| ③ 操作手册 | ✅ | `docs/M6-user-manual.md`：10 分钟五看流程、每步产物、常见卡点处理表、三条使用纪律 |
| ④ 定价/订阅实验设计 | ✅ | `docs/M6-pricing-experiment.md`：计量口径直接复用 `usageAccounting`（workspace × 工具 × 次数 + token），三档套餐 + 三个实验（配额感知/配额上限/决策包付费点）+ 何时接支付的前置条件；**本轮不接支付** |
| ⑤ PRD 记录 | ✅ | 本节 + §15.11 的决策 A 记录 |
| ⑥ 用户侧待办 | 📌 已写明做法 | 3 份真实品类决策包交管理层评审、10 分钟计时可用性测试、性能基线读数——做法写在检查清单 §6/§8 与操作手册里 |

**M6 的诚实边界**：本轮完成的都是"可交付物 + 权限收口"，**没有任何一项替代用户侧验证**。
§12 M6 的验收（3 份真实决策包、10 分钟计时测试）只能在真实使用中产生，我无法代做。

---

### 15.13 UI 与线框图一致性修复轮 —— 记录（2026-09）

**触发**：用户对照 `docs/ui-wireframes.html` 发现实现与流程图有多处不一致（"很多不一样的地方"），并要求逐项修复；期间线上 phase-0 预览出现 `React #310` 白屏。
**核对口径**：抽出线框图区块文本 + 代码 grep 双证，逐屏列表存于 `docs/ui-conformance-audit.md`（8 屏主流程 + 23 屏二级页/明细层，标 ✅ 一致 / 🔧 已修 / ⏳ 待修）。

推送区间：`phase-0` 分支 `9281c3f → b7f2c3f`（**main 保持 `63abd4c` 未动**）。

| 项 | 状态 | 落点 |
| --- | --- | --- |
| ① 看用户 ④ 细分标准 | ✅ 已推 | `SegmentStandard`（勾选需求域的有序标准 + 主标准理由），看市场"方案 A 按需求域切"优先采用 |
| ② 看市场「查看完整大盘 →」 | ✅ 已推 | 每个细分卡与方案卡各一个入口；大盘顶部显示"当前：X（来自看市场）"+「切到全市场」 |
| ③ 看竞对 L3 下钻 ⑦⑧⑨⑩ | 🔧 两轮 | 第一轮已带入 ASIN；第二轮（⏳4）精确落到视图：`competitorDrilldown.ts` 单一来源 + `CompetitorHub.preselectedResultTab`/`resultTabRequestKey`，**同一次请求只应用一次**，⑨ 直达流量视图 |
| ④ 看机会「全局先后建议」 | ✅ 已推 | 独立区块：确定性排序（评分 → 覆盖度 → 前置资源缺口）+ 排序理由 + 已拍板标记，明写"先做哪个由你拍板" |
| ⑤ 决策草稿全程常驻（⏳1） | ✅ 已推 | 线框图 Screen 2 标为"核心 IA 变化"：抽出 `DecisionDraftRail`，由 `ProjectWorkspace` 在 `xl:sticky` 右栏常驻（六个 Tab 都在）；向导内重复列删除，保存后 `bumpDraft()` 刷新右栏 |
| ⑥ 线上白屏 `React #310` | ✅ 已推 | 根因：`UserLookView` 的 `useState` 落在 `if (!data) return <加载中/>` **之后**（条件 hook）。已移回 hook 区并加注释；新增 `tests/hookOrder.test.ts`（AST 级）守死"hook 不得出现在任何 early return 之后" |
| ⑦ 导出报告 §1-§7 固定叙事（⏳2） | ✅ 已推 | HTML/MD 改为「首页只放结论 → §1 研究目标 → §2 用户需求 → §3 市场细分 → §4 竞品缺口 → §5 自身适配 → §6 机会结论 → §7 证据索引」；两份导出共用同一份 block 文档模型（标题/顺序不可能漂移）；缺数据一律"未提供"、listing 缺字段"未抓取"；取数口径与看竞对界面完全一致（同一对比池与确定性算法），无竞对列时**不判赢面** |
| ⑧ 二级页 ①②③④⑪⑫⑬⑭ 独立页（⏳3） | ✅ 已推 | `l3Pages.ts`（8 页契约表，四句抄自线框图 note）+ 唯一页壳 `L3Sheet`（全屏、返回/Esc、dialog 语义、滚动锁、契约条常驻）；**内联与二级页共用同一份实现**（`variant='inline'` / `'sheet'`，状态留在拥有数据的视图里，经 `onRegisterL3Bodies` 注册正文，避免复制 markup 与业务逻辑）。残留三处已收口（详见核对表 ⏳3 残留）：① 层筛选已补（状态由 `UserLookView` 持有，内联与二级页共用）；⑬ 点卡片标题已能进详情；⑭ 控制点阈值已可在二级页⑭ 改（`utils/controlPointEditing.ts`，改阈值即作废「已确认」，重算不覆盖人工值）。唯一"故意不做"的是线框图 ① 的**拖动改层**：层间保留比例是量出来的确定性数字，不接受手势改写 |
| ⑨ 管理员身份恒为 false（本轮额外发现） | ✅ 已推 | 真实故障：`AiSettingsPanel` 写 `isAdminSession(undefined)` → `Boolean(user) && …` 恒 false，**两个管理员 Tab 对所有人都不显示**。已改为 `getCurrentUser()`；并把管理员身份收回**服务端权威**（`ADMIN_EMAILS` 白名单，随 login/register/新增的 `/api/auth/me` 下发 `isAdmin`），前端启动时 `refreshAdminFlag()` 重新确认；`tests/adminGate.test.ts` 7 条断言守死。开通步骤见 `docs/M6-launch-checklist.md` §3.0 |

**本轮新增的确定性护栏**（都不依赖 AI 判断）：
1. `tests/hookOrder.test.ts` —— AST 扫描所有 `.tsx`，禁止 hook 出现在 early return 之后（防 #310 复发）；
2. `tests/competitorDrilldown.test.ts` —— 下钻入口共享表 + 源码级守卫（按钮文案只能来自一处）；
3. `tests/adminGate.test.ts` —— 全仓库禁止再出现"恒 false 的管理员判定"（`isAdminSession(undefined)`，注释豁免），并锁死"服务端权威 + 接口层 403"两条口径；
4. `tests/l3Pages.test.ts` —— 8 个二级页的注册表/入口/页壳语义，以及"内联与二级页必须共用同一份实现"；
5. `vite.config.ts` 的 `server.watch.ignored` —— 修掉编辑 `api/**` 时 dev server 的 `EBUSY` 崩溃（`[action].ts.<pid>.<uuid>.tmpdir` 被 watch 到）。

**诚实边界**：本轮全部是"实现对齐线框图"，**没有**替代用户侧验证；W3–W4 前的 3 份真实品类决策包、10 分钟计时可用性测试、4 视口视觉回归基线仍待用户侧完成（见 `docs/M6-launch-checklist.md` §6/§8）。

---

### 15.14 一致性收口 —— 完成记录（2026-09）

推送区间：`phase-0` 分支 `960bdd9 → 8e286f2`（**main 保持 `63abd4c` 未动**）。
验证：`tsc --noEmit` exit 0；`node tests/runAll.mjs` → **39 套件 / 372 断言 passed / 0 failed**；`vite build` exit 0。

§15.13 留下的三条残留（核对表 ⏳3 残留）本轮全部收口：

| 残留项 | 状态 | 落点 |
| --- | --- | --- |
| ① 二级页缺「层筛选」 | ✅ | `SearchPathDetailBlock` 新增「全部 / 认知层 / 考虑层 / 决策层 / 场景层」；**筛选状态由 `UserLookView` 持有**（放在 `if (!data)` 早退之前），内联区块与二级页① 共用同一份状态 → 从二级页返回主屏筛选仍保留；只筛行、不重算（占比/月搜索量/本层合计照旧）；空层显示「该层暂无关键词证据」 |
| ① 「拖动改层」 | ❌ **故意不做** | 层间顺序与比例是 `computeSearchPathFlow` **量出来的确定性数字**。允许手势改写会把"漏斗"结论变成展示内容，与"数字必须确定性可追溯"冲突；页面上明写「顺序是量出来的，不是偏好设置」，不假装可拖。这是全项目**唯一**一条"故意不做"的线框图项 |
| ⑬ 点卡片标题不跳转 | ✅ | 卡片标题本身可点开二级页⑬（右下「查看详情」保留） |
| ⑭ 控制点阈值不可改 | ✅ | 新增 `src/utils/controlPointEditing.ts`：`editControlPointThreshold` / `resetControlPointThreshold` / `canEditThreshold` / `countEditedThresholds` / `controlPointIdentity` / `mergeControlPointsPreservingEdits`。两条硬规则：**阈值一改，「已确认」立即作废**（用户拍的是那个数字）；**人工改过的阈值不被重算静默覆盖**（按 id/身份对齐保留，推导里已消失的人工点也保留）。`buildDecisionPackage` 增 `previousControlPoints`、`generateOpportunityCandidates` 增 `existingCards`，两条路径（AI 给的决策包 / 确定性补全）都做保留合并 |

**本轮新增的工程化护栏**：
6. `tests/controlPointEditing.test.ts`（16 条）—— 阈值编辑/恢复默认/拒绝空输入/身份稳定性/重算不覆盖人工值的合并规则；
7. `tests/runAll.mjs` + `npm test` 指向它 —— 一条命令跑完全部套件，并**揪出"没打印 result 行的套件"**（防止"没跑起来却被当成通过"的假绿）；
8. `docs/M6-ui-acceptance-checklist.md` —— 给验收者（非实现者）的逐条清单：点哪里 → 该看到什么，含主流程 8 屏、二级页抽查、PRD 商用口径（权重 25/25/25/15/10、未选目标细分 ×0.6、覆盖度不入 100 分、AI 只解释不改分）、负面用例（零机会/无竞对/硬约束/游客/非管理员 403），以及**三项"看起来像 bug 其实不是"**的误判清单；已挂进操作手册与上线检查清单（检查项 6.5）。

**至此 Kairo 与 PRD、线框图的一致性已无待修项**；剩下的全部是用户侧验证与上线拍板（见 §16 与 `docs/M6-launch-checklist.md` §8）。

---

### 15.15 AI 请求地址配置改造 —— 完成记录（2026-09）

**用户诉求（原话）**：「API 请求地址请帮我默认为对应大模型的地址，然后我也可以选择改，另外增加一个自定义配置，方便我增加第三方中转api。」

**改造前的事实（先说清楚问题是什么）**：
1. 设置里那个「API 请求地址」框，语义是"自定义地址，**留空 = 走本站默认代理**"，而默认代理是**同源路径** `/api-proxy/<id>`，由 `vercel.json`（线上）与 `vite.config.ts`（本地）转发到真站点 —— 这是为了绕开浏览器 CORS，不是多余设计；
2. 8 个内置供应商里，**7 个的转发目标就是官方站点**，**只有 DeepSeek 被指向第三方中转 `https://openrouter.fans`**（该值在 `vercel.json`、`vite.config.ts`、设置页 placeholder、`aiConfig` 的中转补全逻辑里四处一致，属**既有有意设定，不是脏数据**）；
3. 界面上**完全看不出**上面这两件事：用户不知道自己的请求实际打到哪里，也不知道 DeepSeek 走的是中转；
4. 填绝对地址时是**浏览器直连**，只有对方开了 CORS 才通 —— 这就是"填了第三方中转却用不了"的根因。

**本轮改了什么**：

| # | 改动 | 落点 |
| --- | --- | --- |
| ① | **地址事实表**（唯一来源）：每个供应商记 `officialUrl`（官方）与 `proxyTarget`（本站默认实际转发到的地址），并标记"默认走中转"的供应商 | `src/utils/aiEndpoints.ts` |
| ② | **说真话的界面**：设置页同屏显示「官方地址 / 本站默认转发到 / 本次实际请求 + 走哪条通道」；DeepSeek 额外给显著提示「不是官方地址」+ 一键「改用官方地址」；**不把绝对地址预填进输入框**（否则会静默把所有请求变成跨域、打挂现有调用） | `AiSettingsPanel.tsx` |
| ③ | **新增「自定义 / 第三方中转」供应商**：无默认地址与默认模型（必须自填），填 Base URL（自动补全到 `/chat/completions`）+ 模型名 + Key，按 OpenAI 兼容发送 | `aiConfig.ts`（`AiProvider` 增 `custom`） |
| ④ | **服务端转发**：绝对地址改由 `POST /api/ai/chat` 代发（浏览器直连会被 CORS 拦）；文本与视觉两条路径共用同一判定，**未填地址时行为与改造前完全一致（零回归）**；流式在 relay 通道下**降级为一次性返回**（不静默失败） | `api/ai/[action].ts`、`aiConfig.ts` 的 `sendOpenAICompatOne`/`streamText` |
| ⑤ | **SSRF 防护**（转发即"服务器替你请求任意地址"，必须在服务端做）：只允许 http/https；拒绝带账号密码；拒绝 `localhost`/`*.local`/`*.internal`/无点号内网短名；拒绝私有、环回、链路本地、运营商级 NAT、组播与保留段（含 `169.254.169.254` 云元数据），IPv6 覆盖 `::1`/`fc00::/7`/`fe80::/10`；**必须登录**（不能变成开放代理）；请求体 ≤1MB、响应 ≤4MB、上游超时 120s；错误信息一律过 `redactText` 脱敏、只回内容不回上游原文 | 同上 + `src/utils/keyMasking.ts` 新增 `redactText` |
| ⑥ | **上游返回的"人话"错误**：把首页当 API 地址（返回 HTML）、上游 401/429、非 OpenAI 兼容格式、空返回与非 JSON，各自给出能照着改的中文提示 | `parseRelayUpstream`（纯函数，可单测） |

**Key 的数据流向（如实告知用户，并写在界面上）**：自定义中转走服务端转发时，用户的 Key 会经过本站服务端一次，**不落库、不写缓存、不写日志、不进用量记账**；同时保留"直连"选择（中转自身允许跨域时不经服务端）。内置 7 个供应商的默认链路不变，Key 仍只在浏览器与官方站点之间。

**验证**：
- `tests/aiEndpoints.test.ts` 20 条断言：事实表完整性、deepseek 必须标为中转且与 `vercel.json`/`vite.config.ts` 一致、通道判定矩阵（proxy/relay/direct）、**24 个危险地址全部被拒** + 4 个合法中转地址放行、上游返回解析 5 类情形、源码守卫（服务端必须复用同一份校验、设置页不得预填绝对地址、运行时两条 AI 路径都经过判定、流式必须降级）；
- 全量：`node tests/runAll.mjs` → **40 套件 / 395 断言 / 0 failed**；`tsc --noEmit` 0；`vite build` 0；
- 对**运行中的服务**做过端到端验证（dev server + 真实 HTTP）：无 token → 401；8 个危险地址 → 400 且带明确中文原因；缺 Key/缺模型/非 OpenAI 形态 → 400；合法外站地址 → 真的尝试转发且**错误信息里没有 Key**。

**诚实边界（必须知道）**：
1. `validateRelayTarget` 只做**字面量**校验，**不做 DNS 解析后的二次复核** —— 理论上存在"域名解析到内网 IP"的绕过。当前是内部自用 + 必须登录，风险可接受；**对外多租户前必须在服务端解析后再复核一次**。
2. 服务端转发目前**只支持 OpenAI 兼容**形态（`/chat/completions`）；Claude/Gemini 原生形态的中转需要对方提供 OpenAI 兼容入口（界面已写明）。
3. 流式输出在转发通道下不可用（降级为一次性返回）。
4. DeepSeek 的默认中转**未被本轮改动**（避免悄悄打断用户现有可用链路）；本轮把这件事变成"可见 + 一键可改"。

---

### 15.16 「获取模型」—— 填完 Key 点一下列出可用模型（2026-09）

**用户诉求**：「当我填完key,我希望可以点击获取模型，然后我可以选择模型，类似于 CC SWITCH 中的设置。」

**要解决的真问题**：模型名是用户最容易填错的东西（各家命名不同、中转站还常改）。但"列出模型"这个接口和聊天接口**不是一套规矩**，且**浏览器直连同样会被 CORS 拦**：

| 供应商 | 取模型列表的路径 | Key 放哪 |
| --- | --- | --- |
| OpenAI / DeepSeek / Qwen / Moonshot / 豆包 / 自定义中转 | `GET {base}/v1/models`（豆包 `/api/v3/models`） | `Authorization: Bearer` |
| 智谱 | `GET {base}/api/paas/v4/models` | `Authorization: Bearer` |
| Claude | `GET {base}/v1/models` | `x-api-key` + `anthropic-version` |
| Gemini | `GET {base}/v1beta/models` | **query `?key=`** |

**实现**：
- 新增 `src/utils/aiModels.ts`（纯逻辑，可测）：`planModelsRequest`（算出这次请求长什么样：路径 / 鉴权方式 / 通道 / 一句人话说明）、`modelsAuthHeaders`、`modelsUrlWithKey`、`parseModelsResponse`（**前端与服务端共用同一份解析**）。
- `api/ai/[action].ts` 新增 `models` action：与聊天转发同一套底线（必须登录、目标过 `validateRelayTarget`、只做 GET、Key 不保存不记录、错误脱敏、响应上限、30s 超时）。
- `aiConfig.fetchAvailableModels()`：**复用与聊天相同的通道判定** —— 内置供应商走同源代理；填了绝对地址则经 `/api/ai/models` 由服务端代取。
- 设置页：模型选择框下方新增「**获取模型**」按钮（未填 Key 时禁用，并说明原因）；拉回来的模型渲染成标签，**点一个即选中**并自动写入「已获取 / 自定义模型」下拉，另有「全部加入可选列表」；失败时给出能照着改的中文，并明确告知"手填模型名照样能用"。

**解析兼容四种返回形状**：OpenAI 兼容 `{data:[{id}]}`、Gemini `{models:[{name:'models/...'}]}`（自动去掉 `models/` 前缀）、纯数组、中转站常见包装 `{data:{models:[...]}}`；结果去重并排序。

**验证**：`tests/aiEndpoints.test.ts` 扩到 **27 条断言**（新增 7 条：逐家路径与鉴权方式、Key 落位、自定义地址下判定为 relay、自定义供应商空地址时明确提示、四种返回形状、去重排序与五类失败提示、源码守卫）；全量 `node tests/runAll.mjs` → **40 套件 / 402 断言 / 0 failed**；`tsc` 0；`vite build` 0。并对**运行中的服务**端到端验证 `/api/ai/models`：未登录 401；`127.0.0.1` / `169.254.169.254` / `10.1.2.3` / `[::1]` / 内网短名 `nas` 全部 400 且带明确原因；合法地址真的去取、失败信息里没有 Key。

**诚实边界**：中转站的模型列表接口不保证存在（很多自建中转不实现 `/models`）——所以"拉不到"是正常情况，界面必须允许手填且不卡死；这两点已写进界面文案与验收清单。

---

### 15.17 AI 配置简化 + DeepSeek 默认改回官方（2026-09）

**用户反馈（原话）**：「api设置的太复杂，我需要简单，傻瓜式操作，另外deepseek的默认路径不要是 https://openrouter.fans ，要改成deepseek官方，这些信息可以不用展示出来，信息爆炸了，需要简单，傻瓜式操作，选模型→填key→获取模型，选中模型。」

**三处改动**：

| # | 改动 | 落点 |
| --- | --- | --- |
| ① | **DeepSeek 默认改回官方**：`vercel.json` 的 `/api-proxy/deepseek` 转发目标 `openrouter.fans` → `api.deepseek.com`；`vite.config.ts` 的 dev 代理同步改；事实表 `proxyTarget` 也改为官方（`relayedByDefault` 标记随之取消，现在 8 个内置供应商**全部**等于官方地址） | `vercel.json`、`vite.config.ts`、`aiEndpoints.ts` |
| ② | **模型名修正**：DeepSeek 的 `defaultModel` 由 `deepseek-chat` 改为 **`deepseek-flash`**，可选列表改为 `deepseek-flash` / `deepseek-v4-pro`。依据是官方报错原文「The supported API model names are deepseek-flash, deepseek-v4-pro, but you passed deepseek-V4-flash」（用户实测；旧列表里的 `deepseek-chat`/`deepseek-reasoner` 已不被接受，会直接 400） | `aiConfig.ts` |
| ③ | **设置页砍成四步**：① 选 AI 供应商 → ② 填 API Key → ③ 获取模型（点一个即选中）→ 当前使用下拉。Key 输入框**移到模型之前**（否则"获取模型"永远处于禁用态，用户会以为坏了）。原先占满屏的「官方地址 / 本站默认转发到 / 本次实际请求」三行事实、长段说明、恢复默认说明、直连开关、手动加模型，**全部收进「高级设置」折叠**（`<details>`），默认不展开 | `AiSettingsPanel.tsx` |

**没有丢掉的诚实性**（只是不再喊）：
- 「默认地址不是官方」的提示机制**保留**，但只在真的出现偏差时才显示（现在不会出现）；
- Key 的数据流向说明保留在高级设置里；
- 「浏览器直连」开关保留（用户截图中 `api.deepseek.com` 实测可跨域直连，这类用户可以选不经服务端）；
- 「拉不到模型列表」时明确告知可手填，不卡死。

**验证**：`tests/aiEndpoints.test.ts` 扩到 **32 条断言**。新增/改写 4 条关键守卫：① 所有内置供应商的默认目标必须等于官方地址、且**任何供应商都不得再出现 openrouter**；② `vercel.json` 与 `vite.config.ts` 必须与事实表一致（防"改了代码没改转发"）；③ DeepSeek 默认模型必须是官方实际支持的名字、`deepseek-chat`/`deepseek-reasoner` 不得回归；④ 设置页必须是三步顺序（含"Key 必须在获取模型之前"）+ 高级设置必须折叠 + 旧冗长文案不得回归。
全量：`node tests/runAll.mjs` → **40 套件 / 407 断言 / 0 failed**；`tsc` 0；`vite build` 0。

**⚠️ 需要用户注意的一件现实问题**：如果原来的 DeepSeek Key 是**那个中转站发的**（openrouter.fans），它换成官方地址后**一定用不了**，需要去 `platform.deepseek.com` 拿官方 Key。另外用户截图里上游返回 `Your api key: ****.com is invalid` —— Key 以 `.com` 结尾，说明 Key 框里存的可能是地址而不是 Key，需要核对。

---

### 15.18 侧栏恢复老版模块 —— 与线框图冲突的用户指示（2026-09）

**用户指示（原话）**：「帮我把原来这几个板块的东西，暂时先恢复，放到项目中心下面去，按这个顺序」（附图为老版侧栏：市场大盘 / 竞品分析 / 用户洞察 / 关键词分析 / 利润计算器）。

**这是一个明确推翻线框图的决定，必须留痕**：
- 线框图第 1 屏（`docs/ui-wireframes.html`）与 `docs/product-charter.md` 的原则是"**关键词 / VOC / 竞对相关模块降为明细层，不入顶级导航**"，侧栏只保留 项目中心 / 利润计算器 / 市场大盘·全市场 / 设置；
- 用户本轮明确要求把这些模块放回侧栏、挂在「项目中心」下面，并指定顺序。

**执行结果**：按用户指令执行，并把它标成"**用户指示的临时回退**"（核对表新增 ⚠️ 标记），而不是偷偷改掉线框图。

| 项 | 内容 |
| --- | --- |
| 现侧栏结构 | 项目中心 → （缩进子分组）市场大盘 → 竞品明细 → 用户洞察 → 关键词分析 → 利润计算器；底部仍为 保存数据/历史 · 重新上传数据 · 设置 · 头像 |
| 术语处理 | 用户口中的"竞品分析"**未采用**：项目有术语守卫测试（`tests/terminology.test.ts`）明令禁止该词（PRD 断链 #10 的原样复现），工具层正式名为「**竞品明细**」，与页面标题一致。需要对用户说明；若坚持改词，须同时改术语标准与 PRD |
| 结构守卫 | 新增 `tests/sidebarNav.test.ts`（7 条断言）：顺序固定、五个按钮都必须真的切视图、必须位于项目中心的缩进子分组内、偏差说明必须留在源码注释里、禁用词不得回流、底部动作不得被顶掉 |
| 冲突留痕 | 源码注释、`docs/ui-conformance-audit.md` 第 1 屏行（⚠️）、本节 |
| 待用户确认 | 这是一次"临时恢复"还是长期形态？若长期保留，需要回头修订线框图与 §5.1 的导航原则；若只是临时，请在我做完其他事项后告知恢复时机 |

**验证**：`node tests/runAll.mjs` → **41 套件 / 414 断言 / 0 failed**；`tsc` 0；`vite build` 0。

---

### 15.19 背景信息扩充 + 「看自己」砍成 3 个拍板问题 —— 用户指示的偏离（2026-09）

**用户原话（两段，2026-09）**：

1. 「设置里面的背景信息，是不是还不太够，不足以支持你做看自己的信息输入？可以继续加，但要有逻辑和依据，最好多一些选择题（下拉选项），填空题控制在3个。」
2. 「看自己，为啥还要这么多东西要确认，很奇怪，有了背景信息，再针对性回答3个问题就行了吗，不要问这么多了。」（附图为 42 项四态确认：目标 5 / 能力 10 / 资源 9 / 约束 8 / 经验 5 / 决策边界 5）

**改动前的事实（先说清问题）**：

| 层 | 改动前 | 问题 |
| --- | --- | --- |
| 设置 → 背景信息 | **11 个自由文本框**（称呼/角色/公司/品牌/品类/站点/经验/目标/约束/表达风格/其他） | 全是填空；没有"这一项影响哪个判断"的依据；字段不可被程序读取，除了喂给 AI 之外什么也做不了 |
| 看自己 · 结构化自评 | **42 项四态**（已具备/部分具备/不具备/待确认，逐项点 + 备注） | 用户要点 42 次；其中"目标/能力/资源/经验"里的信息背景信息本来就能给 |
| 看自己 · 账号背景库 | **20 项四态**（六维度，逐项点） | 与上面 42 项高度重叠，等于同一件事问两遍 |
| 看自己 · 品类选择题 | **5-8 道**项目内动态题（种子题 + AI 增补） | 需求×能力类问题问了 4 道，答完才知道"我们行不行" |

三张卷子加起来 62+ 次点击，且大多数问题的答案在"背景信息"里已经存在。

---

#### A. 设置 → 背景信息：改成"成组 + 有依据 + 选择为主"

**7 组 29 个字段**，每一组都写清"它影响五看里的哪一个判断"（这一句直接显示在 UI 上）：

| 组 | 影响哪个判断（UI 原文） | 字段（全部为下拉/多选，除标注外） |
| --- | --- | --- |
| 主体与规模 | 影响「看自己 → 能不能交付」与「赢的路径」里的成本判断 | 卖家类型 / 团队规模 / 年营收量级 / 主战场站点（多选）/ **品牌名（自由文本）** |
| 资金与风险 | 影响「看机会 → 先做什么」与机会卡的前置资金缺口 | 单项目可投入预算 / 可接受最长验证周期 / 风险偏好 / 账期能力 |
| 供应链与产品 | 影响「看自己 → 能否交付」与「人优我廉」的成本判断 | 供应链类型 / 是否自有模具 / 可接受 MOQ / 典型交期 / 质量管控方式 |
| 合规与资质 | 影响硬约束：认证缺失 ⇒ 机会不能「直接进入」（最高「验证后进入」） | 已有认证（多选，含互斥的"暂无"）/ 法务合规支持 |
| 能力 | 影响「看自己 → 适配度」与「赢的路径」能不能接住需求 | 产品定义 / 研发设计 / 内容素材 / 广告投放 / 运营 / 售后（统一四档：自建团队·外包可控·外包不可控·无） |
| 经验与资产 | 影响「看自己 → 适配度」与机会卡的证据可信度 | 类目经验年限 / 是否有成功项目 / 现有 Listing·品牌资产 / 客户反馈渠道 / **现有 ASIN·链接（自由文本）** |
| 表达与备注 | 影响 AI 怎么说话与「机会卡」的落地说明（不参与任何打分） | 希望 AI 怎么说 / **一句话备注（自由文本）** |

- **自由文本恰好 3 个**（品牌名 / 现有 ASIN·链接 / 一句话备注），由 `BACKGROUND_TEXT_FIELD_KEYS` 单一来源定义，有源码级守卫；
- **全部字段允许留空**（`optional: true` 是注册表的强制字段），渐进式补全；完整度显示成「背景信息完整度 7/29」并明写"**填得越全，「看自己」的问题就越少**"；
- 旧版 11 个自由文本**没有丢**：结构化不了的内容进 `legacyNotes`，仍进 AI 上下文，只是不再渲染成长表单；
- **只影响判断输入，不影响评分公式**：注册表里没有任何权重字段（源码守卫断言背景信息模块里不得出现 `weight`/`score`），机会卡权重仍是 **25/25/25/15/10**（§8.1）。

#### B. 「看自己」= 背景信息摘要 + 能力推导 + 3 个拍板问题

界面（主屏）从"62 项要确认"变成四块，且**默认折叠**：

1. **背景信息摘要（只读）**：7 组 × 「已填 x/y」+ 每组一句"影响哪个判断"；点开才看已填值；补全去「设置 → 背景信息」（二级页⑪ 仍是同一份实现）；
2. **能力推导**：24 项能力由背景信息**确定性推出**，一行汇总（已具备/部分具备/不具备/判断不了各几项），点「展开看每一条怎么推的」看每条的 `ruleId` + 人话依据，并可**逐条覆盖**（覆盖显示「已人工覆盖」，可「恢复推导」）；
3. **3 个拍板问题**：最低毛利率 / 止损条件 / 必须满足的进入条件（Q3 多选，止损条件可补充一句话）；
4. **适配度与硬约束结论**：适配度（相关题均分 × 背景信息完整度）+ 硬约束红条「不可直接进入」+ 旧版迁移记录。

**这 3 个问题为什么是这 3 个**（为什么不是别的）：

| 问题 | 为什么必须问人 | 它落到哪个下游 |
| --- | --- | --- |
| ① 最低毛利率（下拉区间，选项带 `floorPercent`） | 背景信息只有"预算规模/风险偏好"，**红线是人的决定**；没有它，"人优我廉"根本无法判定（§6.3），止损控制点也没有分母 | 直接写进 `competitorLook.ourCapabilityFinancials.marginFloor` → `decideWinningPath` 的 `marginOk`；并作为机会卡利润假设口径 |
| ② 止损条件（下拉 + 可补充） | 机会卡的「止损控制点」必须用户拍板（AI 只起草）；没有止损线就没有下行保护 | 硬约束（选"还没定"→ 决策边界不具备）+ 止损控制点起草 |
| ③ 必须满足的进入条件（多选） | 这是**唯一能把背景信息变成硬约束**的问题：你要求"认证齐备"，而背景信息=暂无 ⇒ 硬约束 ⇒ 最高只能"验证后进入" | 准入控制点 + 硬约束红条 |

三题都标 `isBoundary: true`，全部保留"决策边界类答不具备 ⇒ 关联机会不能直接进入"的语义（§6.4）。
**被放弃的不是项目相关性**：AI 仍可增补项目内问题（`normalizeAiQuestions` / `mergeCategoryQuestions` 原样保留，最多 8 题，
界面上标「AI 增补」），只是**不再默认塞给用户**。

#### C. 背景信息 → 能力推导规则表（确定性，24 项全覆盖）

规则在 `src/utils/capabilityDerivation.ts` 的 `CAPABILITY_RULES` 里，**每项一条规则、有规则编号**（UI 上直接显示 `ruleId`）；
纯函数：同输入同输出、无时间戳、无随机、**AI 不参与**。未填 = `unknown`（判断不了），**绝不当作"不具备"**。

| 能力项（规则编号） | 判定规则（摘要） |
| --- | --- |
| 启动资金 `funding.from_budget` | 预算 <5万→不具备；5-20万→部分；≥20万→具备 |
| 广告预算 `funding.ad_from_budget` | 同上（按项目预算推导可投入的广告盘子） |
| 可承受亏损周期 `risk.loss_tolerance_from_cycle_and_appetite` | 验证周期 ≤1月→不具备；1-3月→部分；≥3月→具备；风险偏好=保守时降一档 |
| 工厂资源 `supply.factory_from_supply_type` | 自有/长期合作工厂→具备；贸易商→部分；无稳定供应链→不具备 |
| MOQ `supply.moq_from_range` | ≤1000→具备；1000-3000→部分；>3000→不具备（硬约束） |
| 账期 `supply.payment_terms_from_answer` | 有→具备；部分→部分；没有→不具备 |
| 交付周期 `supply.lead_time_from_range_and_supply_type` | ≤30天→具备；30-60天→部分；>60天→不具备；无稳定供应链时直接不具备 |
| 类目经验 `team.category_years` | 没做过/<1年→不具备；1-3年→部分；≥3年→具备 |
| 产品定义 / 研发设计 / 内容素材 / 售后 `team.ability_*` | 自建团队→具备；外包可控→部分；外包不可控→部分（缺口）；无→不具备 |
| 广告能力 `team.ability_ads_with_team_size` | 在上一行基础上：**团队 1-2 人 且 广告=无 或 外包不可控 → 不具备**（用户举的例子） |
| 运营能力 `team.ability_ops_with_team_size` | 同上（团队 1-2 人 + 运营无人管 → 不具备） |
| 认证 `compliance.certifications_vs_marketplaces` | 全是"暂无"→不具备（硬约束）；主战场含 EU 但无 CE→不具备；主战场含 US 但无 FCC/FDA/CPC→部分（是否必需取决于品类）；否则具备 |
| 商标 / 专利风险 / 品类准入 | 背景信息里没有对应字段 → `unknown`（**诚实说判断不了**，要用户逐条覆盖或让 AI 增补问题） |
| 目标售价 / 目标毛利率 / 净利率红线 / 回本周期 | 属项目级决策（利润计算器 / 机会卡 / 第 1 个拍板问题）→ `unknown`，背景信息不参与 |
| 验证期长度 `risk.validation_length_from_cycle` | ≤1月→不具备；1-3月→部分；≥3月→具备 |
| 库存风险承受 `risk.inventory_from_appetite_and_terms` | 进取→具备，其余→部分；无账期降一档；预算<5万再降一档 |

`unknown` 的项**不进适配度分母**、**不算缺口**（前置资源缺口只认 lack/partial），避免"因为没填而被判成不行"。

#### D. 被删掉的 42 项去哪了

| 原 42 项 | 现在由谁承担 |
| --- | --- |
| 目标（项目目的/目标用户/销量目标/上市时间/战略意义）5 项 | **不再单独问**：项目目标在项目中心建项目时填；机会卡里由利润假设与路线图承担；不再要求逐项四态确认 |
| 能力 10 项 + 资源 9 项 + 经验 5 项（共 24 项） | **改为背景信息确定性推导**（上表 24 项能力），可逐条覆盖；覆盖显式标记 `source: manual`，AI 不得改写 |
| 约束 8 项 | 拆分：MOQ/交期/认证/预算/风险偏好 → 背景信息（推导上面「MOQ/交付周期/认证/启动资金/库存风险承受」）；类目限制/体积重量 → 留给机会卡的风险与控制点；不再逐项确认 |
| 决策边界 5 项（最低毛利/最高 CPC/最大验证成本/止损条件/进入条件） | **保留为 3 个拍板问题**（最低毛利率 / 止损条件 / 必须满足的进入条件）；"最高 CPC 与最大验证成本"并入机会卡的准入/过程控制点阈值（二级页⑭ 可编辑），不再单独问 |

**旧数据不丢、也不放行**：迁移时把旧版 42 项里「决策边界 / 约束」类答过"不具备/部分具备"的记录转成
`SelfAssessment.legacyBoundaryNotes`，**继续参与硬约束判定**（`buildHardConstraintVerdict` 的第 4 个来源），
界面上也照实显示"旧版 42 项里的决策边界记录"。

#### E. 硬约束链路怎么证明没断

链路（**读数路径本轮一行没改**，只换了数据来源）：

```
3 个拍板问题的答案
  → categoryQuiz.boundaryItemsFromQuiz()            （category='boundary' 的 SelfAssessmentItem[]）
  → ourCapability.hardConstraintsFromSelfAssessment()（lack → blocked=true）
  ├→ buildOurCapability().hardConstraintBlocked → decideWinningPath()（赢的路径：可不可以"直接进入"）
  └→ buildHardConstraintVerdict() → 存进 SelfAssessment.hardConstraintVerdict
        → OpportunityDecisionBoard 的机会卡顶部红条（"不可直接进入"）
背景信息推导出的合规/MOQ/财务红线 lack
  → capabilityLibrary.capabilityHardConstraints() → 同一个 verdict（第 2 个来源）
AI 增补的边界题答"不具备" → quizHardNotes（第 3 个来源）
旧版 42 项迁移记录 → legacyNotes（第 4 个来源）
```

**自己的断言（`tests/capabilityDerivation.test.ts`）**：构造"背景信息=暂无认证"+"拍板要求认证齐备"，
断言 ① 决策边界项 status=lack；② `buildHardConstraintVerdict().blocked === true`；
③ 真跑 `decideWinningPath()`：赢面成立（人无我有）但 `canEnterDirectly === false`，结论里必须出现"验证后进入"，
blockers 里必须有"硬约束否决"；④ 反向（背景信息补上 CE/FCC）⇒ blocked=false、notes 为空。
另有 1 条断言专门守"旧版迁移记录仍然 blocked=true"。

#### F. ⚠️ 与线框图 / PRD 的偏离（用户指示，如实记录，不偷偷改）

- 线框图第 6 屏写的是「账号背景库（六维度·渐进）+ **品类选择题（5-8 题）** + 适配度与硬约束」；
  二级页 ⑪ 是"账号背景库（六维度）"、⑫ 是"品类选择题与依据"。
- 本轮按用户指示**偏离**：交互换成「背景信息摘要（只读）+ 能力推导（可覆盖）+ **3 个拍板问题**」；
  二级页 ⑪ 标题改为「背景信息与能力推导（只读）」、⑫ 改为「3 个拍板问题与依据」。
- 保留的部分：⑪⑫ **仍是独立 L3 全屏页**，且与内联**共用同一份实现**（`variant` 模式，`tests/l3Pages.test.ts` 继续守着）。
- 偏差同时记进 `docs/ui-conformance-audit.md` 第 6 屏行（⚠️ 标记）与 `docs/M6-ui-acceptance-checklist.md` §5。

#### G. 验证（实测原始输出见本轮交付报告）

- `node node_modules/typescript/bin/tsc --noEmit` → **exit 0**；
- `node tests/runAll.mjs` → **44 套件 / 453 断言 passed / 0 failed / 问题套件 0 个**（改动前基线 42 套件 / 423 断言，**断言数只增不减**）。
  本轮自己新增 **26 条**：`tests/capabilityDerivation.test.ts` 20 条（字段注册表守卫、自由文本 ≤3 的双证、3 个问题的双证、
  推导纯函数可复现、覆盖优先、硬约束端到端到 `canEnterDirectly=false`、真实 SSR 渲染、评分权重未被碰）
  + `tests/capabilityQuiz.test.ts` 13→16 条 + `tests/ourCapability.test.ts` 10→13 条。
  （跑出 44 套件里另有 1 个 `apiEsmImports.test.ts`（4 条）来自同工作区**另一处并发改动**，不是本轮产物。）
- `node node_modules/vite/bin/vite.js build` → **exit 0**（`✓ built`，产物 `dist/assets/index-*.js`）。

#### H. 遗留边界（没做到 / 已知取舍）

1. **不能从「看自己」直接打开设置弹窗**：设置弹窗由 `App.tsx` 持有，跨层回调会牵动 App 壳；
   现在的做法是：背景信息摘要只读 + 文案指路「设置 → 背景信息」+ 二级页⑪ 展示同一份数据，
   并在 `saveUserBackground()` 里广播事件让「看自己」**立刻重新推导**（不用刷新页面）。
2. **项目相关性变弱（本轮最大的产品取舍）**：不再默认问"这条需求你能不能接住"（原来最多 4 道需求题，直接填
   `ourCapabilityByNeedId`）。现在推导出的是**域级能力**（供应链/合规/团队/资金/风险），它进适配度、进前置资源缺口、
   进硬约束，但**不映射到"某条需求"**。后果：`decideWinningPath` 里"我们能不能接住这条需求"更容易落在
   `unknown`，从而把"人无我有 / 人有我优"判成"我们的能力没有覆盖这些需求（或尚未自评）"。
   补法有两条（都在界面上可走）：① 让 AI 增补项目内问题（`normalizeAiQuestions` 通道保留，最多 8 道，
   答完仍会写回 `ourCapabilityByNeedId`）；② 在竞对页人工指定每条需求的可做性（既有能力，未改）。
   **如果你觉得这个取舍不对，正确做法是恢复"按需求提问"而不是恢复 42 项四态表**（详见 §15.19 D 的取舍表）。
3. **`unknown` 变多**：商标/专利/类目准入/财务口径共 6 项背景信息给不了，界面上明确显示"判断不了"，
   需要用户逐条覆盖或靠 AI 增补问题；它们不进适配度分母，**不会把适配度压低**，但也不提供信息。
4. **旧版 42 项里"目标/能力/资源/经验"的非 lack 记录不迁移**（只有决策边界/约束类的 lack/partial 迁移），
   因为新模型没有对应字段；如果用户当初在旧版里标过"不具备"的能力项，它现在由背景信息推导结果代替。
5. **报告口径文案是文案债**：`opportunityHtmlReport.ts` 里适配度那一行仍写「背景库完整度」、硬约束口径仍写
   「自评里"决策边界/约束"类」，而数值来源本轮已换成"背景信息 + 3 个拍板问题"（`computeBackgroundCompleteness`
   替换了 `computeCompleteness`）。**数字口径一致，用词没跟上**，本轮没改这两句文案（避免顺手动报告模板）。

---

### 15.20 删除「项目归档」+ 分支模型收成只有 main（2026-09）

**用户指示**：「现在是不是可以删掉 phase 0 了，本地也是。保留 main 就行，之后的推送可以推到 main 了。」
以及「归档是什么意思，有意义吗，没有的话删掉这个功能」。

**一、分支模型变更（已执行）**
- 远端与本地 `phase-0` **均已删除**；此后**只保留 `main`**，所有提交直接推 `main`（`main` 即生产分支，Vercel 自动部署）。
- 合并方式：`phase-0` → `main` 用的是**合并提交**（不是强推），因为 `main` 上有一条 `phase-0` 没有的提交
  `63abd4c "Redeploy production from main"`（**空提交**，只为触发重部署，未改任何文件）。
  合并结果与 `phase-0` 内容逐字节一致（`git diff` 验证），并在合并后的 `main` 上重跑全量验证。
- 发布标签：`v2.9.0`。
- PRD 里其他章节残留的 `phase-0` 字样属**历史记录**（当时的评审对象/推送区间），保留不改。

**二、「归档」调查结论：代码里是两个不同的东西，处理方式不同**

| 名字 | 实际是什么 | 处理 |
| --- | --- | --- |
| **项目归档** | 项目卡上的「归档/恢复」按钮 + 「已归档」状态筛选项；归档后项目默认从列表消失 | **删除** |
| **报告归档** | 全局工具（关键词/VOC/竞对）生成的报告自动汇总进该项目的报告库 | **保留**，只把文案改准（"自动归档到这里"→"用五看工具生成的报告会自动汇总到这里"） |

**删除项目归档的三条理由**：
1. 与 `product-charter` 的原则直接冲突 —— 产品"服务少量深度项目，不承担复杂的项目组合管理"；
2. 代价真实：多一个状态、两个图标按钮、一个筛选项，以及"归档和删除到底差在哪"的持续困惑；
3. **风险真实**：`filter(p => p.status !== 'archived')` 让归档项目默认从列表消失，用户会以为项目丢了。
   要清理列表已经有"删除"（带二次确认），归档只是"不敢删又不看"的中间态。

**数据安全**：不动历史数据。界面不再按 `archived` 过滤，因此历史上被归档的项目**重新出现在活跃列表里**。
`ResearchProject['status']` 仍保留 `'archived'`（历史记录可能带这个值），但不再有任何写入口。

**验证**：`node tests/runAll.mjs` 全绿；`ProjectCenter` / `projectStore` 的归档代码与 `searchProjects` 的
archived 分支一并删除，未留死代码。

---

### 15.21 线上管理员后台 FUNCTION_INVOCATION_FAILED 的两个根因（2026-09）

**症状**：线上管理员后台每个页签都报 `HTTP 500`，其中审计页给出 Vercel 平台级错误
`FUNCTION_INVOCATION_FAILED`（说明函数在 `try/catch` **之外**崩溃，通常是模块加载阶段）。
本地一切正常 —— 典型的"本地全绿、线上炸"。

**根因（两个，都已修）**：

1. **缺表被当成服务器错误**（这就是界面上那个 HTTP 500）
   `api/admin/[action].ts` 原有 9 处 `if (error) return json(res, 500, {...})`。表不存在时 PostgREST 返回
   `relation "public.usage_events" does not exist`，被直接当成 500。**但缺表不是故障，是"该跑迁移了"**。
   → 新增统一出口 `dbError(res, where, error)`：缺表（`42P01` / "does not exist" / schema cache）
   返回 **200 + `needsMigration` + 缺哪张表 + 指引**；其他错误返回 500 + 原始信息 + 是哪个接口。
   界面对缺表显示**琥珀色友好提示**，不再显示红色报错；对真错误则把**服务端返回的原文**显示出来
   （以前只有"HTTP 500"，拿到也没法定位）。

2. **`api/**` 的相对 import 缺扩展名**（平台级崩溃的头号嫌疑）
   逐文件核对后规律非常干净：**线上能用的文件，相对 import 都带 `.js`**（`api/auth/[action].ts → './_shared.js'`、
   `api/health.ts → './auth/_shared.js'`）；**线上崩掉的三个文件恰恰有 8 处无扩展名**：

   | 文件 | 无扩展名的 import |
   | --- | --- |
   | `api/admin/[action].ts` | `../../src/utils/usageAccounting`、`../../src/utils/keyMasking` |
   | `api/data/[action].ts` | `../../src/utils/poolCache`、`poolCacheStore`、`usageAccounting` |
   | `api/ai/[action].ts` | `../../src/utils/keyMasking`、`aiEndpoints`、`aiModels` |

   本地 `tsx` / Vite 会自动补扩展名，线上 Node 的 ESM 解析不会 → 函数在加载阶段就崩。
   8 处全部补成 `.js`，与线上可用的文件写法一致；新增 `tests/apiEsmImports.test.ts`（4 条断言）钉死复发。

**诊断能力（临时）**：新增 `api/diag.ts` —— **不静态引入任何业务模块**（只有 `@vercel/node` 类型），
全部用动态 `import()` + `try/catch` 逐个试：带/不带扩展名两种写法、Supabase 客户端与 `projects` 表探测，
返回 Node 版本、`VERCEL_ENV`、commit、各环境变量的**布尔**存在性、每一步的成功/失败与**错误原文**。
用户只需打开 `GET /api/diag` 就能把真相发回来，不必翻 Vercel 日志。定位完成后删除。
（安全：不读任何表数据、不回显任何密钥值，只有布尔。）

**顺带修掉的"看不到真相"问题**：管理员面板以前把非 JSON 响应吞成 "HTTP 500"，现在会带出原始响应前 300 字。

**验证**：`tsc` 0；`node tests/runAll.mjs` 全绿（含新增 `apiEsmImports` 4 条）；`vite build` 0；
本地 `/api/diag` 实测返回全部步骤。

---

### 15.22 MCP 配置简化 + 二级页"标签不许撒谎"（2026-09）

**用户指示**：「mcp的配置设置，也简化，傻瓜化，没必要出现的文字都删掉。」

**MCP tab 精简**（215 行 → 约 136 行）：
- 全局说明压成**一句**（密钥由平台服务端管理，这里只看状态）；
- 每个数据源压成**一行**：种类标签 + 名称 + 启用勾选 + 「检查」（结果直接显示 可用/不可用）；
- **删除**所有营销式/重复提示（每个供应商一段"不要填 XX 否则跨域"、每类一段"适合什么场景"共 4 段）；
- MCP 地址这类"默认不用管"的东西**收进「高级」折叠**，只留一句"只有自建中转时才填"；
- 5 个「添加 XXX」大按钮 → 一行小字标签按钮。

**二级页契约的诚实性修正**：页壳顶部原写死「**线框图**对这一页的要求（**原文**）」。⑪⑫ 已按用户指示改过设计
（见 §15.19），四句因此不再是线框图原文 —— 那句话就成了"界面在撒谎"。现在：
`L3Page` 新增 `deviation` 字段；有偏离的页改为「本页要求（已按用户指示调整，非线框图原文）」并显示偏离说明。
`tests/l3Pages.test.ts` 新增 3 条断言：**只有 ⑪⑫ 允许有 deviation**（改写别的页必须先声明）、
偏离说明必须写清"线框图原文是什么 + 谁要求改的"、页壳必须随 deviation 切换标签。

---

### 15.23 背景信息改成"亚马逊卖家的普遍特征" + 品牌多值与品牌资产页（2026-09）

**用户原话（四条，2026-09）**：

1. 「背景设置的问题，我觉得不符合亚马逊卖家的普遍特征，你可以先去了解一下，再来设置问题」
2. 「另外品牌应该可以添加多个。」
3. 「另外品牌点进去还有次级页面，可以设置品牌的相关资产和信息。」
4. 「以下文字内容删掉：① 选、② 填、③ 获取模型，点一个用…这里填的是「谁在做判断」：以选择题为主（自由文本只有 3 个），
   每一组都写明它影响五看里的哪个判断。填得越全，「看自己」里要你回答的问题就越少、适配度也就越可信；
   留空不阻塞分析。信息只保存在本机浏览器。背景信息完整度 27/29：已足够可信。」

---

#### A. 先做功课：亚马逊卖家的真实分类（本轮字段的依据）

**结论先行**：亚马逊卖家的差异**不在"企业规模"，而在"打法与约束"**——经营模式（精品/精铺/铺货/代发/跟卖/收购）、
配送方式、品牌与备案、测评资源、图片能力、单链接广告预算、店铺矩阵。上一版按"主体与规模（含年营收量级）"
组织字段，是照传统企业刻度写的，所以"不接地气"。

| 调研到的事实 | 对字段设计的影响 | 来源 |
| --- | --- | --- |
| 北美语境把 Amazon 生意分成 6 种：private label / wholesale / retail arbitrage / online arbitrage / dropshipping / handmade；私标"更赚钱但需要更多资源"，三大挑战是找产品、Listing 优化、**review acquisition** | 新增「经营模式」（6 选项）、「图片与内容能力」、「测评与评论资源」 | [ZonGuru: Amazon Business Models Explained (2026)](https://www.zonguru.com/blog/amazon-business-models-guide) |
| 中国跨境圈的实际分法是 **精品 / 精铺 / 铺货**：精品一个运营只带 5–8 条链接、预算充足、前期 3–6 个月烧钱（品牌/垂直/广告/价格操作/测评/站外）；精铺 6–8 条以上或 <100 SKU、预算偏紧（**美国站精准长尾词 CPC 约 $1.5–3**）；铺货靠自研 ERP + FBM 高单价蓝海选品，**单店有上限所以必须多开店铺** | 新增「经营模式」「店铺 / 账号矩阵」「单链接月广告预算（不投 / ＜$500 / $500-2000 / $2000-1 万 / ＞1 万）」，并在字段说明里直接引用 CPC 事实 | [知无不言社区：何为精品、精铺和铺货](https://www.wearesellers.com/question/105675)、[连连跨境百科：亚马逊卖家的三种运营模式](https://global.lianlianpay.com/article_wiki/32-39698.html) |
| 铺货/精铺都靠"大量账号 + 稳定货源 + 可控交付"；泛铺的出货方式是**国内自发货 + 海外仓/FBA 配合** | 新增「配送方式（FBA / FBM / 海外仓 / 混合）」「选品来源（1688 / 工厂直供 / 展会 / 跟卖 / 自有研发）」 | 同上 |
| 品牌卖家有"三堵护墙"：品牌、私域流量池、研发设计能力 | 品牌升级为**独立档案 + 次级页**（身份/资产/策略），研发设计单列能力项，并把「能否开模定制」拆成独立字段 | 同上 |
| Brand Registry 是 A+、品牌旗舰店、品牌广告与透明计划的前置；没有备案拿不到这些工具 | 「品牌备案（Brand Registry）」与「商标状态」**拆成两个字段**（商标是法律状态，备案是平台权益） | [Amazon Seller Forums: Brand Building 103（Brand Registry 工具与保护）](https://sellercentral.amazon.co.uk/seller-forums/discussions/t/5a7b8546-7fa8-4245-a4e3-c02698c3caa4) |
| 亚马逊《卖家行为准则》：一个销售地区原则上只保留**一个**卖家平台账户，"合理业务需要"（如拥有多个品牌）才允许第二个 | 「店铺 / 账号矩阵」如实填写；多店是备货与风险的分散手段，也是铺货模式的前提 | [亚马逊卖家行为准则摘要（深交所招股书引文）](http://reportdocs.static.szse.cn/UpFiles/rasinfodisc1/202309/RAS_202309_56E5953B2ACC4858AD2FBD3366A4E29C.pdf) |
| 欧洲站除了 CE 还有 EPR/包装法/欧代等合规约束，缺了会被卡在入仓/上架环节 | 「目标站点」多选保留（EU 会带出 CE 硬约束），认证选项补 EPR | [跨境合规标签：欧盟产品资质加强管控](http://seller.dhgate.com/news/media/i659201.html) |

**没被引用的"事实"一律不做字段**（这是上一版的病根：字段看着专业，但其实不改变任何判断）。

---

#### B. 字段集前后对比（7 组 29 字段 → 6 组 29 字段）

| 上一版 | 本轮 | 为什么 |
| --- | --- | --- |
| 主体与规模：卖家类型 / 团队规模 / **年营收量级（到"1 亿以上"）** / 主战场站点 / **品牌名（单值文本）** | 经营模式与站点：**经营模式**（精品深耕/精铺/铺货/代发/批发跟卖/品牌收购）/ 卖家类型（工厂型·贸易型·品牌型·服务商）/ **配送方式（多选）** / **店铺矩阵** / 目标站点 / **品牌名（多值列表）** | "年营收量级"是传统企业刻度，亚马逊中小卖家填不了也不用填；卖家类型保留（中国跨境圈的通行分法）；品牌名从"一个文本框"改成"可添加多个" |
| 资金与风险：单项目预算 / 验证周期 / 风险偏好 / 账期 | 资金与广告：单项目**备货**资金（≤5 万 / 5-20 万 / 20-50 万 / ≥50 万）/ **单链接月广告预算** / 验证周期 / 风险偏好 | 广告预算**按链接**才是卖家的真实口径（CPC 事实见上表）；预算上限从"100 万以上"收到"50 万以上"（去大企业刻度） |
| 供应链与产品：供应链类型 / 是否自有模具 / MOQ / 交期 / **质量管控方式** | 供应链与备货：供应链类型 / **选品来源（多选）** / **能否开模定制** / MOQ / 交期 / 账期 | "质量管控方式"没有任何规则用它，也没有一个判断依赖它 → 删；"自有模具"和"能否开模定制"是一件事，合并；**选品来源**是卖家真正的差异点 |
| 合规与资质：认证（多选）/ **法务·合规支持** | 品牌备案与合规：**商标状态** / **品牌备案（Brand Registry）** / 认证（+EPR） | 「法务支持」同样没有规则用它 → 删；把**商标与备案拆开**（法律状态 ≠ 平台权益）；商标字段补上后，「看自己」的"商标"从**永远"判断不了"**变成一条真规则 |
| 能力：产品定义 / 研发设计 / **内容·素材** / 广告投放 / 运营 / **售后** | 团队与能力：团队规模 / 产品定义 / 研发设计·开模 / **图片与内容能力**（自建摄影设计 / 外包可控 / 只有基础图 / 无）/ 广告投放 / 运营 / **测评与评论资源**（有稳定合规渠道 / 只有少量真实客户 / 完全没有） | 「内容素材」升级成"图片与内容能力"（亚马逊成败关键）；「售后」不再单独问——**由配送方式推导**（FBA 由亚马逊承接客服与退货）；新增**测评与评论资源**（私标第一约束）与独立能力项 `review_resource` |
| 经验与资产：类目经验 / **是否有成功项目** / **现有 Listing·品牌资产** / **客户反馈渠道** / 现有 ASIN·链接 | 经验与备注：类目经验年限 / 现有 ASIN·链接 / 一句话备注 | "是否有成功项目""现有资产""客户反馈渠道"都与别的字段重复（现有 ASIN 就能证明你在平台上做过；资产归品牌档案）→ 删；**旧值不丢**（见 D 节留档） |
| 表达与备注：希望 AI 怎么说 / 一句话备注 | 经验与备注：一句话备注 | "希望 AI 怎么说"只影响口吻却占一个下拉 → 删（表达偏好仍可由备注给出） |

**不变量**（与 §15.19 一致）：全部字段可留空；自由文本 **≤3**（本轮恰好 2 个：`existingAsin` / `freeNote`）；
品牌名是 `kind: 'list'` 的**多值输入**，不占自由文本预算；设置页 profile tab 的文本框数量由源码级守卫断言；
注册表里没有任何权重字段（机会卡权重仍是 25/25/25/15/10）。

---

#### C. 每个字段都要能改变判断（这一条是本轮最有价值的守卫）

用户的要求"每个字段都要能回答它会改变五看里的哪个判断"在实现上有两个硬约束：

1. **字段级 `affects` 必填**：`BackgroundField.affects`（新）与 `BackgroundGroup.affects`（原有）都必须非空且写明影响什么；
2. **字段与规则必须对得上**：`tests/capabilityDerivation.test.ts` 新增守卫，同时断言四件事：
   - 源码里 `pick/pickMulti/byOption/abilityRule` 取到的字段**都真实存在**（不许引用幽灵字段）；
   - `RULE_INPUT_FIELDS`（声明清单）与源码扫描结果**完全一致**（加了用法忘登记 / 登记了没真用，都会红）；
   - 注册表里**每个非备注字段**都必须被至少一条规则用到 —— 不许有"填了也白填"的字段（这就是删掉
     年营收量级 / 质量管控方式 / 法务支持的原因）；
   - 豁免清单 `BACKGROUND_ADVISORY_FIELDS` 最多 2 项、只能是备注类文本、必须写明豁免理由（防止拿它当后门藏字段）。

**规则表本轮的实际增强**（`src/utils/capabilityDerivation.ts`）：

| 能力项 | 新增/变化的规则 |
| --- | --- |
| 广告预算 `funding.ad_from_listing_budget` | 不再拿"项目预算"当广告预算代理，改成**单链接月广告预算**（不投广告=不具备；＜$500=部分；≥$500=具备） |
| MOQ `supply.moq_from_range_and_business_model` | 铺货/代发 → **只允许更保守**（降一档）；精品/收购 **不放宽** —— MOQ 是硬约束，不能因为"你是精品卖家"就静默解锁"直接进入" |
| 交付周期 `supply.lead_time_from_range_type_and_fulfillment` | 有 FBA/海外仓时，工厂交期长一点也接受（尾程已在目的地仓） |
| 工厂资源 `supply.factory_from_supply_type_and_sourcing` | 新增"选品来源=工厂直供/自有研发"→ 升一档；新增"卖家类型=工厂型"→ 即使没填供应链类型也能判定 |
| 研发设计 `team.ability_rnd_with_customization` | 「可以开模」升一档、「只能拿现货」降一档 |
| 内容/设计 `team.ability_content_with_brand_registry` | 没备案 ⇒ 用不了 A+/品牌旗舰店 ⇒ **降一档** |
| 广告能力 `team.ability_ads_with_team_size_and_budget` | 原有"1-2 人 + 没人管 = 不具备"保留，另加"有投放能力但预算=不投广告 → 降为部分具备" |
| 售后 `team.after_sales_from_fulfillment_and_team` | **不再问用户**：FBA=具备、海外仓=部分、FBM + 1-2 人=不具备 |
| **评论资源 `team.review_resource_from_channel`（新能力项）** | 与「测评与评论资源」一一对应；`CAPABILITY_ITEMS` 24 → 25 项 |
| 库存风险 `risk.inventory_from_model_appetite_terms_and_stores` | 经营模式（代发/铺货升档、精品/收购降档）与店铺矩阵（多店分散 → 升档）都进这条规则 |
| 商标 `compliance.trademark_from_status_and_registry` | **从"永远 unknown"变成真规则**：已注册=具备 / 申请中·授权=部分 / 无商标=不具备（进硬约束） |
| 类目经验 `team.category_years_with_existing_listing` | 没填年限但填了现有 ASIN → 至少"部分具备"（你确实在这个平台做过） |

---

#### D. 品牌多值 + 品牌资产次级页

**数据存哪（不新增服务端表）**：品牌存在**背景信息里**——`UserBackgroundProfile.brands: BrandProfile[]`，
与背景信息共用同一个 localStorage 键（按 userId 隔离）、同一个保存入口 `saveUserBackground()`、
同一个刷新事件 `kairo:user-background-updated`。背景信息里「品牌名」字段（`kind: 'list'`）的取值口径
就是 `brands.map(b => b.name)`，**不许在 `fields` 里再存一份品牌名**（守卫断言 `fields.brandNames === undefined`）。

> 关于"现有云同步通道"：核对过代码，云同步（`src/utils/cloudSync.ts` → `api/projects/*`）**只同步项目数据**，
> 背景信息本身一直是本机浏览器存储、没有服务端通道。本轮**没有**为此新增表或接口；
> 品牌与背景信息保持同一份存储，将来背景信息上云时品牌自动跟着走。

**形态（不许复制两份实现）**：
- `src/components/BrandDetailSheet.tsx` 里的 `BrandAssetsBlock` 是**唯一一份字段渲染**，两种形态：
  `variant="inline"`（设置页里点品牌就地展开）与 `variant="sheet"`（全屏二级页正文）；
- 全屏页复用**既有 L3 页壳** `src/components/L3Sheet.tsx`（返回 / Escape / 锁滚动 / 四句契约一套不重写）。
  为此把 `L3Sheet` 的 `page` 参数类型从 `L3Page` 放宽成结构化的 `L3SheetPageInfo`（`L3Page` 天然满足它，
  注册表与既有 8 屏一行未改），并支持 `badgeLabel`；
- 品牌页不是线框图编号页，因此 `brandPageInfo()` 带 `deviation`，页壳会显示「本页要求（已按用户指示调整，非线框图原文）」；
- 守卫放在新套件 `tests/brandAssets.test.ts`（照 `tests/l3Pages.test.ts` 的模式）：
  `<BrandAssetsBlock` 至少 2 处、`variant="sheet"` 恰好 1 处、`BRAND_GROUPS.map` 恰好 1 处、
  设置页只能通过 `<BrandListEditor>` / `<BrandDetailSheet>` 使用品牌（不许自己再渲染一份字段）。

**品牌字段三组 20 项**（每项都有 `affects`，与背景信息同一套口径）：

| 组 | 字段 |
| --- | --- |
| 品牌身份（6） | 品牌名 / 商标状态 / 商标号 / 注册类目（尼斯分类）/ 品牌备案（Brand Registry）/ 所属公司主体 |
| 品牌资产（9） | 品牌旗舰店链接 / A+ 页面状态 / 品牌故事 / 主视觉·logo / 已有 Listing 数 / 平均评分 / 累计评论数 / 品牌搜索量（若已知）/ 关联店铺·账号（多选） |
| 品牌策略（5） | 价格带定位 / 主打卖点（多选）/ 目标人群 / 竞品品牌（自由多值）/ 下一步计划 |

**品牌的多值与默认**：添加 / 重命名 / 删除 / 设默认（`withSingleDefault` 保证"有且只有一个默认品牌"，
删掉默认品牌时默认位顺延）；重名与空名直接拒绝；上限 20 个；改名不丢资产；
「品牌名列表整体写回」用 `syncBrandNames`（保留已有品牌资产，只补新增、只删移除）。
**旧数据不丢**：上一版只有一个「品牌名」文本框 → 迁移成 1 条品牌记录（已断言）。

**留档**：被本轮删掉的旧结构化字段（年营收量级 / 质量管控方式 / 自有模具 / 现有资产 / 客户反馈渠道 /
法务支持 / 表达偏好 / 是否有成功项目）在读取时**翻成人话写进 `legacyNotes`**（`RETIRED_STRUCTURED_FIELDS`），
字段可以下线，用户填过的东西不丢。

---

#### E. 设置页 profile tab：删掉那段文字，依据改成角标

| 用户点名删除的文案 | 处理 |
| --- | --- |
| 「① 选 / ② 填 / ③ 获取模型，点一个用」 | **不在背景信息页**（它在 API 与模型页，属于另一处并发改动）——本轮未碰该 tab |
| 「这里填的是「谁在做判断」：以选择题为主（自由文本只有 3 个），每一组都写明它影响五看里的哪个判断。」 | 已删除（设置页源码里连注释都不再逐字引用，避免守卫扫描时误判"文案回流"） |
| 「填得越全，「看自己」里要你回答的问题就越少、适配度也就越可信；留空不阻塞分析。信息只保存在本机浏览器。」 | 已删除；**并且这句话是错的**：§15.19 之后「看自己」固定只问 3 个拍板问题，背景信息填多填少都不会减少题量，只影响适配度可信度。因此 `describeBackgroundCompleteness()` 里的"填得越全…问题就越少"一并去掉，守卫断言它不许回来 |
| 「背景信息完整度 27/29：已足够可信。」 | 长句已删除；只保留短徽章「背景信息完整度 x/29」+ 一句 ≤30 字的短提示（如「完整度 12/29：可随时补，只影响适配度可信度 · 建议先补「品牌备案与合规」」） |

**角标**：每一组的「影响哪个判断」收进 `<details>` + 一行短标签（默认只占一行小字，点开才展开）；
每个字段的 `affects` 同样收进一行「影响」角标 —— **并且没用原生 `title` 属性**
（§15.24 已把原生 title 列为反模式：不能换行、触屏/键盘用户看不到）。
`<details>` 不是自造组件，是原生可访问元素；等 §15.24 的 `InfoTip` 口径可分割时可以替换 ——
但目前**不能**直接替换：`tests/infoTip.test.ts` 的反向守卫明确禁止把「硬约束 / 合规 / 验证后进入」这类关键口径藏进角标文案，
而背景信息 6 组里有 1 组、字段里有若干个的 `affects` 正是这类口径（如"影响硬约束：认证缺失 ⇒ 不能直接进入"）。
把它们换成浮层角标会让守卫变红，也会让关键口径从正文里消失。

---

#### F. 验证与断言数

- `node node_modules/typescript/bin/tsc --noEmit` → **exit 0**；
- `node tests/runAll.mjs` → **48 套件 / 512 断言 passed / 2 failed**（本轮开工时的基线：44 套件 / 455 passed / 1 failed）。
  **那 2 条失败与本节无关，全部来自同一工作区另一处并发改动**（MCP 密钥口径反转）：
  `tests/securityKeys.test.ts` 的两条断言仍写着"密钥只在服务端 / 平台供应商不得有密钥输入框"，
  而 MCP tab 已按新的用户决策改成"每个数据源都能填自己的 Key（只存本机浏览器）"；
  同一时刻 `tsc` 也一度因为对方正在改 `src/utils/mcpConfig.ts`（缺 `pushServerKeys` 等标识符）而失败，随后恢复。
  本轮**没有**碰 api / mcp / features / prompts / diagnostics 这几个 tab，也没有碰 `api/*` 与密钥相关文件。
  只跑本轮涉及的 8 个套件：`node tests/runAll.mjs brandAssets capabilityDerivation capabilityQuiz ourCapability l3Pages hookOrder terminology infoTip`
  → **8 套件 / 100 断言 passed / 0 failed**；
- 本轮新增/改写：`tests/brandAssets.test.ts`（**新套件**，12 条）+ `tests/capabilityDerivation.test.ts`（20 → 25 条），
  断言数只增不减、没有降低守卫强度；
- `node node_modules/vite/bin/vite.js build` → **exit 0**（`✓ built in 8.87s`）。

---

#### G. 遗留边界（没做到 / 已知取舍）

1. **`InfoTip` 没接上**（原因见 E 节末）：角标目前是原生 `<details>`；要换成 `InfoTip`，得先把
   「硬约束 / 合规 / 验证后进入」这类口径从角标文案里拆出来留在正文，否则会踩 §15.24 的反向守卫。
2. **背景信息与品牌都只在本机浏览器**（按账号隔离），换设备要重填 —— 与改动前一致，本轮没有变差，也没有新增同步通道。
3. **商标/备案字段有两份口径**：背景信息里的是"主力品牌"的粗口径（用于推导与硬约束），品牌次级页里的是逐品牌明细。
   两处**不自动同步**（避免隐式改写用户填的明细）；将来若要合并，需要一条显式的"以默认品牌为准"规则。
4. **品牌资产（A+ / 评分 / 评论量 / 品牌搜索量）只进 AI 上下文与展示，不参与任何推导**：
   "多少评论量算资产"需要领域阈值，本轮不猜（宁可留白，也不写没有依据的规则）。
5. **`review_resource` 目前只被背景信息推导**：还没有在「看机会」的控制点/前置资源里单独使用（缺口文案会显示它）。
6. **`capabilityLibrary` 的能力项 24 → 25**（新增「评论资源」）：下游按 key 读取，新增 key 是加法；
   但"24 项"这个数字若被别的文档引用过，需要同步。
7. **旧字段值的留档是"只读留档"**：它进 AI 上下文，但界面上不显示（避免又把长表单请回来）。

---

### 15.24 长说明收进「信息角标」——全 app 角标规范（2026-09）

**用户原话**：「另外：角色与权限口径 …（一整段长说明）… 这里不要展示，或者你做成一个角标。其他地方也是（整个 app），需要大篇幅说明的地方，不要占用地方，做成角标来，鼠标悬浮或者点击到角标才会显示。」

**这一轮解决的真实问题**：界面里躺着大量"解释性长文"——卡片标题下 2–4 行注脚、原理科普、重复的操作提示。它们把版面撑满，把真正的结论与证据挤下去；而用户真正需要反复看的是结论。同时仓库里已经出现了 **4 份各写各的手搓角标**（`MetricCard` 的 TooltipIcon、`BrandLeaderboard` 的 ColumnHeaderHint、`MarketConcentrationChart` 的内联 showHint 浮层、`CompetitorHub` 的 Tip），行为互不一致：有的只能悬浮不能点击固定、有的不能被键盘聚焦、有的没有 Esc、CompetitorHub 那份还用原生 `title` 兜底（不能换行、触屏看不到）。

#### 1）组件：`src/components/ui/InfoTip.tsx`（全 app 唯一实现）

| 维度 | 规范 |
| --- | --- |
| 形态 | 一个 ⓘ 图标按钮（lucide `Info`，`w-3.5 h-3.5`，`text-[#aeaeb2] hover:text-indigo-600`），外层 `relative inline-flex align-middle` —— **默认不占位**，内联在标题/标签后面即可 |
| 交互 | 鼠标悬浮显示 · **点击固定（可再次点击关闭）** · `Esc` 关闭 · 点击外部关闭 · `Tab` 可聚焦（聚焦即显示） |
| 无障碍 | 触发按钮 `tabIndex={0}` + `aria-label` + `aria-describedby` + `aria-expanded`；浮层 `role="tooltip"` |
| 内容 | `title?` + `paragraphs?: string[]`（推荐多段）或 `content?` 单段或 `children` 自定义节点；**不许用原生 `title` 属性糊弄** |
| 排版 | 浮层 `max-w-xs`（`size="md"` 时 `max-w-sm`），`rounded-xl border border-black/8 bg-white shadow-lg`，`z-50` |
| 窄容器 | 浮层用 `position: fixed`（不是 absolute）：表头/卡片标题的祖先常带 `overflow-hidden`，absolute 会被裁掉；并做水平夹取 + 垂直自动翻转，`align` 可选 center/start/end |
| 细节 | 悬浮离开有 120ms 延迟（否则鼠标从角标移向浮层会穿过间隙而闪断）；点击 `preventDefault + stopPropagation`（角标常放在 `<label>` / 可点击卡片里） |

#### 2）什么内容**能**收、什么内容**绝对不能**收（本节比"省地方"重要）

**能收进角标**：定义与原理科普（HHI 怎么算）、"为什么这么设计"的说明、重复出现的同义说明、操作小提示（哪里能点什么）、系统确定性行为的补充解释。

**绝对不能收（必须留在正文，逐字保留）**：

1. **硬约束与决策边界**：如「决策边界不满足 ⇒ 所有机会最高只能『验证后进入』，不得直接进入」；
2. **法律 / 合规 / 安全口径**：密钥只在服务端、数据是否出本机、合规风险、不可承接边界；
3. **会改变结论解读的口径**：如「判断不了 ≠ 不具备」「0 个机会是合法结论」「样本不足 ≠ 低机会」；
4. **禁止性规则**：如「只有下面两种零机会写法是被允许的」；
5. **错误提示、空状态、失败原因**（空状态必须说明原因、缺什么、如何补）；
6. **用户此刻必须据此动作的下一步指引**。

一句话判断标准：**藏起来会不会让用户做错决定？会 → 留在正文；不会 → 可以进角标。**
这条写进了 `src/components/ui/InfoTip.tsx` 的文件头注释，并由 `tests/infoTip.test.ts` 的反向守卫强制（正则扫角标文案，命中「硬约束 / 合规 / 不得直接进入 / 验证后进入 / 密钥」等词即失败并提示"关键口径不许藏进角标"）。

#### 3）已改造清单（文件:行 → 收了多少字 → 正文留了什么）

> 行号是**本轮落地时**的快照（其它轮次改动会让它整体移位）；`tests/infoTip.test.ts` 里的清单文件只校验"文件 + 条数"，不锁行号，避免与并行的改动互相打架。

| 文件:行 | 原文 | 收进角标的内容 | 正文留了什么 |
| --- | --- | --- | --- |
| `TeamSettingsPanel.tsx:114`（**用户点名处**） | 4 行角色映射 + 1 段说明 ≈ 212 字 | 角色 → 项目默认角色 / 能否管理团队 / 项目内权限以成员角色为准 | 「角色口径」4 字 |
| `AiSettingsPanel.tsx:998`（features tab） | 42 字 | 评分卡 8 维度说明 + 关掉不影响结论 | 「默认关闭；打开后市场大盘顶部出现评分卡。」18 字 |
| `SelfReadinessPanel.tsx:421` | 42 字 | 背景信息在哪填、它影响什么 | 「从「设置 → 背景信息」填」7 字 |
| `SelfReadinessPanel.tsx:495` | 63 字 | 推导规则确定性 / AI 不改写覆盖 | 「判断不了 ≠ 不具备；要改结论请逐条覆盖」13 字（口径留正文） |
| `SelfReadinessPanel.tsx:653` | 55 字 | 为什么只问这三件事 | 「只问 3 个拍板问题：毛利率 / 止损 / 进入条件」16 字 |
| `SelfAssessmentView.tsx:158` | 64 字 | 能力由背景信息推导（可逐条覆盖） | 「3 个拍板问题 → 机会卡适配度与硬约束判定」15 字 |
| `OpportunityDecisionBoard.tsx:516` | 65 字 | 排序按「评分→覆盖度→资源缺口」、AI 不改分也不排序 | 「排序只是建议，先做哪个由你拍板」14 字 |
| `OpportunityDecisionBoard.tsx:586` | 41 字 | 优先级矩阵四象限读法 | 「横轴评分 × 纵轴覆盖度」8 字 |
| `OpportunityDecisionBoard.tsx:1142` | 146 字（页壳顶部最大一块） | 控制点默认值来源、改阈值后「已确认」作废、恢复默认 | 「阈值可直接改；改完需重新拍板」13 字 |
| `MarketLookView.tsx:232` | 35 字 | AI 只解释差异与盲区、改动无效 | 「只解释差异与盲区，不改分」11 字 |
| `CompetitorHub.tsx:1317` | 47 字 | 主图 / 附图 / A+ 的展开与排序 | 「默认只看主图；要展开附图 / A+ 看角标」14 字 |
| `UserInsights.tsx:1146` | 37 字 | 评论列表搜索与「仅图 / 视频」单独设置 | 「以上筛选作用于 KPI / 标签图 / 趋势」10 字 |
| `UserInsights.tsx:1620` | 30 字 | TOP 标签月度对比读图说明 | 标题自带（0 字） |

**顺带统一的 4 份手搓实现（内容一字未改，只换实现）**：`MarketConcentrationChart.tsx:122`（HHI 说明，73 字）、`MetricCard.tsx:37/71`（指标 tooltip）、`BrandLeaderboard.tsx:247`（有效 ASIN 数量口径）、`KeywordAnalysisView.tsx:509`（机会分算法，含权重表）、`CompetitorHub.tsx:1602–1715`（9 处指标口径，原实现带原生 `title`）。

合计收起平铺说明约 **950 汉字**，删除手搓浮层实现 4 份（约 130 行）。

#### 4）这一轮**主动没改**的地方（判断力所在，别当成漏做）

| 位置 | 为什么保留 |
| --- | --- |
| `OpportunityDecisionBoard` 零机会结论「只有两种写法被允许」 | 禁止性规则，藏起来用户会写出不合规的结论 |
| 各处「硬约束否决 / 最高只能验证后进入」红条 | 决策边界，直接决定机会能不能做 |
| `AdminConsolePanel` 密钥只在服务端、指纹不回显 | 安全口径，用户据此决定敢不敢填 Key |
| `SystemDiagnosticsPanel` 遗留浏览器内 Supabase 配置提示 | 密钥卫生隐患，属安全提示 |
| `UserInsights` VOC 标签「四类需同时满足（且）」 | 会改变筛选结果的解读 |
| 所有空状态 / 错误提示（如「还没有机会卡」「云同步未启用」） | 空状态必须说明原因、缺什么、如何补 |
| `LoginPage` 整页营销文案 | 那是落地页主线，不是"说明"；收进角标等于重做落地页 |
| `LookAiBar` 的 `hint`（各看"AI 会做什么"一句） | 是入口处操作指引；且它是 prop 字符串，改角标要动共享组件 API |
| `UserInsights` 深度洞察报告卡内的说明 | 该卡片本身就是 `<button>`，角标也是 button —— 嵌套 button 是非法 HTML |

#### 5）验证

- `tests/infoTip.test.ts`（新增，**16 条断言**）：交互齐备（悬浮 / 点击固定 / Esc / 键盘聚焦 / 点击外部 / aria-role）、**不是原生 `title`**、团队空间那一处已收进角标且正文只剩 ≤10 字摘要、改造清单逐文件可查（≥6 处 / ≥8 个文件 import）、**反向守卫**（角标文案里不得出现硬约束 / 合规 / 不得直接进入 / 验证后进入 / 密钥等关键词）+ 反向守卫自身的有效性样例 + **SSR 真渲染**（关闭态只渲染 ⓘ 按钮、无原生 title、无浮层）。
- `node node_modules/typescript/bin/tsc --noEmit` 0；`node node_modules/vite/bin/vite.js build` 0；`node tests/runAll.mjs` 本套件全绿。

---

### 15.25 竞品明细默认抓「整套图」（除 A+）+ 缺表自愈（2026-09）

**用户的两条指示**：
1. 「另外竞品明细中的提示词可能要修改，因为现在卖家精灵不止可以抓首图，可以抓整套图，我需要默认抓整套图（除了A+）。」
2. 「帮我修复」—— 管理员后台那条琥珀色提示「数据库还没迁移（不是故障，跑一次就好）…去 Supabase → SQL Editor 执行
   `supabase/migrations/all_in_one.sql`」。

#### 一、抓图口径变更（先说清"改之前到底抓了什么"）

代码里**没有**一条"抓首图"的 LLM 提示词 —— 用户说的"提示词"，实际是**抓取口径**：它藏在"抓什么字段"的编排里。
改之前的真实口径（如实记录）：

| 位置 | 改之前 | 改之后 |
| --- | --- | --- |
| 竞品明细「主图对比」 | **只有一张首图**：`asin_detail` 快照的 `zoomImageUrl \|\| imageUrl`（演示数据同样是单张 `zoomImageUrl`） | 数据层给出**整套图** `imageUrls[]`，读取走 `asinGalleryUrls()`（老快照自动退回首图，不会变空） |
| Listing 全字段抓取（MCP `asin_listing`） | 把接口返回的 `images[]` **原样**落库：无口径、无上限、A+ 混不混全看接口心情 | 默认 `imageScope: 'gallery'`、`maxImages: 9`、`includeAplusImages: false`；超出上限**截断并记数**（`imagesTruncatedByCap`） |
| A+ 模块图 | 与图库无区分（A+ 图片一直靠本地 zip 图包预览，与抓取无关） | **默认不抓**；只有显式 `includeAplusImages: true` 才读，且**永不混进图库数组** |
| 竞品明细的可见性 | 对比表里只有一个"N 张" | 新增「**整套图（Listing 图库：主图 + 附图）**」区块：逐张缩略图 + 上限说明 + 未抓取时的下一步 + A+ 口径说明 |

**口径只有一处权威定义**：`src/utils/listingImages.ts`
（`LISTING_IMAGE_SCOPE = 'gallery'`、`LISTING_IMAGE_MAX = 9`、`LISTING_IMAGE_INCLUDE_APLUS_DEFAULT = false`、
`extractListingImages()`、`listingImageFetchArgs()`），抓取层 `listingFetchPlan.ts` 与取数层 `sellerspriteApi.ts` 共用。

**上限的依据**：Amazon 详情页图库是"1 张主图 + 若干附图"，图位上限 9 张（A+ 模块图不占图位）。
接口一次返回整套图，所以上限是**防膨胀硬闸**（有些接口会把变体图、关联推荐图混进同一个数组），不是成本闸；
超出部分截断并如实记数，不静默丢弃。

**成本影响：不变（这是本轮刻意守住的线）**。整套图来自**同一次** `asin_listing` 调用（TTL 7 天），
不是"每张图一次调用"；`tests/listingImages.test.ts` 断言 `step.calls === 1` 且一个 ASIN 只规划一次 Listing 抓取。
A+ 开关同样不新增调用。**`usageAccounting` 与 `listingFetchPlan` 的成本口径一个字没改**——
如果将来要抓 A+ 模块图/变体图需要额外接口，那是**新的成本项**，必须先在这里写清楚并按 ASIN 计（不能悄悄翻倍）。

**A+ 为什么默认不带**：A+ 是详情页下半屏的品牌故事模块，与买家第一眼看的图库不是一回事；
混在一起，"第 N 张对第 N 张"的横向对比就失去可比性。要看 A+：抓取时显式开启，或用竞品图包（zip 的「附图 / A+」文件夹）。

**AI 提示词同步**（用户可编辑资产，只改默认值）：`AiPromptManager` 的 `competitor_listing` / `competitor_full_report`
把"主图"改成"整套图（Listing 图库的主图 + 附图，逐张看信息密度；A+ 默认不抓，没数据就不要提）"。

#### 二、缺表自愈（把"去跑迁移"变成可照做）

先查清事实 —— 两条**真 bug**，都不是"用户不会用"：

1. **缺表提示永远只显示 `public`**。服务端取表名的正则是 `/"([a-z_]+)"/`，而 PostgREST 原文是
   `relation "public.usage_events" does not exist` —— **第一对引号里是 schema 名**。于是界面只剩一句泛泛的指引，
   用户拿不到任何有用信息（"信息没展示到位"的根因）。→ 改为优先取 `"public.<table>"`（纯函数 `missingTableName()`）。
2. **`app_config` 从来没建过**。管理员「配置中心」的读/写与数据池密钥解析（`api/data` 的 `resolveProviderSecret`）
   一直在读写 `public.app_config`，但 001-009 **没有任何一条建表语句** —— 结果是"迁移全跑完，配置中心还是坏的"，
   而缺表提示又把用户指回同一份迁移文件，形成死循环。→ 新增 **010 · app_config** 到 `all_in_one.sql`
   （幂等、只有 service_role 可读写）。
   顺带纠正：`project_assets` 是 **Storage bucket**（007 建桶），不是数据表；现在标为 `kind: 'bucket'`，
   用 `storage.getBucket` 探测，不再伪装成"缺表"（否则界面永远显示"缺表"，而迁移文件里根本没有它的建表语句）。

改完的自愈流程（管理员后台 → 环境自检）：

1. **逐表探测**：每张表给出"已建 / 缺失（必建）/ 缺失（可选）+ 迁移版本 + 缺失影响什么"，界面**逐张列出**；
2. **一键复制建表 SQL**：只复制**缺失那几张表**的 DDL（`navigator.clipboard.writeText`）+ toast
   "已复制，去 Supabase → SQL Editor 粘贴执行"；复制被浏览器拒绝时把 SQL 展开在面板里，绝不"复制了个空的"；
   只复制缺的（不是整份迁移），需要策略依赖时自动带上共享函数段；
3. **写清"不跑也能用"**：用量统计 / 审计日志 / 数据池缓存持久化 / 报告资产上传会缺失或退化
   （缓存退化为进程内 → 实例回收即丢、外部调用成本变高）；只有 `projects` / `project_members` 缺失时，
   项目与协作数据才真的存不下来；
4. **权限错误不再被当成缺表**：`42501` / `permission denied` / RLS 一律 500 并写明"这不是缺表，跑迁移解决不了"，
   `42P01` / "does not exist" / schema cache 才走 `needsMigration`（分类抽成纯函数 `classifyDbError()`，可单测）。

**DDL 不手抄**：`src/utils/migrationSqlTables.ts` 由 `scripts/gen-migration-sql.mjs` 从 `all_in_one.sql`
**按锚点逐字切出**（不重打一遍 SQL）。`tests/migrationSql.test.ts` 钉死三件事：表名集合与迁移文件一致、
每段 DDL 逐字可溯源、复制内容里没有迁移文件里不存在的语句。

**测试与验证**：新增 `tests/listingImages.test.ts`（11 条）与 `tests/migrationSql.test.ts`（14 条）；
`node tests/runAll.mjs` 全绿；`tsc --noEmit` / `vite build` 通过（本轮新增 `src/utils/listingImages.ts`、
`src/utils/migrationSql.ts`、`src/utils/migrationSqlTables.ts`、`scripts/gen-migration-sql.mjs`）。

---

### 15.26 自带密钥（BYO Key）—— 用户推翻决策 A（2026-09）

**用户原话**：「需要其他用户也能填key，后续我们再考虑做付费，那时候再关闭mcp入口，换成计费模式。」

由此**推翻 §15.11 的决策 A**（"数据池密钥只在服务端，浏览器不保存任何密钥"+配套的"MCP 面板不得有密钥输入框"），改为 **BYO Key**：

- **所有用户**都能在「设置 → MCP 数据」给每个数据源（卖家精灵 / 西柚洞察 / 领星 / Sorftime / 自定义）填自己的 Key；每个数据源都要有 **名称 / 地址 / Key / 验证** 四样（同一段 JSX，不按 kind 分叉）。
- 不填 = 走服务端平台 Key（团队共享兜底）；平台 Key 仍然**永不进入浏览器**，管理员在「管理员后台 → 配置中心」配置。
- 用户 Key 只存他自己的 `localStorage`，随**同源 POST 请求体**（`userKey`）带给网关；网关解析顺序固定为「① 用户 Key → ② 平台 Key」，**只在当次请求内使用**。
- **安全红线**：不写库（不写 `app_config`、不写任何表）、不写日志（含 console，上游报错先 `redactText`）、不进任何响应体（连平台 Key 的指纹都不回）、不进 URL query。服务端对用户 Key 做基本校验（去空白 / 长度 ≤512 / 拒控制字符），不合法一律当"未提供"静默回退平台，不报错泄露细节。
- **用量记账**新增 `keySource: 'user' | 'platform' | 'none'` 字面量（绝不记 Key），付费阶段据此区分用户自付与平台承担。
- 「验证」改为按来源验证（`POST /api/data/verify`，只做 MCP 握手，不记用量、不占配额）；自定义 MCP 的地址过 `validateRelayTarget` 且只转发 initialize。

**守卫口径变化**（不是放宽）：原来的安全靠"面板里没有输入框"顺带保证，现在换成 6 条各自可测的显式约束 —— `tests/securityKeys.test.ts` 从 9 条涨到 15 条，另新增 `tests/dataPoolKeyFlow.test.ts`（9 条行为测试：Key 只进请求体、保存零网络请求、按来源验证、记账只记标签）。

**遗留项**：`usage_events` 需要增量迁移加 `key_source` 列（本轮未改 `supabase/migrations/**`；网关写入已做"无该列时自动退化"的兼容，迁移前后都不丢记账）。细节见 `docs/byo-key-model.md`。

---

### 15.27 顺手收尾与用户要求的三处删改（2026-09）

本轮除了 §15.23–§15.26 四件大事，还按用户逐条要求做了这些收尾（都在同一轮交付里）：

| # | 用户原话 / 要求 | 做了什么 |
| --- | --- | --- |
| ① | 「以下文字内容删掉：① 选 、② 填、③ 获取模型，点一个用」 | AI 设置页的**步骤编号全部删除**（现在就是「选 AI 供应商 / API Key / 模型」三块 + 折叠的高级设置） |
| ② | 「验证的按钮放在模型名的前面」 | 按钮行改为 **[获取模型] [验证]**，**都在"模型"下拉之前**；未填 Key 时两个都禁用并提示 |
| ③ | 「手动添加模型名可以删掉」 | 整块删除，并清掉因此变成死代码的 `useState`／两个 handler／`Plus` 图标（不留误导后人的空壳） |
| ④ | 「MCP 填密钥的地方在哪里」 | 修复**一条真实死路**：旧代码要求"先填写密钥"才让点「验证」，但决策 A 早已删掉密钥输入框 → 除被环境变量预填的管理员，所有人只会看到"请先填写密钥"。同时在 §15.26 里把 MCP 改成所有用户可填 Key |
| ⑤ | 「竞品明细中的提示词…默认抓整套图（除了 A+）」 | 由 §15.25 落数据口径；本轮**补完最后一步可见性**：竞品明细「① 买家先看主图」以前只显示一张首图，现在显示**整套图库缩略图条**（第 1-9 张、默认不含 A+、超限显示"已截断"，老快照只有一张时不会变空白） |
| ⑥ | 下钻文案与新口径不一致（C 报告指出） | `competitorDrilldown.ts` 的 ⑧ 文案从"逐张对比主图（第 1-7 张）"改为"**逐张对比 Listing 图库（主图 + 附图，默认不含 A+）**" |

**本轮补的一条迁移**：`usage_events` 增加 `key_source` 列（§15.26 的自带密钥归因需要它才能持久化）。
直接写进 `all_in_one.sql` 的 008 区块内（`add column if not exists` + 一条按来源聚合的索引，全幂等），
并重新生成 `src/utils/migrationSqlTables.ts`（"一键复制 SQL"的生成物）—— 因此**已跑过 008 的库再整体重跑一次
`all_in_one.sql` 就会补上这一列**，无需单独记一条编号迁移。

**界面借鉴（用户要求重做）**：上一版写的是"机制借鉴"（被用户明确否掉），已删除，换成
**`docs/ui-benchmark-mockups.html`** —— 5 个**画出来的界面样张**（三栏工作区 / 问题清单+修复面板 / 高密度数据表 /
上手清单+进度环 / ⌘K 命令面板），每块标注抄自哪个产品的**哪一屏**，并用 Kairo 真实字段填充。
请用户挑"样张几先做"再动真页面。

---

### 15.28 密钥跟着账号走 + 政策 A（平台 Key 不再兜底）（2026-09）

**用户原话**：「我的mcp没有跟着账号走吗，需要跟着账号」。用户同时拍板**密钥政策 = A**：
MCP 取数**要求用户填自己的 Key**，**取消平台 Key 兜底**（这条代码里原本已经是如此，本轮把它钉死）。

**问题（真实死路）**：§15.26 的 BYO Key 把用户 Key 只存在**他自己的浏览器 localStorage**
（`amzdev_mcp_settings__<userId>`）。于是换设备 / 换浏览器 / 清一次缓存再登录同一账号，Key 就没了 ——
用户只能重填一遍，还会以为"我填的东西被弄丢了"。

**做法（四件事）**：

1. **按用户存密钥的表**：新增 `public.user_provider_keys`（`user_id` / `provider` / `value` / `updated_at`，
   主键 `(user_id, provider)`），**RLS 全部拒绝**（`using (false) with check (false)`），只有 `service_role`
   能读写。写进 `supabase/migrations/all_in_one.sql` 的 **011 区块**（全幂等），并重新生成
   `src/utils/migrationSqlTables.ts`（`scripts/gen-migration-sql.mjs` 的 `TABLES` 清单同步加表）。
2. **服务端三个动作**（`api/data/[action].ts`）：`keyList`（只回 `configured` + `maskKey` 指纹，**绝不含明文**）、
   `keySet`（校验非空 / trim / 长度 ≤512 / 拒控制字符后 upsert）、`keyClear`（删该用户该 provider 一行）。
   **`userId` 一律取自 JWT（`verifyToken`），绝不接受客户端传来的 `userId`** —— 跨账号隔离靠这一条 + RLS。
3. **网关解析顺序**：① 库里该用户（按 JWT 的 userId）的 Key → ② 迁移期兼容：本次请求体里的 `userKey`
   （旧客户端还在传，用完即弃）→ ③ 都没有 → `source:'none'` + 报"请填写你自己的 Key"。
   **平台 Key 兜底整段删除**：`resolvePlatformSecret()` 与相关 import 一并删掉（不留死代码）；
   `api/admin` 的「配置中心」与 `app_config` 表**没动**（那是另一条链路）。`keySource` 记账保持
   （用了用户 Key 记 `'user'`，都没有记 `'none'`）。
4. **客户端与界面**（跨设备的关键）：
   - `src/utils/mcpConfig.ts` 本地只留**非密钥**配置（id / 名称 / 地址 / 启用 / kind）；
     新增 `loadUserKeyStatuses()` / `saveUserKey()` / `clearUserKey()`；删掉"读本机明文 Key"的
     `getUserKeyForProvider()`（不允许再有两处真相）；本机旧 `secretKey` 一次性 `keySet` 迁移到账号，
     **成功后删除本机副本**（失败则保留副本并如实提示）；
   - `src/utils/dataPoolClient.ts`：上一轮的 `withUserKey()` 通道**删除**（含 `listingFetchExecutor.ts`
     里的用法）—— 服务端既已按 JWT 取 Key，客户端再传一份只是多一次明文在网络上流动的机会；
     `ProviderVerifyResult.keySource` 保留（服务端会回报）；
   - 设置页 MCP tab（`src/components/AiSettingsPanel.tsx`）：登录后从 `keyList` 读状态；**已配置**时显示
     `已配置（sk-1…ab12）` + 「替换」/「清除」，**未配置**显示输入框；替换输入框写完即清空
     （**页面上永远不出现明文**）；名称 / 地址 / Key / 验证 四样齐全；一行 ≤30 字提示
     「密钥存在你的账号里，换设备登录自动带上；服务端不回传明文。」

**红线（一条都没破）**：平台密钥仍不得在浏览器出现任何明文（政策 A 之后它连兜底都不是）；
任何响应体 / 日志 / 审计详情里都不许出现明文密钥（**用户自己的也不行**，只给掩码指纹）；
跨账号隔离（A 读不到 / 改不了 / 清不掉 B 的）；校验与长度上限；错误信息脱敏。

**测试与验证**：`tests/securityKeys.test.ts` 15 → **17** 条（新增"密钥只许写 `user_provider_keys` 且只有
service_role 能读""userId 只认 JWT：三个动作都不接受客户端 userId""返回体只允许 `maskKey` 指纹"等），
`tests/dataPoolKeyFlow.test.ts` 9 → **13** 条（新增"客户端不再传 Key""keySet 不带 userId""迁移成功即删本机副本 /
失败保留副本""服务端就算回了明文也不采纳"），`tests/migrationSql.test.ts` 保持 **14** 条（表名集合与新表逐字溯源）。
`node tests/runAll.mjs` → **49 套件 / 535 断言 / 0 failed**；`tsc --noEmit`、`vite build` 均通过。

**已知遗留（没有掩饰）**：① `api/admin/[action].ts` 的缺表自检清单未加这张表（该文件不在本轮授权范围），
未跑 011 的库要靠 MCP 面板的琥珀色提示（`keyList` 返回 `needsMigration`）发现；
② 多个「自定义 MCP」条目共用 `provider='custom'` 那一格 Key（要按条目分开得给表加 `entry_id` 维度）；
③ 真·登录时刻的迁移触发点在 `src/App.tsx`，本轮不在授权范围，改由"打开 MCP 设置页 / 调数据池"两条等价时机触发。
细节见 `docs/byo-key-model.md`。

---

### 15.29 线上库迁移落地 + RLS 隔离实测 + 本地依赖树事故复盘（2026-09-16）

本轮三件事都不是产品功能改动，但都决定"能不能放心上商用"。

**① 线上库迁移执行完毕（用户选 PAT 路线，由我执行）**

- 迁移前线上只有 `projects` / `project_members` 两张表；`usage_events`、`audit_events`、`pool_cache`、
  `app_config`、`user_provider_keys` 与 `usage_events.key_source` 全缺 —— 这正是"跨设备密钥"用不了的原因
  （界面走琥珀色"缺表"提示，不静默失败）。
- 本轮 `SUPABASE_PAT=<pat> node scripts/run-migration.mjs` 执行 `all_in_one.sql`（23007 字符 / 609 行）：
  **7/7 张表就绪 ｜ RLS 七张全开 ｜ `usage_events.key_source` 已存在**。
- 用户 Key 的安全性口径不变：PAT 只从环境变量读，不写盘、不进 git、不回显；**用完由用户撤销该 token**。

**② "RLS 写了策略"不等于"别人真的读不到"——做了真请求验证（新增上线清单 2.0c）**

做法：用 PAT 插一条假密钥行（`user_id` 为随机 uuid）→ 用 anon key 走真实 PostgREST 请求读 / 插 / 改 / 删
→ 再用 PAT 核对结果 → 删除假行。实测结果：

| 攻击面 | 结果 |
| --- | --- |
| anon 读 7 张表 | 全部 HTTP 200 + **0 行**（那条真实存在的假行也看不到） |
| anon 插入 | **401 / `42501`**：`new row violates row-level security policy` |
| anon DELETE | HTTP 200 + 返回体 `[]`，**影响 0 行**（假行仍在） |
| anon UPDATE（改成 `HACKED`） | HTTP 200 + 返回体 `[]`，**值未被篡改** |

结论：**登录这个网页的其他账号既拿不到、也改不了、更删不掉你的 Key** —— 这是直接证据，不是"策略看起来很严"。
（注意纯 `204/200` 不等于生效：第一次验证时我把 anon DELETE 的 `204` 误读成"可能删掉了"，补了一次结果核对才确认。）

**③ 本地依赖树事故（不是源码问题，但要记住教训）**

- 现象：`tsc` / `node tests/runAll.mjs` / `vite build` 三条命令**全部**报 `MODULE_NOT_FOUND`
  （`tsx/dist/cli.mjs`、`typescript/bin/tsc`、`vite/bin/vite.js` 都不在）。
- 根因：同一工作区里另有一次 `pnpm install` **中途被打断**，`node_modules` 里 47 个包的入口文件被删空，
  根目录还残留 npm 时代的 410 个旧实体目录（与 pnpm 的符号链接混在一起）。
- 修法：本机没有 npm，用 harness 自带的 pnpm，**先 `pnpm import` 把 `package-lock.json` 转成 `pnpm-lock.yaml`
  （版本完全对齐），再 `install --frozen-lockfile`**；否则 pnpm 会按 `^`/`~` 重新解析，装出与 Vercel 不一致的树。
- 口径已写进 `AGENTS.md`：本机唯一的安装器、正确安装姿势、验证三件套与基线（49 套件 / 538 断言 / 0 失败），
  以及"**三件套报模块找不到时先怀疑依赖树，不要先怀疑源码**"。

**新增工具**：`scripts/check-db-status.mjs` —— 只用 anon key、只发 GET 的迁移状态探针
（用 `PGRST205` 区分"表不存在"，用"200 + 空数组"识别"表在但 RLS 全拒"即已就绪），
不需要 PAT 就能随时回答"数据库迁没迁"。它和 `run-migration.mjs` 各加了两条安全守卫
（只读、不落盘、不回显密钥值），`tests/securityHygiene.test.ts` 9 → **11** 条。

**验证**：`tsc --noEmit` exit 0 ｜ `node tests/runAll.mjs` **49 套件 / 538 断言 / 0 失败** ｜ `vite build` exit 0。

---

## 17. 国内底座迁移（后置，另行立项）

> 用户已确认（§15.1-23）：现阶段沿用 Vercel + Supabase，先把 App 功能做好；国内迁移（免 VPN）在功能打磨完成后另行立项。

**为什么后置是合理的**：当前是"内部团队自用为主"，海外访问可用 VPN 扛；而国内迁移的 ICP 备案（首次约 1-2 周）、数据库改造、认证改造都是重活，现在做会拖慢产品本身。

**迁移目标（届时再细化）**：

| 层 | 现在 | 国内候选 | 说明 |
| --- | --- | --- | --- |
| 数据库 | Supabase Postgres | 腾讯云 CloudBase 云数据库 / 阿里云 RDS | projects/members 表迁移 |
| 认证 | Supabase Auth（已自建 JWT+pbkdf2） | 自建（查 app_users 表）或 CloudBase 认证 | 代码已有自建认证雏形，迁移成本低 |
| 存储 | Supabase Storage | 七牛 Kodo / 腾讯云 COS / CloudBase 存储 | 报告资产、竞品图包 |
| 托管 | Vercel | CloudBase 静态托管+云函数 / 国内 ECS | 前端 + api/* |

**迁移前置门槛**：域名 + ICP 备案（企业/个体户主体），首次约 1-2 周——这是国内免 VPN 的第一道闸，比选哪家云更优先。

**迁移友好护栏（现在就要遵守，避免将来被绑死）**：
1. **数据层已抽象在 `api/*` 服务端函数 + 数据池网关之后**，前端不直接接触 Supabase（现状已如此，§2.6/§11.2）——迁移时只改服务端，前端零改动；
2. 新增代码**不要**在组件层直接 import Supabase 客户端，一律走 api/* 或数据层封装；
3. 认证 token 逻辑（自建 JWT + pbkdf2）保持独立，不依赖 Supabase Auth 的专属特性。

**已知风险（后置期间接受）**：Vercel/Supabase 国内访问不稳定、Supabase 免费额度有限——内部自用阶段用 VPN/海外节点容忍；对外产品化（Phase B）前必须完成国内迁移。


---

*PRD V2.11 完。配套 UI 线框图：`docs/ui-wireframes.html`（主流程 8 屏）、`docs/ui-wireframes-l3.html`（二级页与明细层 23 屏）；一致性核对表：`docs/ui-conformance-audit.md`（含 ⚠️ 用户指示的偏离清单）；界面借鉴样张：`docs/ui-benchmark-mockups.html`。交付分支：只有 `main`（生产）；发布标签 `v2.9.0`。*

